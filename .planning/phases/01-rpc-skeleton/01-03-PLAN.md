---
phase: 01-rpc-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - ui-ink/src/__tests__/tools-fs.test.ts
  - tools/fs.py
  - tools/__init__.py
  - tests/test_fs.py
autonomous: false
requirements:
  - RPC-03
  - RPC-05
tags:
  - rpc
  - vitest
  - regression
  - deletion
  - manual-verify

must_haves:
  truths:
    - "ui-ink/src/__tests__/tools-fs.test.ts 에 read_file 5케이스 (성공/offset+limit/존재안함/디렉토리/대용량 offset) 가 vitest 로 작성되어 green (D-17)"
    - "tools/fs.py 에서 read_file 함수가 deletion 되었다 (D-18)"
    - "tools/__init__.py 의 TOOL_MAP 에서 'read_file' 항목과 TOOL_DEFINITIONS 의 read_file 엔트리가 deletion 되지 않고 유지된다 — LLM 이 여전히 read_file tool schema 를 보고 호출하지만 실행은 클라 측 (Plan 02 의 CLIENT_SIDE_TOOLS 분기가 잡음)"
    - "단, tools/fs.py 에서 read_file 본체가 빠졌으므로 tools/__init__.py 의 from .fs import 라인에서 read_file 만 제외하고 TOOL_MAP['read_file'] 매핑도 제거 — CLIENT_SIDE_TOOLS 분기로만 동작 (실수로 우회 호출 시 KeyError 대신 명시적 분기 누락이 드러나도록)"
    - "tests/test_fs.py 의 TestReadWriteEdit 안 read_file 관련 3건 (test_write_and_read_round_trip 의 read 부분 / test_read_offset_limit / test_read_accepts_file_path_alias) 이 정리되었다 — read 검증은 vitest 로 이전, write_file/edit_file 케이스는 Phase 2 까지 유지"
    - "전체 pytest 회귀: 기존 199건 - read_file 관련 deletion (~3건) + Plan 02 신규 7건 = ~203건 green"
    - "전체 vitest 회귀: 기존 + 신규 5케이스 + (Plan 01 의) protocol/dispatch 회귀 = green"
    - "수동 검증 통과: 외부 PC (또는 같은 macOS 의 다른 cwd) ui-ink → 'CLAUDE.md 읽어줘' → LLM 이 read_file 호출 → 클라 측 파일이 읽혀서 LLM 응답에 인용됨. 서버 PC 의 CLAUDE.md 가 아닌 클라 PC 의 CLAUDE.md 가 들어옴 (D-19)"
  artifacts:
    - path: "ui-ink/src/__tests__/tools-fs.test.ts"
      provides: "read_file 5케이스 vitest 회귀"
      min_lines: 60
    - path: "tools/fs.py"
      provides: "read_file 함수 본체 deletion (write_file/edit_file/list_files/grep_search 는 Phase 2 까지 유지)"
    - path: "tools/__init__.py"
      provides: "from .fs import 에서 read_file 제거 + TOOL_MAP['read_file'] 항목 제거 (TOOL_DEFINITIONS 의 read_file schema 는 유지 — LLM 이 호출 schema 알아야 함)"
    - path: "tests/test_fs.py"
      provides: "read_file 관련 케이스 deletion (write/edit/sandbox/grep/list 는 유지)"
  key_links:
    - from: "ui-ink/src/__tests__/tools-fs.test.ts"
      to: "ui-ink/src/tools/fs.ts"
      via: "import {readFile}"
      pattern: "import.*readFile.*from.*tools/fs"
    - from: "tools/__init__.py"
      to: "tools/fs.py"
      via: "from .fs import (read_file 제외)"
      pattern: "from .fs import"
    - from: "수동 검증"
      to: "외부 PC 의 read_file 결과"
      via: "ui-ink → harness_server → agent → CLIENT_SIDE_TOOLS 분기 → ws → 클라 readFile → ws → agent → LLM"
      pattern: "D-19"
---

<objective>
Phase 1 의 회귀/마무리/검증을 한 번에 처리한다. Plan 01+02 가 만든 RPC 골격이 정확히 동작함을 (1) vitest 5케이스로 자동 회귀, (2) 기존 Python 측 read_file 본체와 pytest 의 read_file 케이스 deletion (D-18), (3) 외부 PC ui-ink 수동 검증 (D-19) 으로 closure 한다.

Purpose: PoC 의 정의 자체가 "외부 PC ui-ink 가 자기 PC read_file 결과를 LLM 컨텍스트로 받음" — Plan 03 가 이 closure 를 책임진다. Python 측 dual-stack 을 끝내야 정책 (CLAUDE.md "legacy 삭제 default") 도 충족.

Output:
- ui-ink/src/__tests__/tools-fs.test.ts (신규) — vitest 5케이스
- tools/fs.py (수정 — read_file 함수 본체 deletion)
- tools/__init__.py (수정 — from .fs import 갱신 + TOOL_MAP 에서 read_file 매핑 제거. TOOL_DEFINITIONS 의 read_file schema 는 유지)
- tests/test_fs.py (수정 — read_file 케이스 deletion)
- 수동 검증 체크포인트 — 사용자가 외부 PC 시나리오로 D-19 통과 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/CLIENT-TOOLS-DESIGN.md
@.planning/phases/01-rpc-skeleton/01-CONTEXT.md
@.planning/phases/01-rpc-skeleton/01-PATTERNS.md
@.planning/phases/01-rpc-skeleton/01-01-SUMMARY.md
@.planning/phases/01-rpc-skeleton/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 03 가 의존하는 핵심 interface. -->

Plan 01 의 readFile (ui-ink/src/tools/fs.ts) 시그니처:
```typescript
export async function readFile(rawArgs: Record<string, unknown>): Promise<ToolResult>
// 성공: {ok: true, content: string, total_lines: number, start_line: number, end_line: number}
// 실패: {ok: false, error: string}
```

기존 pytest read_file 케이스 (tests/test_fs.py:56-83 — 동등 변환 ground truth):

```python
class TestReadWriteEdit:
    def test_write_and_read_round_trip(self, tmp_path):
        # write 후 read — total_lines/content 검증

    def test_read_offset_limit(self, tmp_path):
        # offset=3, limit=2 → start_line=3, end_line=4, line2/line3 만

    def test_read_accepts_file_path_alias(self, tmp_path):
        # file_path= 인자 호환 — alias 정규화는 agent.py 에서 (D-16) 하므로 vitest 측은 path 만 검증
```

vitest 스타일 (ui-ink/src/__tests__/protocol.test.ts ground truth):

```typescript
import {describe, it, expect} from 'vitest'
// node:test 아님 — vitest 사용 (package.json 의 "test": "vitest run" 확인됨)
```

tools/__init__.py 의 read_file 매핑 (line 1, 311 — 두 위치 수정):

```python
# line 1
from .fs import read_file, write_file, edit_file, list_files, grep_search
# line 311
TOOL_MAP = {
    'read_file': read_file,
    ...
}
# line 21-38 (TOOL_DEFINITIONS read_file schema — 유지!)
TOOL_DEFINITIONS = [
    {'name': 'read_file', ...},  # LLM 이 이 schema 보고 read_file 호출. 유지 필수.
    ...
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ui-ink/src/__tests__/tools-fs.test.ts — readFile vitest 5케이스</name>
  <files>ui-ink/src/__tests__/tools-fs.test.ts</files>
  <read_first>
    - ui-ink/src/tools/fs.ts (Plan 01 결과 — readFile 시그니처와 반환 schema)
    - ui-ink/src/__tests__/protocol.test.ts (vitest 스타일 reference)
    - tests/test_fs.py:56-83 (5케이스 의 Python 동등 — write_and_read_round_trip / read_offset_limit / read_accepts_file_path_alias 그리고 추가로 존재안함/디렉토리/대용량 offset)
    - .planning/phases/01-rpc-skeleton/01-CONTEXT.md (D-17)
    - .planning/phases/01-rpc-skeleton/01-PATTERNS.md (line 617-732 — vitest 패턴 ground truth)
  </read_first>
  <behavior>
    - **케이스 1 (성공/전체)**: 두 줄 파일 → ok=true, total_lines=2, content 에 'hello' 와 'world' 포함, content 가 cat -n 스타일 (' '*3+'1\thello\n'+' '*3+'2\tworld\n' 같은 형태)
    - **케이스 2 (offset+limit)**: 10줄 파일 (line0..line9) + offset=3, limit=2 → start_line=3, end_line=4, content 에 'line2' 와 'line3' 만 (line0/line1/line4 없음)
    - **케이스 3 (존재 안 함)**: 존재 안 하는 경로 → ok=false (error 메시지 존재)
    - **케이스 4 (디렉토리)**: tmpdir 자체 경로 → ok=false (EISDIR 또는 동등)
    - **케이스 5 (대용량 offset)**: 2줄 파일 + offset=100, limit=10 → ok=true, content 빈 문자열, sliced 가 빈 배열이라 안전
    - 모든 케이스: beforeEach 로 mkdtemp → afterEach 로 fs.rm({recursive, force}) — 격리 보장
  </behavior>
  <action>
    1. ui-ink/src/__tests__/tools-fs.test.ts 신규 작성. 기존 vitest 스타일 (`__tests__/protocol.test.ts`, `dispatch.test.ts`) 따름:

       ```typescript
       // RPC-05 (D-17): read_file 5케이스 vitest 회귀.
       // Python tests/test_fs.py:TestReadWriteEdit (line 56-83) 의 동등 변환 + 존재안함/디렉토리/대용량 offset 추가.
       import {describe, it, expect, beforeEach, afterEach} from 'vitest'
       import {promises as fs} from 'node:fs'
       import {tmpdir} from 'node:os'
       import {join} from 'node:path'
       import {readFile} from '../tools/fs.js'

       describe('readFile (RPC-03)', () => {
         let dir: string

         beforeEach(async () => {
           dir = await fs.mkdtemp(join(tmpdir(), 'harness-fs-'))
         })
         afterEach(async () => {
           await fs.rm(dir, {recursive: true, force: true})
         })

         it('성공 — 전체 읽기 시 줄 번호 prefix + total_lines 반환', async () => {
           const p = join(dir, 'sample.txt')
           await fs.writeFile(p, 'hello\nworld\n')
           const r = await readFile({path: p})
           expect(r.ok).toBe(true)
           if (r.ok) {
             expect(r['total_lines']).toBe(2)
             expect(r['content']).toContain('hello')
             expect(r['content']).toContain('world')
             // cat -n 스타일 — 4자리 right-aligned padding + tab
             expect(r['content']).toMatch(/^\s+1\thello\n/)
           }
         })

         it('성공 — offset/limit 분기 (Python test_read_offset_limit 동등)', async () => {
           const p = join(dir, 'multi.txt')
           const lines = Array.from({length: 10}, (_, i) => `line${i}\n`).join('')
           await fs.writeFile(p, lines)
           const r = await readFile({path: p, offset: 3, limit: 2})
           expect(r.ok).toBe(true)
           if (r.ok) {
             expect(r['start_line']).toBe(3)
             expect(r['end_line']).toBe(4)
             expect(r['content']).toContain('line2')
             expect(r['content']).toContain('line3')
             expect(r['content']).not.toContain('line0')
             expect(r['content']).not.toContain('line4')
           }
         })

         it('실패 — 존재하지 않는 파일은 ok=false', async () => {
           const r = await readFile({path: join(dir, 'nope.txt')})
           expect(r.ok).toBe(false)
           if (!r.ok) {
             expect(typeof r.error).toBe('string')
             expect(r.error.length).toBeGreaterThan(0)
           }
         })

         it('실패 — 디렉토리는 ok=false (EISDIR)', async () => {
           const r = await readFile({path: dir})
           expect(r.ok).toBe(false)
         })

         it('대용량 offset — 파일 끝 너머 offset 도 안전 (sliced 빈 배열)', async () => {
           const p = join(dir, 'small.txt')
           await fs.writeFile(p, 'a\nb\n')
           const r = await readFile({path: p, offset: 100, limit: 10})
           expect(r.ok).toBe(true)
           if (r.ok) {
             expect(r['content']).toBe('')
             // sliced 빈 배열 — start=99, end_line = start + 0 = 99
             expect(r['start_line']).toBe(100)
             expect(r['end_line']).toBe(99)
           }
         })

         it('인자 누락 — path 없으면 ok=false 명시 에러', async () => {
           const r = await readFile({})
           expect(r.ok).toBe(false)
           if (!r.ok) {
             expect(r.error).toContain('path')
           }
         })
       })
       ```

       (총 6개의 it — D-17 의 5케이스 + path 누락 1건. 5케이스 약속은 핵심 schema 케이스만 셈. path 누락은 readFile 의 가드 검증 추가분.)

       **Important — 대용량 offset 케이스의 end_line 값**: Python tools/fs.py:read_file 의 로직과 동등하게 검증. start = max(1, 100) - 1 = 99, sliced = lines[99:99+10] = [] (lines 길이 2), end_line = start + len(sliced) = 99. start_line = start + 1 = 100. (PATTERNS.md 가 99 로 표기했으나 실제 Python 동등 동작 검증을 위해 readFile 코드 실행 후 확인 권장 — 만약 readFile 의 산출이 다르면 fs.ts 측 수정이 필요한 게 아니라 Python 측 동등성 재확인 — 이 케이스는 expect 값을 readFile 실제 output 에 맞춤. 빈 sliced 시 start_line=100/end_line=99 가 Python 의 'start + 1 / start + len(sliced)' 와 일치.)

    2. CLAUDE.md 규칙 — 한국어 주석, single quote, 세미콜론 없음, 2 spaces.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink &amp;&amp; bun run test -- tools-fs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ui-ink/src/__tests__/tools-fs.test.ts` 존재
    - `grep -q "describe('readFile" ui-ink/src/__tests__/tools-fs.test.ts` 매치
    - `grep -c "it(" ui-ink/src/__tests__/tools-fs.test.ts` 결과 ≥ 5
    - `grep -q "mkdtemp" ui-ink/src/__tests__/tools-fs.test.ts` 매치 (격리 fixture)
    - `cd ui-ink &amp;&amp; bun run test -- tools-fs` exit 0 (모든 it green)
    - `cd ui-ink &amp;&amp; bun run test` 전체 회귀 0
  </acceptance_criteria>
  <done>
    vitest 5케이스 + path 누락 가드 1건 = 6 it green. Plan 01 의 readFile 이 Python read_file schema 와 동등함을 자동 보장.
  </done>
</task>

<task type="auto">
  <name>Task 2: Python 측 read_file deletion — tools/fs.py + tools/__init__.py + tests/test_fs.py</name>
  <files>tools/fs.py, tools/__init__.py, tests/test_fs.py</files>
  <read_first>
    - tools/fs.py (line 55-79 — read_file 본체. 정확한 line 범위와 함수 경계 확인)
    - tools/__init__.py (line 1 — from .fs import 라인, line 21-38 — TOOL_DEFINITIONS 의 read_file 항목, line 310-329 — TOOL_MAP)
    - tests/test_fs.py (전체 — read_file 관련 테스트 위치 식별: TestReadWriteEdit 의 test_write_and_read_round_trip, test_read_offset_limit, test_read_accepts_file_path_alias)
    - .planning/phases/01-rpc-skeleton/01-CONTEXT.md (D-18)
    - .planning/phases/01-01-SUMMARY.md, 01-02-SUMMARY.md (Plan 01+02 완료 상태 확인)
  </read_first>
  <behavior>
    - tools/fs.py 에서 read_file 함수 본체만 deletion (write_file/edit_file/list_files/grep_search/_PRUNE_DIRS/_unified_diff/_resolve_path/set_sandbox 는 모두 유지)
    - tools/__init__.py 의 from .fs import 에서 read_file 만 제거
    - tools/__init__.py 의 TOOL_MAP 에서 'read_file': read_file 매핑 제거
    - tools/__init__.py 의 TOOL_DEFINITIONS 의 read_file schema 항목은 유지 — LLM 이 호출 schema 를 보고 호출 결정함. 유지 안 하면 LLM 이 read_file 을 호출 안 하게 됨
    - tests/test_fs.py 에서 read_file 만 의존하는 케이스 제거: test_read_offset_limit, test_read_accepts_file_path_alias 두 건. test_write_and_read_round_trip 은 write 검증을 read 로 확인하는 구조라 → write 결과 자체를 path.read_text() 로 검증하도록 수정 (read_file 의존 제거)
    - 다른 테스트 (TestSandbox, TestEdit*, TestList*, TestGrep*) 는 그대로 유지
    - import error / unknown attribute 발생하지 않음
  </behavior>
  <action>
    1. tools/fs.py 의 read_file 함수 (line 55-79) 본체 통째 deletion. _resolve_path 와 write_file 사이의 def read_file ~ except Exception ~ return ... 까지 전체 삭제. 이전/이후 줄의 빈 줄 1개는 유지 (가독성). _resolve_path 다음에 write_file 이 바로 오게 됨.

    2. tools/__init__.py 의 line 1 수정 — read_file 만 제거:

       ```python
       # 기존
       from .fs import read_file, write_file, edit_file, list_files, grep_search
       # 변경
       from .fs import write_file, edit_file, list_files, grep_search
       ```

    3. tools/__init__.py 의 TOOL_MAP (line 310-329) 에서 'read_file': read_file 한 줄 제거:

       ```python
       # 기존
       TOOL_MAP = {
           'read_file': read_file,
           'write_file': write_file,
           ...
       }
       # 변경
       TOOL_MAP = {
           # 'read_file' 은 클라 측 (RPC-02 / agent.CLIENT_SIDE_TOOLS) — 서버 dispatch 안 함
           'write_file': write_file,
           ...
       }
       ```

    4. tools/__init__.py 의 TOOL_DEFINITIONS (line 21-38) 의 read_file schema 항목은 그대로 유지. 변경 없음. 위 dict 항목 (name='read_file', description, parameters) 은 LLM 이 호출 형태를 알아야 하므로 필수.

    5. tests/test_fs.py 수정:

       - test_read_offset_limit (line 67-76) 통째 deletion
       - test_read_accepts_file_path_alias (line 78-83) 통째 deletion
       - test_write_and_read_round_trip (line 57-65) 수정: read_file 의존 제거 → 직접 path.read_text() 로 검증:

         ```python
         def test_write_and_read_round_trip(self, tmp_path):
             '''write_file 의 round-trip 검증 — read 부분은 vitest 로 이전 (Phase 1).'''
             path = tmp_path / 'sample.txt'
             result = fs.write_file(str(path), 'hello\nworld\n')
             assert result['ok'] is True
             # read_file 은 클라 측 — Python 에서 직접 path.read_text 로 round-trip 검증
             assert path.read_text() == 'hello\nworld\n'
         ```

         (또는 이름을 test_write_round_trip 으로 변경해도 됨 — semantics 더 정확.)

       - 다른 케이스는 손대지 않음 (TestSandbox 의 6건, TestReadWriteEdit 의 edit_* 4건, TestListFiles, TestGrepSearch 등 모두 유지)

    6. 검증:
       - Python import: `.venv/bin/python -c "from tools import fs; print('read_file' in dir(fs))"` → False 출력
       - Python TOOL_MAP: `.venv/bin/python -c "from tools import TOOL_MAP; print('read_file' in TOOL_MAP)"` → False
       - Python TOOL_DEFINITIONS: `.venv/bin/python -c "from tools import TOOL_DEFINITIONS; names = [t['name'] for t in TOOL_DEFINITIONS]; assert 'read_file' in names, names"` → 통과 (schema 유지)
       - pytest 회귀: `.venv/bin/python -m pytest tests/ -x` → 기존 199건 - 3건 deletion + Plan 02 의 7건 = 약 203건 green
       - agent.py 의 CLIENT_SIDE_TOOLS 분기 + Plan 02 의 회귀 테스트는 read_file 호출이 rpc_call 위임으로 가서 영향 없음

    7. CLAUDE.md 규칙 — Python 4 spaces, 한국어 주석.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness &amp;&amp; .venv/bin/python -c "from tools import fs, TOOL_MAP, TOOL_DEFINITIONS; assert not hasattr(fs, 'read_file'); assert 'read_file' not in TOOL_MAP; assert 'read_file' in [t['name'] for t in TOOL_DEFINITIONS]" &amp;&amp; .venv/bin/python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^def read_file" tools/fs.py` 매치 안 됨 (deletion 확인)
    - `grep -q "from .fs import.*read_file" tools/__init__.py` 매치 안 됨
    - `grep -q "'read_file': read_file" tools/__init__.py` 매치 안 됨
    - `grep -q "'name': 'read_file'" tools/__init__.py` 매치 (TOOL_DEFINITIONS 의 schema 유지)
    - `grep -q "test_read_offset_limit" tests/test_fs.py` 매치 안 됨
    - `grep -q "test_read_accepts_file_path_alias" tests/test_fs.py` 매치 안 됨
    - `.venv/bin/python -c "from tools import fs; assert not hasattr(fs, 'read_file')"` exit 0
    - `.venv/bin/python -c "from tools import TOOL_MAP; assert 'read_file' not in TOOL_MAP"` exit 0
    - `.venv/bin/python -c "from tools import TOOL_DEFINITIONS; assert 'read_file' in [t['name'] for t in TOOL_DEFINITIONS]"` exit 0 (schema 보존)
    - `.venv/bin/python -m pytest tests/ -x` exit 0 (회귀 0 — read_file 케이스 deletion 후 Plan 02 의 신규 7건 포함하여 통과)
  </acceptance_criteria>
  <done>
    Python 측 read_file 본체 + TOOL_MAP 매핑 + pytest read_file 케이스 deletion 완료. TOOL_DEFINITIONS 의 schema 는 유지 (LLM 호출 schema). pytest 전체 green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): 외부 PC 수동 검증 — D-19 시나리오</name>
  <files>(N/A — 코드 수정 없음. 사용자 수동 검증 only)</files>
  <action>(체크포인트 — 아래 how-to-verify 의 단계대로 실제 ui-ink 클라를 외부 PC 또는 다른 cwd 에서 띄워 D-19 시나리오를 사용자가 직접 실행. Claude 측 자동 작업 없음.)</action>
  <verify>(자동 verify 없음 — 사용자가 LLM 응답 본문에서 외부 PC 의 CLAUDE.md 마커 텍스트를 직접 확인하고 resume-signal 입력)</verify>
  <done>사용자가 approved 또는 통과 명시 입력. 실패 시 디버깅 단서 + 어느 단계에서 깨졌는지 기재.</done>
  <what-built>
    Phase 1 의 PoC 는 "외부 PC ui-ink → 자기 PC read_file → LLM 컨텍스트 삽입" 라운드트립이 동작하는지 자동 테스트로는 일부만 커버됨. Plan 01 + 02 + 03 Task 1+2 까지 완료된 시점에 사용자가 직접 외부 PC (또는 같은 macOS 의 다른 cwd) 에서 ui-ink 를 띄워 실제 동작을 확인해야 D-19 가 closure 됨.
  </what-built>
  <how-to-verify>
    아래 시나리오는 D-19 의 verbatim. 외부 PC 가 없으면 같은 macOS 의 다른 cwd 로 simulate.

    **준비 (서버 PC)**:
    1. 집 머신에서 harness_server.py 실행:
       ```
       cd /Users/johyeonchang/harness
       .venv/bin/python harness_server.py
       ```
       기본 BIND=127.0.0.1 (외부 노출 시 HARNESS_BIND=0.0.0.0). 포트 7891.
    2. 별도 터미널에서 서버 PC 의 cwd 안에 marker 파일 만들지 말 것 — 기본 CLAUDE.md 가 이미 있음. 서버 PC 에서는 더 이상 read_file 이 도는 게 아니라는 점을 검증해야 하므로 서버 PC 의 CLAUDE.md 내용을 한 번 cat 해서 외부 PC 의 것과 다르다는 걸 미리 비교용으로 저장.

    **외부 PC (또는 같은 macOS 의 다른 cwd)**:
    3. 새 cwd 만들고 그 안에 다른 내용의 CLAUDE.md 작성:
       ```
       mkdir -p /tmp/harness-client-test && cd /tmp/harness-client-test
       echo '# 외부 PC 마커' > CLAUDE.md
       echo '이 파일은 외부 PC 의 CLAUDE.md 입니다 (수동 검증용).' >> CLAUDE.md
       ```
    4. ui-ink 클라이언트 띄움 (서버 PC 와는 다른 cwd 에서):
       ```
       cd /Users/johyeonchang/harness/ui-ink   # ui-ink 소스 위치
       HARNESS_URL=ws://127.0.0.1:7891 \
       HARNESS_TOKEN=<설정된 토큰> \
       HARNESS_CWD=/tmp/harness-client-test \
       bun start
       ```
       (외부 PC 시뮬레이션은 같은 머신 + 다른 working_dir 만으로도 충분 — read_file 이 클라 측에서 도는지 검증이 핵심.)
    5. 프롬프트 입력: `./CLAUDE.md 읽어줘`
    6. LLM 이 read_file 호출 → 응답에 `# 외부 PC 마커` 와 `이 파일은 외부 PC 의 CLAUDE.md 입니다` 텍스트가 인용되어 나오는지 확인.

    **통과 조건 (D-19 verbatim)**:
    - [x] 외부 PC (또는 다른 cwd) 의 CLAUDE.md 가 LLM 응답에 인용됨
    - [x] 서버 PC 의 CLAUDE.md (현 cwd 의 CLAUDE.md, 즉 harness 프로젝트 루트의 한국어 에이전트 지침) 가 응답에 안 들어옴
    - [x] tool_request 송신 + tool_result 수신 라운드트립이 ws 메시지 로그에 보임 (선택 — 디버깅용)
    - [x] 30s 안에 응답 완료 (D-11 timeout 내)

    **실패 시 디버깅 단서**:
    - 응답에 서버 PC CLAUDE.md 내용이 들어왔으면 → agent.py 의 CLIENT_SIDE_TOOLS 분기가 안 잡힌 것. agent.run 의 rpc_call 인자가 None 으로 들어가는지 확인 (run_agent 의 rpc_call=rpc_call 인자 누락 가능)
    - 응답이 'rpc_call 콜백 없음' 에러로 오면 → harness_server.py 의 run_agent 가 rpc_call 인자를 안 넘긴 것
    - 응답이 'tool_call timeout' 으로 오면 → 클라 측 dispatch.ts 의 tool_request 핸들러가 안 잡혀 결과 안 옴 (Plan 01 Task 3 회귀)
    - 응답이 'tool_call aborted (disconnect)' 으로 오면 → 클라가 disconnect 됐거나 readFile 이 throw 한 후 tool_result 송신이 실패 (registry.ts 의 boundClient 미주입)

    **정상 통과 시**:
    `approved` 입력 또는 "통과 — D-19 외부 PC 시나리오 OK" 명시.
  </how-to-verify>
  <resume-signal>"approved" 또는 "통과 — D-19 외부 PC 시나리오 OK" 또는 디버깅 단서/실패 사례 기재 (실패 시 어떤 단계에서 어떻게 깨졌는지)</resume-signal>
</task>

</tasks>

<verification>
- vitest 전체: `cd ui-ink && bun run test` 전체 green (기존 + 신규 tools-fs.test.ts 5+1 케이스)
- pytest 전체: `cd /Users/johyeonchang/harness && .venv/bin/python -m pytest tests/ -x` 전체 green (~203건)
- 정적 grep:
  - read_file 본체 deletion: `! grep -q "^def read_file" tools/fs.py`
  - TOOL_MAP 매핑 deletion: `! grep -q "'read_file': read_file" tools/__init__.py`
  - TOOL_DEFINITIONS schema 유지: `grep -q "'name': 'read_file'" tools/__init__.py`
  - vitest 5케이스 존재: `[ "$(grep -c 'it(' ui-ink/src/__tests__/tools-fs.test.ts)" -ge 5 ]`
- 수동 검증 통과 (D-19) — 사용자 confirm
</verification>

<success_criteria>
- Phase 1 의 PoC 가 closure: 외부 PC ui-ink 가 자기 PC read_file 결과를 LLM 컨텍스트로 받음 (D-19 통과)
- Python 측 read_file dual-stack 종료 — fs.py:read_file 함수 + TOOL_MAP 매핑 + pytest 케이스 deletion
- TOOL_DEFINITIONS 의 read_file schema 는 유지 — LLM 이 호출 schema 를 인식하고 read_file 도구 호출
- vitest 5+1 케이스 green + pytest 회귀 0
- Phase 2 진입 가능 — write/edit/list/grep 의 클라 이전을 같은 패턴으로 확장
</success_criteria>

<output>
After completion, create `.planning/phases/01-rpc-skeleton/01-03-SUMMARY.md` covering:
- vitest 5+1 케이스 결과 (어떤 it 가 어떤 Python pytest 와 매핑되는지 표)
- tools/fs.py read_file deletion 의 line 범위 변화 (before/after line count)
- tools/__init__.py 의 변경 부위 (from .fs import / TOOL_MAP / TOOL_DEFINITIONS 유지 인용)
- tests/test_fs.py 의 변경 부위 (deletion 한 함수 이름들 + test_write_and_read_round_trip 의 read 의존 제거)
- D-19 수동 검증 결과 (사용자 통과 여부 + 어떤 cwd 에서 어떤 파일이 인용됐는지)
- pytest/vitest 최종 회귀 결과
- Phase 1 closure: REQ-ID RPC-01, RPC-02, RPC-03 (read_file), RPC-05 (read_file) 모두 충족 — Phase 2 진입 준비 완료
</output>
