---
phase: 01-rpc-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - harness_server.py
  - agent.py
autonomous: true
requirements:
  - RPC-01
  - RPC-02
tags:
  - rpc
  - python
  - asyncio
  - agent
  - thread-bridge

must_haves:
  truths:
    - "harness_server.py 의 _dispatch_loop 가 type='tool_result' 메시지를 받으면 ws._pending_calls[call_id] 의 Future 에 결과를 set 한다"
    - "harness_server.py 의 run_agent 안에 rpc_call(name, args, timeout=30.0) -> dict 클로저가 정의되고 agent.run 의 rpc_call 인자로 주입된다"
    - "rpc_call 호출 시 ws 로 {type:'tool_request', call_id, name, args} 가 송신되고 Future 가 set 될 때까지 thread-safe 하게 대기한다 (D-09)"
    - "ws disconnect 시 ws._pending_calls 의 모든 Future 가 cancel 된다 (D-12)"
    - "타임아웃 (default 30s, D-11) 발생 시 rpc_call 이 {ok:false, error:'tool_call timeout (30s)'} 를 반환한다"
    - "agent.py 의 run() 시그니처에 rpc_call=None 인자가 추가되고, fn_name in CLIENT_SIDE_TOOLS 일 때 rpc_call(fn_name, args) 결과를 result 로 사용한다 (D-08)"
    - "agent.py 의 CLIENT_SIDE_TOOLS = {'read_file'} 가 모듈 상단에 정의된다 (D-10)"
    - "agent.py 가 read_file 호출 시 args 의 file_path → path alias 정규화를 수행한다 (D-16)"
    - "기존 pytest 199건 중 fs.read_file 관련 케이스를 제외하고 회귀 0 — agent.run signature 변경이 다른 caller 를 깨뜨리지 않는다"
  artifacts:
    - path: "harness_server.py"
      provides: "rpc_call 클로저 + ws._pending_calls + tool_result dispatch + disconnect cleanup"
      contains: "_pending_calls"
    - path: "agent.py"
      provides: "CLIENT_SIDE_TOOLS 분기 + rpc_call 인자 + alias 정규화"
      contains: "CLIENT_SIDE_TOOLS"
    - path: "tests/test_rpc_bridge.py"
      provides: "tool_result dispatch + disconnect cleanup 회귀 3건"
    - path: "tests/test_agent_client_side_dispatch.py"
      provides: "agent.run 의 CLIENT_SIDE_TOOLS 분기 + alias + fallback 회귀 4건"
  key_links:
    - from: "harness_server.py:run_agent"
      to: "agent.run"
      via: "rpc_call=rpc_call 키워드 인자 주입"
      pattern: "rpc_call=rpc_call"
    - from: "harness_server.py:_dispatch_loop"
      to: "ws._pending_calls"
      via: "case 'tool_result' 의 future.set_result"
      pattern: "_pending_calls\\.get\\(call_id\\)"
    - from: "agent.py:run"
      to: "rpc_call(fn_name, args)"
      via: "CLIENT_SIDE_TOOLS 분기"
      pattern: "fn_name in CLIENT_SIDE_TOOLS"
    - from: "harness_server.py:_run_session finally"
      to: "ws._pending_calls cleanup"
      via: "future.cancel() 일괄"
      pattern: "fut\\.cancel\\(\\)"
---

<objective>
Python 백엔드 (harness_server.py + agent.py) 에 RPC 진입점을 추가한다. 서버가 클라 측 도구를 위임 호출할 수 있게 pending_calls + tool_result dispatch + thread→async bridge 를 구축하고, agent.py 의 도구 dispatch 분기에 CLIENT_SIDE_TOOLS = {'read_file'} 를 끼워넣어 read_file 만 클라 위임으로 전환한다.

Purpose: Plan 01 이 만든 클라 측 RPC 핸들러를 서버가 호출할 수 있어야 PoC 가 동작한다. confirm_write 의 thread→async bridge 패턴 (harness_server.py:274-290, D-09) 을 그대로 응용 — Event 대신 Future 사용 (결과값 운반).

Output:
- harness_server.py (수정) — ws._pending_calls 도입, run_agent 안 rpc_call 클로저, _dispatch_loop tool_result case, _run_session finally 의 disconnect cleanup
- agent.py (수정) — CLIENT_SIDE_TOOLS 상수, run() 의 rpc_call 인자, line 564 직전 분기, file_path alias 정규화
- tests/test_rpc_bridge.py (신규) — pending_calls dispatch 분기 + disconnect cancel 회귀
- tests/test_agent_client_side_dispatch.py (신규) — CLIENT_SIDE_TOOLS 분기 + alias + fallback 회귀
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/CLIENT-TOOLS-DESIGN.md
@.planning/phases/01-rpc-skeleton/01-CONTEXT.md
@.planning/phases/01-rpc-skeleton/01-PATTERNS.md
@.planning/phases/01-rpc-skeleton/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 02 가 참조하는 핵심 interface (Plan 01 결과 + 기존 코드). -->

Plan 01 에서 확정된 메시지 schema (Python 이 이 형태로 송수신):

```typescript
// 서버 → 클라 (Python 이 송신)
{type: 'tool_request', call_id: string, name: string, args: object}

// 클라 → 서버 (Python 이 수신, _dispatch_loop 의 새 case)
{type: 'tool_result', call_id: string, ok: boolean, result?: object, error?: {kind: string, message: string}}
```

기존 confirm bridge 패턴 (harness_server.py:274-290 — D-09 의 ground truth):

```python
def confirm_write(path: str, content: str | None = None) -> bool:
    event = asyncio.Event()
    state._confirm_event = event
    state._confirm_result = False
    asyncio.run_coroutine_threadsafe(
        send(ws, type='confirm_write', path=path,
             old_content=_read_existing_file(path)), loop
    )
    future = asyncio.run_coroutine_threadsafe(
        asyncio.wait_for(event.wait(), timeout=60), loop
    )
    try:
        future.result(timeout=61)
    except Exception:
        pass
    return state._confirm_result
```

기존 confirm_write_response dispatch case (harness_server.py:830-837 — analog):

```python
elif t == 'confirm_write_response':
    if ws is not room.active_input_from:
        continue
    state._confirm_result = msg.get('accept', msg.get('result', False))
    if state._confirm_event:
        state._confirm_event.set()
```

기존 agent.py:run signature (line 416-431):

```python
def run(
    user_input: str,
    session_messages: list = None,
    working_dir: str = '.',
    profile: dict = None,
    context_snippets: str = '',
    plan_mode: bool = False,
    on_token=None,
    on_tool=None,
    confirm_write=None,
    confirm_bash=None,
    on_unknown_tool=None,
    on_thought=None,
    on_thought_end=None,
    hooks: dict = None,
) -> tuple[str, list]:
```

기존 agent.py 의 dispatch 진입점 (line 560-578 — 분기 삽입 위치):

```python
# shell 툴은 working_dir를 cwd로 자동 주입
if fn_name in ('run_command', 'run_python') and 'cwd' not in args:
    args = {**args, 'cwd': working_dir}

fn = TOOL_MAP.get(fn_name)
if fn:
    try:
        result = fn(**args)
    except TypeError as e:
        result = {'ok': False, 'error': f'인자 오류: {e}'}
else:
    result = {
        'ok': False,
        'error': f'툴 "{fn_name}"은 존재하지 않습니다. ...',
        '_unknown_tool': fn_name,
    }
    unknown_tool_count += 1
    if on_unknown_tool:
        on_unknown_tool(fn_name, args)
```

기존 _run_session finally 블록 (harness_server.py:969-978 — disconnect cleanup 추가 위치):

```python
finally:
    room.subscribers.discard(ws)
    if is_shared and room.subscribers:
        try:
            await broadcast(room, type='room_member_left',
                            subscribers=len(room.subscribers))
        except Exception:
            pass
    _maybe_drop_room(room)
```

run_agent 의 agent.run 호출부 (harness_server.py:362-374 + ephemeral 350-360 — 두 호출 모두 rpc_call 인자 추가):

```python
_, state.messages = agent.run(
    user_input,
    session_messages=state.messages,
    working_dir=state.working_dir,
    profile=state.profile,
    context_snippets=snippets if isinstance(snippets, str) else '',
    plan_mode=plan_mode,
    on_token=on_token,
    on_tool=on_tool,
    confirm_write=confirm_write if state.profile.get('confirm_writes', True) else None,
    confirm_bash=confirm_bash if state.profile.get('confirm_bash', True) else None,
    hooks=state.profile.get('hooks', {}),
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: harness_server.py 에 pending_calls + rpc_call 클로저 + tool_result dispatch + disconnect cleanup</name>
  <files>harness_server.py, tests/test_rpc_bridge.py</files>
  <read_first>
    - harness_server.py 전체 (특히 line 1-30 import, line 80-85 send 헬퍼, line 237-400 run_agent, line 793-870 _dispatch_loop, line 871-980 _run_session)
    - .planning/phases/01-rpc-skeleton/01-CONTEXT.md (D-05, D-06, D-07, D-09, D-11, D-12, D-13)
    - .planning/phases/01-rpc-skeleton/01-PATTERNS.md (line 26-165 — harness_server.py 섹션, rpc_call 변환 패턴 ground truth)
    - tests/test_room_state.py 또는 기존 server 관련 pytest 한두 개 (asyncio test 스타일 reference)
  </read_first>
  <behavior>
    - rpc_call('read_file', {'path': '/tmp/a.txt'}) 가 ws 로 tool_request 송신 후 tool_result 도착 시까지 block (sync 스레드에서 호출)
    - 동일 ws 내 동시 호출 다중 OK — call_id 가 unique 하면 각 Future 가 독립 (D-07)
    - tool_result 의 ok=true → rpc_call 이 {'ok': True, **result} 반환
    - tool_result 의 ok=false → rpc_call 이 {'ok': False, 'error': error.message} 반환
    - 30s timeout 도달 시 → {'ok': False, 'error': 'tool_call timeout (30s)'} 반환 (D-11)
    - ws disconnect 시 모든 pending Future 가 cancel → rpc_call 이 {'ok': False, 'error': 'tool_call aborted (disconnect)'} 반환 (D-12)
    - late tool_result (이미 done 또는 알 수 없는 call_id) 는 silent drop
    - confirm_write_response/confirm_bash_response 같은 기존 dispatch case 는 회귀 없음
  </behavior>
  <action>
    1. harness_server.py 의 import 영역 (line 5-15) 에 `import concurrent.futures` 추가 (rpc_call 의 CancelledError 분기용 — uuid 는 이미 line 10 에 있음).

    2. _run_session 의 ws 객체에 attribute 부여 — line 889-891 의 `room = _get_or_create_room(...)`/`room.subscribers.add(ws)`/`state = room.state` 직후에 ws._pending_calls 초기화 (D-05, D-06):

       ```python
       room = _get_or_create_room(room_name)
       room.subscribers.add(ws)
       state = room.state
       # RPC-01 (D-05, D-06): 1 ws ↔ pending tool_call 들. BB-2 폐기 후에도 ws scope 그대로 유지.
       ws._pending_calls: dict[str, asyncio.Future] = {}
       ```

       (Python 은 임의 attribute 할당 가능 — websockets.WebSocketServerProtocol 도 동일.)

    3. run_agent 안 — confirm_bash 클로저 정의 직후 (line 306 부근, 즉 confirm_bash 함수의 return state._confirm_bash_result 다음 빈 줄) 에 rpc_call 클로저 추가. confirm_write 패턴 그대로 응용하되 Future 사용:

       ```python
       def rpc_call(name: str, args: dict, timeout: float = 30.0) -> dict:
           '''agent (sync 스레드) 가 호출. ws 로 tool_request 송신 후 tool_result 까지 wait.

           confirm_write (line 274-290) 와 동일 패턴 — Event 대신 Future (결과값 운반).
           D-11 timeout 30s default. D-12 disconnect 시 _run_session finally 가 cancel.
           '''
           call_id = uuid.uuid4().hex
           future: asyncio.Future = loop.create_future()
           ws._pending_calls[call_id] = future
           try:
               asyncio.run_coroutine_threadsafe(
                   send(ws, type='tool_request', call_id=call_id, name=name, args=args), loop
               )
               # asyncio.shield 로 외부 cancel 이 우리 future 를 직접 cancel 못하게 보호.
               # disconnect cleanup 은 명시적으로 future.cancel() 호출 (이중 보호).
               wait_future = asyncio.run_coroutine_threadsafe(
                   asyncio.wait_for(asyncio.shield(future), timeout=timeout), loop
               )
               return wait_future.result(timeout=timeout + 1)
           except asyncio.TimeoutError:
               return {'ok': False, 'error': f'tool_call timeout ({timeout}s)'}
           except (asyncio.CancelledError, concurrent.futures.CancelledError):
               return {'ok': False, 'error': 'tool_call aborted (disconnect)'}
           except Exception as e:
               return {'ok': False, 'error': f'tool_call error: {e}'}
           finally:
               ws._pending_calls.pop(call_id, None)
       ```

    4. run_agent 의 agent.run(...) 호출부 (line 362-374, 그리고 ephemeral 분기 line 350-360) 양쪽에 rpc_call=rpc_call 인자 추가:

       ```python
       # 비-ephemeral (line 362-374)
       _, state.messages = agent.run(
           user_input,
           session_messages=state.messages,
           working_dir=state.working_dir,
           profile=state.profile,
           context_snippets=snippets if isinstance(snippets, str) else '',
           plan_mode=plan_mode,
           on_token=on_token,
           on_tool=on_tool,
           confirm_write=confirm_write if state.profile.get('confirm_writes', True) else None,
           confirm_bash=confirm_bash if state.profile.get('confirm_bash', True) else None,
           hooks=state.profile.get('hooks', {}),
           rpc_call=rpc_call,  # RPC-01: 클라 위임 도구 진입점
       )

       # ephemeral (line 350-360) — 같은 인자 추가
       agent.run(
           user_input,
           session_messages=ephemeral_session,
           working_dir=run_wd,
           profile=run_profile,
           on_token=on_token,
           on_tool=on_tool,
           confirm_write=confirm_write if run_profile.get('confirm_writes', True) else None,
           confirm_bash=confirm_bash if run_profile.get('confirm_bash', True) else None,
           hooks=run_profile.get('hooks', {}),
           rpc_call=rpc_call,  # RPC-01
       )
       ```

    5. _dispatch_loop (line 793-870) 에 case 'tool_result' 추가 — confirm_bash_response (line 839-844) 직후 권장 위치 (cancel 직전):

       ```python
       elif t == 'tool_result':
           # RPC-01: 클라가 보낸 도구 실행 결과를 pending Future 에 주입.
           # 1ws=1session 이라 active_input_from 가드 불필요 — call_id 는 ws scope 에서 unique.
           # late/duplicate (이미 done) 은 silent drop.
           call_id = msg.get('call_id')
           if not call_id:
               continue
           pending = getattr(ws, '_pending_calls', None)
           if not pending:
               continue
           future = pending.get(call_id)
           if future is None or future.done():
               continue
           if msg.get('ok'):
               result_payload = msg.get('result') or {}
               # envelope 의 ok 는 True 로 평탄화 + result dict 의 키 그대로 노출 (Python 도구 schema 일치)
               future.set_result({'ok': True, **result_payload})
           else:
               err = msg.get('error') or {}
               err_msg = err.get('message') if isinstance(err, dict) else str(err)
               future.set_result({'ok': False, 'error': err_msg or '도구 실행 오류 (메시지 없음)'})
       ```

    6. _run_session 의 finally 블록 (line 969-978) — `room.subscribers.discard(ws)` 직전에 disconnect cleanup 추가 (D-12):

       ```python
       finally:
           # RPC-01 (D-12): ws disconnect 시 pending tool_call 일괄 cancel.
           # rpc_call 의 wait_future 가 CancelledError → {'ok': False, 'error': 'tool_call aborted (disconnect)'}.
           for _cid, _fut in list(getattr(ws, '_pending_calls', {}).items()):
               if not _fut.done():
                   _fut.cancel()
           if hasattr(ws, '_pending_calls'):
               ws._pending_calls.clear()

           room.subscribers.discard(ws)
           if is_shared and room.subscribers:
               try:
                   await broadcast(room, type='room_member_left',
                                   subscribers=len(room.subscribers))
               except Exception:
                   pass
           _maybe_drop_room(room)
       ```

    7. tests/test_rpc_bridge.py 신규 작성 — pytest asyncio 회귀 3건 (분기 로직 격리 검증):

       ```python
       '''RPC bridge — pending_calls + tool_result dispatch + disconnect cleanup 회귀.

       rpc_call 클로저 자체는 run_agent 내부 — thread-safety/timeout 통합 검증은 외부 PC 수동 검증 + Plan 03 으로 위임. 본 파일은 _dispatch_loop 의 tool_result 분기 + finally 의 cleanup 분기만 격리 단위 검증.
       '''
       import asyncio
       import pytest
       from unittest.mock import MagicMock


       @pytest.mark.asyncio
       async def test_pending_calls_future_set_on_tool_result_ok():
           '''tool_result(ok=true) 가 ws._pending_calls[call_id].set_result 를 호출 + envelope 평탄화.'''
           loop = asyncio.get_event_loop()
           future = loop.create_future()
           pending = {'abc': future}
           ws = MagicMock()
           ws._pending_calls = pending

           # _dispatch_loop 의 tool_result 분기 시뮬레이션
           msg = {'type': 'tool_result', 'call_id': 'abc', 'ok': True,
                  'result': {'content': 'hello', 'total_lines': 1, 'start_line': 1, 'end_line': 1}}
           call_id = msg.get('call_id')
           f = pending.get(call_id)
           assert f is not None and not f.done()
           result_payload = msg.get('result') or {}
           f.set_result({'ok': True, **result_payload})

           result = await asyncio.wait_for(future, timeout=1)
           assert result['ok'] is True
           assert result['content'] == 'hello'
           assert result['total_lines'] == 1


       @pytest.mark.asyncio
       async def test_pending_calls_error_payload_flattened():
           '''tool_result(ok=false) 가 error.message 를 result.error 로 평탄화.'''
           loop = asyncio.get_event_loop()
           future = loop.create_future()
           msg = {'type': 'tool_result', 'call_id': 'x', 'ok': False,
                  'error': {'kind': 'tool_error', 'message': 'EISDIR'}}
           err = msg.get('error') or {}
           err_msg = err.get('message') if isinstance(err, dict) else str(err)
           future.set_result({'ok': False, 'error': err_msg})

           r = await asyncio.wait_for(future, timeout=1)
           assert r == {'ok': False, 'error': 'EISDIR'}


       @pytest.mark.asyncio
       async def test_pending_calls_cancel_on_disconnect():
           '''disconnect cleanup: ws._pending_calls 의 모든 Future 가 cancel 되어야 함.'''
           loop = asyncio.get_event_loop()
           f1 = loop.create_future()
           f2 = loop.create_future()
           ws = MagicMock()
           ws._pending_calls = {'a': f1, 'b': f2}

           # _run_session finally 분기 시뮬레이션
           for _cid, _fut in list(getattr(ws, '_pending_calls', {}).items()):
               if not _fut.done():
                   _fut.cancel()

           assert f1.cancelled()
           assert f2.cancelled()
       ```

       (rpc_call 클로저의 thread-safety/타임아웃 검증은 외부 PC 수동 검증 + Plan 03 의 통합으로 위임. 본 task 는 분기 로직만 격리 검증.)

    8. CLAUDE.md 규칙 — Python 4 spaces (PEP 8), 한국어 주석. single quote 가능한 곳은 single.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness &amp;&amp; .venv/bin/python -c "import harness_server" &amp;&amp; .venv/bin/python -m pytest tests/test_rpc_bridge.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "_pending_calls" harness_server.py` 매치
    - `grep -q "def rpc_call" harness_server.py` 매치
    - `grep -q "rpc_call=rpc_call" harness_server.py` 매치 (agent.run 호출부 — ephemeral + 비-ephemeral 양쪽)
    - `grep -c "rpc_call=rpc_call" harness_server.py` 결과 ≥ 2
    - `grep -q "elif t == 'tool_result'" harness_server.py` 매치
    - `grep -q "_fut.cancel()" harness_server.py` 매치 (disconnect cleanup)
    - `grep -q "asyncio.shield" harness_server.py` 매치 (이중 보호)
    - `grep -q "import concurrent.futures" harness_server.py` 매치
    - `.venv/bin/python -c "import harness_server"` exit 0 (syntax/import error 없음)
    - `.venv/bin/python -m pytest tests/test_rpc_bridge.py -x` exit 0 (3건 green)
    - `.venv/bin/python -m pytest tests/ -x --ignore=tests/test_rpc_bridge.py` 기존 199건 회귀 0
  </acceptance_criteria>
  <done>
    harness_server.py 에 ws._pending_calls + rpc_call 클로저 + _dispatch_loop case 'tool_result' + _run_session finally 의 disconnect cleanup 추가. agent.run 호출부 2곳에 rpc_call 인자 주입. tests/test_rpc_bridge.py 3건 green + 기존 pytest 회귀 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent.py 에 CLIENT_SIDE_TOOLS 분기 + rpc_call 인자 + alias 정규화</name>
  <files>agent.py, tests/test_agent_client_side_dispatch.py</files>
  <read_first>
    - agent.py 의 line 1-50 (import + 모듈 상수, MAX_TOOL_RESULT_CHARS 등의 위치 확인), line 416-431 (run signature), line 491-580 (tool dispatch loop, 특히 line 560-578 가 분기 삽입 위치)
    - tools/__init__.py (line 310-329 — TOOL_MAP. 'read_file' 항목은 Plan 03 에서 deletion 하지만 본 task 에서는 유지 — CLIENT_SIDE_TOOLS 분기가 먼저 잡혀서 dual-stack 안전)
    - .planning/phases/01-rpc-skeleton/01-CONTEXT.md (D-08, D-10, D-14, D-15, D-16)
    - .planning/phases/01-rpc-skeleton/01-PATTERNS.md (line 169-276 — agent.py 섹션, 분기 삽입 패턴 ground truth)
    - tools/fs.py:55-79 (alias 정규화 비교용 — Python 측 read_file 의 path|file_path alias 처리)
  </read_first>
  <behavior>
    - run() signature 에 rpc_call=None 인자 추가 — 기존 호출부 (test 코드, hooks 등) 가 키워드 인자 안 주면 None 으로 fallback (회귀 없음)
    - 모듈 상단에 CLIENT_SIDE_TOOLS = {'read_file'} (D-10)
    - fn_name='read_file' + rpc_call 주어진 경우: TOOL_MAP['read_file'] 호출 안 됨, rpc_call('read_file', args) 결과가 result 로 들어감
    - fn_name='read_file' + rpc_call=None 인 경우: {'ok': False, 'error': 'rpc_call 콜백 없음 — read_file 은 클라 측 도구입니다'} 반환 (CLI/단독 실행 호환성)
    - args 에 file_path 만 있고 path 없으면 path 로 정규화 (D-16) — 그 외 도구는 영향 없음
    - 기존 confirm/hooks/sticky_deny/run_command cwd 주입 등 모든 분기는 회귀 없음
  </behavior>
  <action>
    1. agent.py 모듈 상단 (line 1-50 의 적당한 위치 — MAX_TOOL_RESULT_CHARS 등 상수 정의 부근) 에 CLIENT_SIDE_TOOLS 추가:

       ```python
       # RPC-02 (D-10): 클라이언트 측에서 실행되는 도구.
       # Phase 1 = read_file 만. Phase 2 에서 fs 전체, Phase 3 에서 shell+git 추가.
       # rpc_call 콜백이 주입된 경우에만 우회 — 단독 CLI 실행 시 fallback 으로 ok=false 에러 반환.
       CLIENT_SIDE_TOOLS = {'read_file'}
       ```

    2. agent.py:run signature (line 416-431) 에 rpc_call=None 인자 추가 — hooks 다음 줄 권장:

       ```python
       def run(
           user_input: str,
           session_messages: list = None,
           working_dir: str = '.',
           profile: dict = None,
           context_snippets: str = '',
           plan_mode: bool = False,
           on_token=None,
           on_tool=None,
           confirm_write=None,
           confirm_bash=None,
           on_unknown_tool=None,
           on_thought=None,
           on_thought_end=None,
           hooks: dict = None,
           rpc_call=None,  # RPC-02 (D-08): callable[(name, args, timeout?), dict] | None
       ) -> tuple[str, list]:
       ```

    3. line 564 의 `fn = TOOL_MAP.get(fn_name)` 직전 (즉 line 562-563 의 cwd 주입 직후) 에 분기 추가. 기존 코드 (line 560-578):

       ```python
       # shell 툴은 working_dir를 cwd로 자동 주입
       if fn_name in ('run_command', 'run_python') and 'cwd' not in args:
           args = {**args, 'cwd': working_dir}

       fn = TOOL_MAP.get(fn_name)
       if fn:
           try:
               result = fn(**args)
           except TypeError as e:
               result = {'ok': False, 'error': f'인자 오류: {e}'}
       else:
           result = {
               'ok': False,
               'error': f'툴 "{fn_name}"은 존재하지 않습니다. 툴을 호출하지 말고 자연어로 직접 답변해 주세요.',
               '_unknown_tool': fn_name,
           }
           unknown_tool_count += 1
           if on_unknown_tool:
               on_unknown_tool(fn_name, args)
       ```

       → 적용 (CLIENT_SIDE_TOOLS 분기 삽입):

       ```python
       # shell 툴은 working_dir를 cwd로 자동 주입
       if fn_name in ('run_command', 'run_python') and 'cwd' not in args:
           args = {**args, 'cwd': working_dir}

       # RPC-02 (D-08, D-10, D-16): 클라 위임 도구 — rpc_call 로 ws→client→ws 라운드트립.
       # rpc_call 미주입 시 (단독 CLI / pytest) fallback 으로 ok=false 에러 반환 — 호환성 유지.
       if fn_name in CLIENT_SIDE_TOOLS:
           if rpc_call is None:
               result = {
                   'ok': False,
                   'error': f'rpc_call 콜백 없음 — {fn_name} 은 클라 측 도구입니다',
               }
           else:
               # D-16: read_file 의 file_path → path alias 정규화 (TS 측은 path 만 받음)
               if fn_name == 'read_file' and 'path' not in args and 'file_path' in args:
                   args = {**args, 'path': args.pop('file_path')}
               result = rpc_call(fn_name, args)
       else:
           fn = TOOL_MAP.get(fn_name)
           if fn:
               try:
                   result = fn(**args)
               except TypeError as e:
                   result = {'ok': False, 'error': f'인자 오류: {e}'}
           else:
               result = {
                   'ok': False,
                   'error': f'툴 "{fn_name}"은 존재하지 않습니다. 툴을 호출하지 말고 자연어로 직접 답변해 주세요.',
                   '_unknown_tool': fn_name,
               }
               unknown_tool_count += 1
               if on_unknown_tool:
                   on_unknown_tool(fn_name, args)
       ```

       **Important**: 기존 fn/result/unknown_tool_count 로직은 그대로 else 안으로 들여쓰기. 기존 회귀 0 보장. line 580-... 의 on_tool/_tool_call_history/post_tool_use/MAX_TOOL_RESULT_CHARS truncation 로직은 분기 밖이라 result 변수가 정의되어 있어 회귀 없음.

    4. tests/test_agent_client_side_dispatch.py 신규 — pytest 4건 (CLIENT_SIDE_TOOLS 분기 회귀):

       ```python
       '''agent.py 의 CLIENT_SIDE_TOOLS 분기 + rpc_call 위임 + alias 정규화 회귀.'''
       import json
       from unittest.mock import MagicMock, patch

       import agent


       def test_client_side_tools_constant():
           '''Phase 1 = read_file 만 클라 위임.'''
           assert agent.CLIENT_SIDE_TOOLS == {'read_file'}


       def test_read_file_uses_rpc_call_when_provided():
           '''rpc_call 주입 시 read_file 이 위임됨 — TOOL_MAP 의 fs.read_file 호출 안 됨.'''
           rpc_call = MagicMock(return_value={'ok': True, 'content': '   1\thi\n',
                                              'total_lines': 1, 'start_line': 1, 'end_line': 1})
           # 한 turn = tool_call → 다음 응답에서 final answer
           tool_call_msg = {
               'role': 'assistant',
               'content': '',
               'tool_calls': [{'function': {'name': 'read_file',
                                             'arguments': json.dumps({'path': '/tmp/x.txt'})}}],
           }
           final_msg = {'role': 'assistant', 'content': '읽었습니다'}
           with patch.object(agent, '_stream_response', side_effect=[tool_call_msg, final_msg]):
               _, msgs = agent.run('읽어줘', session_messages=[], rpc_call=rpc_call)
           rpc_call.assert_called_once_with('read_file', {'path': '/tmp/x.txt'})
           tool_msgs = [m for m in msgs if m.get('role') == 'tool']
           assert len(tool_msgs) == 1
           parsed = json.loads(tool_msgs[0]['content'])
           assert parsed['ok'] is True
           assert parsed['total_lines'] == 1


       def test_read_file_alias_file_path_normalized():
           '''D-16: file_path 만 있고 path 없으면 path 로 alias 정규화.'''
           rpc_call = MagicMock(return_value={'ok': True})
           tool_call_msg = {
               'role': 'assistant',
               'content': '',
               'tool_calls': [{'function': {'name': 'read_file',
                                             'arguments': json.dumps({'file_path': '/tmp/y.txt'})}}],
           }
           final_msg = {'role': 'assistant', 'content': 'done'}
           with patch.object(agent, '_stream_response', side_effect=[tool_call_msg, final_msg]):
               agent.run('x', session_messages=[], rpc_call=rpc_call)
           name, args = rpc_call.call_args[0]
           assert name == 'read_file'
           assert args.get('path') == '/tmp/y.txt'
           assert 'file_path' not in args


       def test_read_file_fallback_when_rpc_call_missing():
           '''rpc_call=None 일 때 ok=false + 명시 에러 메시지 (단독 CLI 호환성).'''
           tool_call_msg = {
               'role': 'assistant',
               'content': '',
               'tool_calls': [{'function': {'name': 'read_file',
                                             'arguments': json.dumps({'path': '/tmp/z.txt'})}}],
           }
           final_msg = {'role': 'assistant', 'content': 'done'}
           with patch.object(agent, '_stream_response', side_effect=[tool_call_msg, final_msg]):
               _, msgs = agent.run('x', session_messages=[], rpc_call=None)
           tool_msgs = [m for m in msgs if m.get('role') == 'tool']
           parsed = json.loads(tool_msgs[0]['content'])
           assert parsed['ok'] is False
           assert 'rpc_call' in parsed['error']
       ```

       (test_read_file_uses_rpc_call_when_provided 는 agent.run 의 main loop 통과 — confirm/cwd 주입 등 분기와의 회귀 검증 겸용. patch.object 로 _stream_response 만 주입.)

    5. CLAUDE.md 규칙 — Python 4 spaces, 한국어 주석.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness &amp;&amp; .venv/bin/python -m pytest tests/test_agent_client_side_dispatch.py -x -v &amp;&amp; .venv/bin/python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CLIENT_SIDE_TOOLS = {'read_file'}" agent.py` 매치
    - `grep -q "rpc_call=None" agent.py` 매치 (run signature)
    - `grep -q "fn_name in CLIENT_SIDE_TOOLS" agent.py` 매치
    - `grep -q "rpc_call(fn_name, args)" agent.py` 매치
    - `grep -q "args.pop('file_path')" agent.py` 매치 (alias 정규화)
    - `.venv/bin/python -c "import agent; assert agent.CLIENT_SIDE_TOOLS == {'read_file'}"` exit 0
    - `.venv/bin/python -m pytest tests/test_agent_client_side_dispatch.py -x` exit 0 (4건 green)
    - `.venv/bin/python -m pytest tests/ -x` 전체 199+ 회귀 0 (test_rpc_bridge + test_agent_client_side_dispatch 포함, fs.read_file 관련 케이스는 본 phase 끝 Plan 03 에서 deletion 예정 — 본 task 에서는 dual-stack 으로 통과)
  </acceptance_criteria>
  <done>
    agent.py 에 CLIENT_SIDE_TOOLS 상수 + run() 의 rpc_call 인자 + line 564 직전 분기 + file_path alias 정규화. tests/test_agent_client_side_dispatch.py 4건 green + 기존 pytest 199건 회귀 0.
  </done>
</task>

</tasks>

<verification>
- Python 빌드 (import): `cd /Users/johyeonchang/harness && .venv/bin/python -c "import harness_server, agent"` exit 0
- pytest 전체: `.venv/bin/python -m pytest tests/ -x` exit 0 (기존 199건 + 신규 7건 = 206+ green. fs.read_file deletion 은 Plan 03 에서.)
- 정적 grep:
  - `grep -q "_pending_calls" harness_server.py`
  - `grep -q "def rpc_call" harness_server.py`
  - `grep -q "elif t == 'tool_result'" harness_server.py`
  - `grep -q "CLIENT_SIDE_TOOLS = {'read_file'}" agent.py`
  - `grep -q "fn_name in CLIENT_SIDE_TOOLS" agent.py`
- 통합 sanity (수동): harness_server 를 띄우고 ui-ink 클라가 붙어 LLM 이 read_file 호출 → tool_request 가 클라로 가서 tool_result 가 회신되는지. 자동화는 Plan 03 의 통합/수동 검증으로 위임.
</verification>

<success_criteria>
- harness_server.py 와 agent.py 가 RPC 위임 골격 완성 — Plan 01 의 클라 측 핸들러와 짝을 이루어 read_file 호출이 ws→클라→ws 로 흐를 수 있는 상태
- pytest 회귀 0 — 기존 199건 + 신규 7건 (test_rpc_bridge.py 3건 + test_agent_client_side_dispatch.py 4건) green
- Plan 03 (vitest + Python deletion + 수동 검증) 진입 가능 — 백엔드 schema 와 dispatch 가 확정됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-rpc-skeleton/01-02-SUMMARY.md` covering:
- harness_server.py 의 변경 부위 (ws._pending_calls 초기화 위치, rpc_call 클로저 line 범위, _dispatch_loop 신규 case 위치, _run_session finally cleanup 위치)
- D-09/D-11/D-12 에 매핑되는 코드 인용
- agent.py 의 CLIENT_SIDE_TOOLS 정의 위치 + run signature 변경 + 분기 line
- 통과한 pytest 케이스 목록 (test_rpc_bridge 3건 + test_agent_client_side_dispatch 4건 + 기존 199건 = 206+)
- 회귀 0 의 근거 (전체 pytest 결과)
- Plan 03 가 deletion 할 잔존 항목 — tools/fs.py:read_file, tools/__init__.py 의 TOOL_MAP['read_file'], tests/test_fs.py 의 read_file 케이스 5건
</output>
