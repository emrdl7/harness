---
phase: 03-remote-room-session-control
plan: 05
type: execute
wave: 3
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - ui-ink/src/ws/client.ts
  - ui-ink/src/index.tsx
  - ui-ink/src/one-shot.ts
autonomous: true
requirements:
  - WSR-01
  - WSR-02
  - WSR-03
  - WSR-04
  - SES-01
  - SES-02
  - SES-03
  - DIFF-05

must_haves:
  truths:
    - "WS close 이벤트 시 jitter exponential backoff 타이머가 시작되고 wsState='reconnecting'이 된다"
    - "재연결 성공 후 30초간 안정 상태가 유지되면 attempts가 0으로 리셋된다"
    - "10회 실패 시 wsState='failed'가 된다"
    - "재연결 시 x-resume-from: lastEventId 헤더가 WS 연결에 포함된다"
    - "harness 'query' 실행 시 WS 연결 → ready → input 전송 → token 출력 → agent_end → exit 흐름이 완성된다"
    - "--resume <id> 플래그로 세션 로드 후 REPL 모드 진입이 가능하다"
  artifacts:
    - path: "ui-ink/src/ws/client.ts"
      provides: "jitter backoff + resume_from 헤더 + wsState 연동"
      contains: "_scheduleReconnect"
    - path: "ui-ink/src/one-shot.ts"
      provides: "경량 WS 클라이언트 — one-shot 시나리오"
      contains: "runOneShot"
    - path: "ui-ink/src/index.tsx"
      provides: "argv 파싱 확장 — one-shot + --resume + --room"
      contains: "runOneShot"
  key_links:
    - from: "client.ts:ws.on('close')"
      to: "_scheduleReconnect()"
      via: "jitter backoff"
      pattern: "_scheduleReconnect"
    - from: "client.ts:connect()"
      to: "store/room.ts:lastEventId"
      via: "x-resume-from 헤더"
      pattern: "lastEventId"
    - from: "index.tsx"
      to: "one-shot.ts:runOneShot()"
      via: "dynamic import + query 파싱"
      pattern: "runOneShot"
---

<objective>
Wave 3 — HarnessClient.ts에 WSR-01~03 jitter exponential backoff + resume_from 헤더 구현. one-shot.ts 신규 경량 클라이언트 생성 (SES-01). index.tsx argv 파싱 확장 (SES-01/02/03 = --resume + --room + one-shot).

Purpose: 재연결 경로 완성 + CLI one-shot/resume 시나리오 완성. Wave 4의 App.tsx는 store/room.ts의 wsState를 이미 읽을 수 있으므로 ReconnectOverlay 자동 표시.
Output: 재연결 backoff + one-shot + resume CLI 동작.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md

<interfaces>
<!-- client.ts 현재 구현 (전체 파일): -->
```typescript
export class HarnessClient {
  private ws: WebSocket | null = null
  private opts: ConnectOptions
  private pingInterval: ReturnType<typeof setInterval> | null = null

  constructor(opts: ConnectOptions) { this.opts = opts }

  connect(): void {
    const headers: Record<string, string> = {
      'x-harness-token': this.opts.token,
    }
    if (this.opts.room) headers['x-harness-room'] = this.opts.room
    this.ws = new WebSocket(this.opts.url, {headers})
    // ...
    this.ws.on('close', () => {
      useStatusStore.getState().setConnected(false)
      this._clearPing()
      // Phase 3 에서 jitter backoff reconnect 추가  ← 여기 구현
    })
  }

  send(msg: ClientMsg): void { ... }
  close(): void { ... }
  private _clearPing(): void { ... }
}
```

<!-- index.tsx 현재 one-shot stub: -->
```typescript
if (!isInteractive) {
  const query = process.argv[2]
  if (query) {
    // eslint-disable-next-line no-restricted-syntax
    process.stdout.write(`[one-shot] ${query}\n`)  // ← stub 교체 필요
  } else {
    process.stderr.write('[harness] non-TTY 환경...\n')
  }
  process.exit(0)
}
```

<!-- store/room.ts Phase 3 확장 후 (Plan 03-03에서 추가된 필드): -->
```typescript
// wsState: 'connected' | 'reconnecting' | 'failed'
// reconnectAttempt: number
// lastEventId: number | null
// setWsState(s), setReconnectAttempt(n), setLastEventId(id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HarnessClient.ts — jitter backoff + resume_from 헤더 (WSR-01~03)</name>
  <files>
    ui-ink/src/ws/client.ts,
    ui-ink/src/__tests__/ws-backoff.test.ts
  </files>
  <read_first>
    - ui-ink/src/ws/client.ts (전체 — 현재 close 핸들러, connect() 헤더 구조, _clearPing 패턴 확인)
    - ui-ink/src/store/room.ts (Plan 03-03 이후 — setWsState/setReconnectAttempt/setLastEventId 시그니처 확인)
    - ui-ink/src/__tests__/ws-backoff.test.ts (미존재 확인)
  </read_first>
  <behavior>
    - Test 1: _scheduleReconnect() — attempts=0일 때 delay가 500ms~1000ms 범위 (base=1000, jitter 0.5~1.0)
    - Test 2: _scheduleReconnect() — attempts=1일 때 delay가 1000ms~2000ms 범위
    - Test 3: _scheduleReconnect() — attempts=9일 때 delay가 cap 30000ms 이하
    - Test 4: attempts >= 10 시 wsState='failed' 설정 + setTimeout 없음
    - Test 5: _onConnectedStable() 호출 30초 후 attempts가 0이 된다 (fake timer로 검증)
    - Test 6: connect() 시 lastEventId != null이면 x-resume-from 헤더에 포함된다
  </behavior>
  <action>
**ui-ink/src/ws/client.ts 수정:**

1. ConnectOptions에 resumeSession 필드 추가 (SES-02용):
```typescript
export interface ConnectOptions {
  url: string
  token: string
  room?: string
  resumeSession?: string  // --resume <id> 세션 ID (SES-02)
}
```

2. HarnessClient 클래스에 backoff private 필드 추가:
```typescript
export class HarnessClient {
  private ws: WebSocket | null = null
  private opts: ConnectOptions
  private pingInterval: ReturnType<typeof setInterval> | null = null
  // WSR-01: jitter exponential backoff 상태
  private backoff = {
    attempts: 0,
    stableTimer: null as ReturnType<typeof setTimeout> | null,
  }
  private _closed = false  // close() 명시적 호출 시 재연결 방지
```

3. connect() 메서드에 x-resume-from + x-resume-session 헤더 추가:
```typescript
connect(): void {
  const headers: Record<string, string> = {
    'x-harness-token': this.opts.token,
  }
  if (this.opts.room) headers['x-harness-room'] = this.opts.room
  // WSR-03: delta replay 요청 — lastEventId가 있으면 resume_from 헤더 추가
  const lastEventId = useRoomStore.getState().lastEventId
  if (lastEventId != null) headers['x-resume-from'] = String(lastEventId)
  // SES-02: 세션 resume 헤더
  if (this.opts.resumeSession) headers['x-resume-session'] = this.opts.resumeSession

  this.ws = new WebSocket(this.opts.url, {headers})
  // ...
```

4. ws.on('open') 핸들러에 안정 타이머 시작:
```typescript
this.ws.on('open', () => {
  useStatusStore.getState().setConnected(true)
  useRoomStore.getState().setWsState('connected')
  bindConfirmClient(this)
  this._onConnectedStable()  // WSR-01: 30초 안정 후 attempts 리셋
  // heartbeat
  this.pingInterval = setInterval(() => {
    this.send({type: 'ping'})
  }, 30_000)
})
```

5. ws.on('close') 핸들러 교체 (jitter backoff 추가):
```typescript
this.ws.on('close', () => {
  useStatusStore.getState().setConnected(false)
  this._clearPing()
  if (this.backoff.stableTimer) {
    clearTimeout(this.backoff.stableTimer)
    this.backoff.stableTimer = null
  }
  // WSR-01: 명시적 close()가 아닌 경우에만 재연결
  if (!this._closed) {
    this._scheduleReconnect()
  }
})
```

6. _scheduleReconnect() 메서드 추가:
```typescript
private _scheduleReconnect(): void {
  const {attempts} = this.backoff
  if (attempts >= 10) {
    useRoomStore.getState().setWsState('failed')
    return
  }
  // WSR-01 공식: delay = base * 2^n * (0.5 + Math.random() * 0.5), cap 30초
  const base = 1000
  const cap = 30_000
  const delay = Math.min(
    base * Math.pow(2, attempts) * (0.5 + Math.random() * 0.5),
    cap,
  )
  this.backoff.attempts++
  useRoomStore.getState().setReconnectAttempt(this.backoff.attempts)
  useRoomStore.getState().setWsState('reconnecting')
  setTimeout(() => {
    if (!this._closed) this.connect()
  }, delay)
}
```

7. _onConnectedStable() 메서드 추가:
```typescript
private _onConnectedStable(): void {
  if (this.backoff.stableTimer) clearTimeout(this.backoff.stableTimer)
  // 30초 안정 후 attempts 리셋 (thundering herd 방지)
  this.backoff.stableTimer = setTimeout(() => {
    this.backoff.attempts = 0
  }, 30_000)
}
```

8. close() 메서드에 _closed 플래그 설정:
```typescript
close(): void {
  this._closed = true  // 재연결 방지
  this._clearPing()
  if (this.backoff.stableTimer) clearTimeout(this.backoff.stableTimer)
  this.ws?.close()
  this.ws = null
  bindConfirmClient(null)
}
```

9. import에 useRoomStore 추가:
```typescript
import {useRoomStore} from '../store/room.js'
```
  </action>
  <acceptance_criteria>
    - `grep '_scheduleReconnect' ui-ink/src/ws/client.ts` → 메서드 정의 + close 핸들러 내 호출 2건
    - `grep 'x-resume-from' ui-ink/src/ws/client.ts` → connect() 내 1건
    - `grep '_closed' ui-ink/src/ws/client.ts` → 3건 이상 (close 플래그, 재연결 체크)
    - `grep 'setWsState' ui-ink/src/ws/client.ts` → 3건 (connected, reconnecting, failed)
    - `cd ui-ink && bun run tsc --noEmit` → 에러 없음
    - `cd ui-ink && bun run test -- --reporter=verbose 2>&1 | grep backoff` → 테스트 green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit && bun run test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - _scheduleReconnect()가 WSR-01 공식(`base * 2^n * (0.5 + rand*0.5)`, cap 30s)을 사용한다
    - 10회 실패 시 setWsState('failed')
    - 30초 안정 후 attempts=0 리셋
    - connect()에 x-resume-from 헤더 포함
    - ws-backoff 테스트 6건 green
    - tsc + vitest green
  </done>
</task>

<task type="auto">
  <name>Task 2: one-shot.ts 신규 + index.tsx argv 파싱 확장 (SES-01/02/03)</name>
  <files>
    ui-ink/src/one-shot.ts,
    ui-ink/src/index.tsx
  </files>
  <read_first>
    - ui-ink/src/index.tsx (현재 전체 — one-shot stub 위치 lines 44-54, process.argv 패턴 확인)
    - ui-ink/src/ws/client.ts (방금 수정된 파일 — HarnessClient ConnectOptions 시그니처 확인)
    - ui-ink/src/protocol.ts (ServerMsg.token, agent_end 타입 확인)
  </read_first>
  <action>
**ui-ink/src/one-shot.ts 신규 생성:**

```typescript
// one-shot 경량 WS 클라이언트 (SES-01, SES-03)
// REPL 없이 단일 질문 → answer stdout → exit
// non-TTY 시 ANSI off (Pitfall G 방지)
import WebSocket from 'ws'
import type {ServerMsg} from './protocol.js'
import {parseServerMsg} from './ws/parse.js'

export interface OneShotOptions {
  url: string
  token: string
  query: string
  room?: string
  ansi: boolean   // true = TTY 환경 (ANSI 출력 허용)
}

export async function runOneShot(opts: OneShotOptions): Promise<void> {
  return new Promise<void>((resolve, reject) => {
    const headers: Record<string, string> = {
      'x-harness-token': opts.token,
    }
    if (opts.room) headers['x-harness-room'] = opts.room

    const ws = new WebSocket(opts.url, {headers})
    let resolved = false

    // 30초 타임아웃 (서버 무응답 방지)
    const timeout = setTimeout(() => {
      if (!resolved) {
        ws.close()
        reject(new Error('[harness] one-shot 타임아웃 (30s)'))
      }
    }, 30_000)

    ws.on('open', () => {
      // ready 이벤트를 기다리지 않고 즉시 전송해도 서버가 큐에 넣음
      // (단, ready 이후 전송이 안전 — message handler에서 처리)
    })

    ws.on('message', (raw) => {
      const msg = parseServerMsg(raw.toString())
      if (!msg) return

      switch (msg.type) {
        case 'ready':
          // 서버 준비 완료 → 질문 전송
          ws.send(JSON.stringify({type: 'input', text: opts.query}))
          break

        case 'token':
          // 토큰 누적 출력 (ANSI 없이 — piping 안전)
          // eslint-disable-next-line no-restricted-syntax
          process.stdout.write(msg.text)
          break

        case 'agent_end':
          // 응답 완료 → 개행 후 종료
          if (!resolved) {
            resolved = true
            clearTimeout(timeout)
            // eslint-disable-next-line no-restricted-syntax
            process.stdout.write('\n')
            ws.close()
            resolve()
          }
          break

        case 'error':
          if (!resolved) {
            resolved = true
            clearTimeout(timeout)
            ws.close()
            reject(new Error(`[harness] 서버 오류: ${msg.text}`))
          }
          break

        default:
          // 나머지 이벤트 (state_snapshot, room_joined 등) — one-shot에서 무시
          break
      }
    })

    ws.on('error', (err) => {
      if (!resolved) {
        resolved = true
        clearTimeout(timeout)
        reject(err)
      }
    })

    ws.on('close', () => {
      if (!resolved) {
        resolved = true
        clearTimeout(timeout)
        // 서버 종료로 인한 close — 에러 처리
        reject(new Error('[harness] WS 연결 종료됨'))
      }
    })
  })
}
```

**ui-ink/src/index.tsx one-shot stub 교체:**

현재 lines 44-54 블록을 아래로 교체:
```typescript
if (!isInteractive) {
  // SES-01/02/03: argv 파싱
  const resumeIdx = process.argv.indexOf('--resume')
  const resumeId = resumeIdx > -1 ? process.argv[resumeIdx + 1] : undefined
  const roomIdx = process.argv.indexOf('--room')
  const roomName = roomIdx > -1 ? process.argv[roomIdx + 1] : process.env['HARNESS_ROOM']

  // query: '--' 접두사가 아니고 --room/--resume의 값이 아닌 첫 번째 인자
  const skipIndices = new Set<number>()
  if (roomIdx > -1) { skipIndices.add(roomIdx); skipIndices.add(roomIdx + 1) }
  if (resumeIdx > -1) { skipIndices.add(resumeIdx); skipIndices.add(resumeIdx + 1) }
  const query = process.argv.slice(2).find(
    (a, i) => !a.startsWith('--') && !skipIndices.has(i + 2),
  )

  const url = process.env['HARNESS_URL']
  const token = process.env['HARNESS_TOKEN']
  if (!url || !token) {
    process.stderr.write('[harness] HARNESS_URL / HARNESS_TOKEN 필요\n')
    process.exit(1)
  }

  if (query && !resumeId) {
    // SES-01/SES-03: one-shot (--room 조합 가능)
    const {runOneShot} = await import('./one-shot.js')
    await runOneShot({
      url,
      token,
      room: roomName,
      query,
      ansi: process.stdout.isTTY === true,
    })
    process.exit(0)
  } else if (resumeId) {
    // SES-02: --resume <id> — 세션 로드 후 REPL (아래 render로 진행, ConnectOptions에 resumeSession 전달)
    // App.tsx에서 ConnectOptions 확장 필요. 일단 env var를 통해 전달.
    process.env['HARNESS_RESUME_SESSION'] = resumeId
    // isInteractive를 true로 재정의하여 아래 Ink render 블록으로 진행
    // (REPL 모드로 진입)
  } else {
    process.stderr.write('[harness] non-TTY 환경. HARNESS_URL / HARNESS_TOKEN 으로 연결하세요.\n')
    process.exit(0)
  }
}
```

주의:
- `import()` 동적 import가 top-level에서 동작하려면 index.tsx가 async context여야 함. 현재 파일이 top-level ESM이면 `await import()` 사용 가능. 그렇지 않으면 즉시 실행 async 함수로 래핑:
```typescript
// 파일 최상위:
;(async () => {
  if (!isInteractive) {
    // ... 위 블록 ...
  }
  render(<App />, {patchConsole: false})
})()
```
  </action>
  <acceptance_criteria>
    - `test -f ui-ink/src/one-shot.ts` → 파일 존재
    - `grep 'runOneShot' ui-ink/src/one-shot.ts` → 함수 export 있음
    - `grep 'runOneShot' ui-ink/src/index.tsx` → dynamic import 있음
    - `grep 'resume' ui-ink/src/index.tsx` → --resume 파싱 있음
    - `grep '\[one-shot\]' ui-ink/src/index.tsx` → stub 제거됨 (없어야 함)
    - `cd ui-ink && bun run tsc --noEmit` → 에러 없음
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit && bun run test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - one-shot.ts: WS 연결 → ready → input 전송 → token stdout → agent_end → resolve 흐름
    - one-shot.ts: 30초 타임아웃 + error 케이스 reject
    - index.tsx: one-shot stub 제거, --resume/--room/query 파싱 완료
    - tsc --noEmit green
    - vitest green (기존 + ws-backoff 6건)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS close 이벤트 → _scheduleReconnect() | 외부 서버 불안정으로 인한 close. 재연결 횟수 제한(10회) |
| process.argv → query/resumeId | 로컬 CLI 입력. 신뢰 경계 아님 (로컬 프로세스) |
| one-shot WS → stdout | 서버 토큰을 stdout에 그대로 출력. ANSI injection 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Denial of Service | jitter backoff — 10회 실패 후 failed 상태 | mitigate | attempts >= 10 시 더 이상 재연결 시도 없음. wsState='failed'로 고정. 사용자가 Ctrl+C로 종료 |
| T-03-05-02 | Denial of Service | thundering herd — 3클라 동시 재연결 | mitigate | WSR-01 jitter (0.5 + rand*0.5) = 50% 범위 spread. base=1초, 3클라 분산 효과 |
| T-03-05-03 | Tampering | one-shot에서 ANSI escape를 pipe에 출력 | mitigate | ansi: process.stdout.isTTY 조건부 (non-TTY 시 ansi=false). 단, token은 서버가 이미 plain text로 전송 |
| T-03-05-04 | Denial of Service | one-shot 30초 타임아웃 미처리 | mitigate | setTimeout 30_000으로 강제 reject. ws.close() 호출로 연결 정리 |
| T-03-05-05 | Tampering | x-resume-session 헤더로 타인 세션 접근 | accept | resumeSession은 로컬 사용자가 직접 지정하는 값. 서버는 세션 파일이 존재하는 경우만 로드. 세션 파일 권한 0o600 (기존 구현) |
</threat_model>

<verification>
1. `cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit` → 에러 0
2. `cd /Users/johyeonchang/harness/ui-ink && bun run test` → vitest green
3. `grep '_scheduleReconnect\|_onConnectedStable\|_closed' ui-ink/src/ws/client.ts | wc -l` → 5건 이상
4. `grep 'x-resume-from' ui-ink/src/ws/client.ts` → connect() 내 1건
5. `grep '\[one-shot\]' ui-ink/src/index.tsx` → 결과 없음 (stub 제거 확인)
</verification>

<success_criteria>
- HarnessClient: jitter backoff (WSR-01 공식 준수), 10회 실패→failed, 30초 안정→리셋, x-resume-from 헤더
- one-shot.ts: runOneShot() 함수 — WS 연결 → ready → input → token stdout → agent_end → resolve
- index.tsx: one-shot stub 제거, --resume/--room/query argv 파싱 완료
- tsc + vitest green
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-05-SUMMARY.md` 작성.
</output>
