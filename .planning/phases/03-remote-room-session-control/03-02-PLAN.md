---
phase: 03-remote-room-session-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - harness_server.py
  - tests/test_harness_server.py
autonomous: true
requirements:
  - PEXT-04
  - PEXT-05

must_haves:
  truths:
    - "WS 연결 시 x-resume-from 헤더가 있으면 ring buffer에서 해당 event_id 이후 이벤트를 순서대로 재송신한다"
    - "클라이언트가 cancel 메시지를 보내면 active_input_from인 경우에만 현재 input task가 취소된다"
    - "CancelledError는 _handle_input() finally에서 busy=False + active_input_from=None으로 정리된다"
    - "agent_cancelled 이벤트가 broadcast된다"
  artifacts:
    - path: "harness_server.py"
      provides: "PEXT-04 resume_from 파싱 + PEXT-05 cancel 처리"
      contains: "resume_from"
    - path: "tests/test_harness_server.py"
      provides: "PEXT-04/05 테스트"
      contains: "TestDeltaReplay"
  key_links:
    - from: "harness_server.py:_run_session()"
      to: "Room.event_buffer"
      via: "x-resume-from 헤더 → ring buffer delta 재송신"
      pattern: "resume_from"
    - from: "harness_server.py:_dispatch_loop()"
      to: "room.input_tasks"
      via: "cancel 케이스 → task.cancel()"
      pattern: "cancel"
---

<objective>
Phase 3 서버 WS 프로토콜 확장 2차 — _run_session()에 x-resume-from 헤더 파싱과 ring buffer delta 재송신을 구현하고 (PEXT-04), _dispatch_loop()에 cancel 메시지 처리와 asyncio task 안전 중단 경로를 추가한다 (PEXT-05).

Purpose: WSR-03 delta replay (Plan 03-05에서 클라이언트 측 구현)와 WSR-04 Ctrl+C 취소 경로(Plan 03-06에서 App.tsx 연결)의 서버 기반을 완성한다.
Output: harness_server.py에 PEXT-04~05 구현 완료 + pytest 테스트 추가 (기존 green 유지)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md
@.planning/BB-2-DESIGN.md

<interfaces>
<!-- _run_session() 현재 진입부 (lines 711-731 근처): -->
```python
async def _run_session(ws):
    room_header = (ws.request.headers.get('x-harness-room', '') or '').strip()
    room_name = room_header if room_header else f'_solo_{uuid.uuid4().hex}'
    is_shared = bool(room_header)
    room = _get_or_create_room(room_name)
    room.subscribers.add(ws)
    state = room.state
    try:
        await send_state(ws, state)
        await send(ws, type='ready', room=room_name)
        await send(ws, type='room_joined',
                   room=room_name,
                   shared=is_shared,
                   subscribers=len(room.subscribers),
                   busy=room.busy)
        # state_snapshot 전송 ...
```

<!-- _dispatch_loop() 현재 구조 (lines 654-709 근처): -->
```python
async def _dispatch_loop(ws, room: 'Room', queue: asyncio.Queue):
    state = room.state
    while True:
        msg = await queue.get()
        if msg is None:
            return
        t = msg.get('type')

        if t == 'input':
            # ... busy 체크 + _spawn_input_task()
        elif t == 'confirm_write_response':
            if ws is not room.active_input_from:  # DQ2 가드
                continue
            # ...
        elif t == 'ping':
            await send(ws, type='pong')
```

<!-- _handle_input() 현재 구조 (lines 617-635 근처): -->
```python
async def _handle_input(ws, room: 'Room', text: str):
    try:
        if text.startswith('/'):
            await handle_slash(ws, room, text)
        ...
    except Exception as e:
        await broadcast(room, type='error', text=f'입력 처리 오류: {e}')
    finally:
        room.busy = False
        room.active_input_from = None
```

<!-- room_joined 현재 서버 전송 (lines 727-731): -->
```python
await send(ws, type='room_joined',
           room=room_name,
           shared=is_shared,
           subscribers=len(room.subscribers),
           busy=room.busy)
# ← members 필드 없음 — protocol.ts 불일치 (RESEARCH.md Pitfall H)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PEXT-04 — _run_session() x-resume-from 헤더 파싱 + delta 재송신</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (_run_session 함수 전체 — room_header 파싱 위치, room_joined send() 호출 위치, state_snapshot 전송 위치 확인)
    - tests/test_harness_server.py (TestDeltaReplay 클래스 미존재 확인, 기존 _run_session 관련 테스트 확인)
  </read_first>
  <behavior>
    - Test 1: x-resume-from 헤더가 없으면 resume_from=None으로 처리된다 (delta 재송신 없음)
    - Test 2: x-resume-from: "5" 헤더 → event_id > 5인 이벤트만 재송신된다
    - Test 3: x-resume-from: "0" 헤더 → ring buffer의 모든 이벤트가 재송신된다
    - Test 4: x-resume-from: "abc" 같은 비정수값 → resume_from=None (무시, 재송신 없음)
    - Test 5: x-resume-from: "999999999999" (2^31 초과) → resume_from=None (방어 처리)
    - Test 6: room_joined 이벤트에 members 필드가 추가된다 (Pitfall H 수정)
  </behavior>
  <action>
1. harness_server.py의 _run_session() 함수에서 room_header 파싱 블록 직후에 추가:
```python
# PEXT-04: resume_from 헤더 파싱 (재연결 시 delta replay 요청)
resume_from_raw = (ws.request.headers.get('x-resume-from', '') or '').strip()
if resume_from_raw.isdigit() and int(resume_from_raw) < 2**31:
    resume_from: int | None = int(resume_from_raw)
else:
    resume_from = None
```

2. room_joined 전송 직후, state_snapshot 전송 이전에 delta 재송신 블록 추가:
```python
# PEXT-04: delta 재송신 — ring buffer에서 resume_from 이후 이벤트 순서대로 전송
if resume_from is not None:
    for (eid, _ts, payload_dict) in list(room.event_buffer):
        if eid > resume_from:
            await send(ws, **payload_dict)
```

3. room_joined send() 호출에 members 필드 추가 (Pitfall H 수정):
```python
await send(ws, type='room_joined',
           room=room_name,
           shared=is_shared,
           subscribers=len(room.subscribers),
           busy=room.busy,
           members=[])  # 현재는 빈 리스트 — room_member_joined 이벤트로 점진 추가
```
   주의: HARNESS_TOKENS 환경변수에서 토큰을 직접 멤버 식별자로 사용하지 않음 (개인정보 노출 금지).
   빈 리스트 전송 = protocol.ts의 members: string[] 타입 충족, 클라이언트가 room_member_joined로 점진 추가.

4. tests/test_harness_server.py에 TestDeltaReplay 클래스 추가:
```python
class TestDeltaReplay:
    def test_no_header_no_replay(self): ...
    def test_resume_from_filters_events(self): ...
    def test_resume_from_zero_sends_all(self): ...
    def test_non_integer_header_ignored(self): ...
    def test_overflow_header_ignored(self): ...
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestDeltaReplay -x -v && .venv/bin/python -m pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _run_session()에 x-resume-from 헤더 파싱 코드가 있다 (isdigit() + 2^31 상한선 체크)
    - delta 재송신 블록이 room_joined 이후에 있다
    - room_joined send()에 members=[] 필드가 추가됐다
    - TestDeltaReplay 5건 green
    - 전체 pytest green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PEXT-05 — _dispatch_loop() cancel 케이스 + _handle_input() CancelledError 처리</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (_dispatch_loop() 전체 — elif 체인 구조 확인, confirm_write_response 가드 패턴 확인; _handle_input() 전체 — except/finally 블록 구조 확인)
    - tests/test_harness_server.py (TestCancelTask 클래스 미존재 확인)
  </read_first>
  <behavior>
    - Test 1: active_input_from과 동일한 ws가 cancel 전송 시 input_tasks의 모든 task가 cancel()된다
    - Test 2: active_input_from이 아닌 ws가 cancel 전송 시 아무 일도 일어나지 않는다 (관전자 취소 방지)
    - Test 3: cancel 메시지 수신 후 broadcast(room, type='agent_cancelled')가 호출된다
    - Test 4: _handle_input()에서 CancelledError 발생 시 finally 블록에서 room.busy=False, room.active_input_from=None으로 정리된다
    - Test 5: task.cancel()이 이미 done() 상태인 task에 호출되어도 오류 없이 처리된다
  </behavior>
  <action>
1. _dispatch_loop()의 elif 체인에서 `elif t == 'ping':` 앞에 cancel 케이스 추가:
```python
elif t == 'cancel':
    # PEXT-05: 입력 주체(active_input_from)만 취소 가능 — confirm_write_response 와 동일한 DQ2 가드
    if ws is not room.active_input_from:
        continue
    # input_tasks에서 살아있는 task를 cancel (done() 체크로 안전 처리)
    for task in list(room.input_tasks):
        if not task.done():
            task.cancel()
    await broadcast(room, type='agent_cancelled')
```

2. _handle_input() 함수의 except 블록에 CancelledError 처리 추가:
```python
async def _handle_input(ws, room: 'Room', text: str):
    try:
        if text.startswith('/'):
            await handle_slash(ws, room, text)
        elif text.startswith('@claude '):
            await run_claude(ws, room, text[8:].strip(), add_to_session=True)
        else:
            await run_agent(ws, room, text)
            await broadcast_state(room)
    except asyncio.CancelledError:
        # PEXT-05: 정상 취소 경로 — busy/active_input_from은 finally에서 정리
        pass
    except Exception as e:
        await broadcast(room, type='error', text=f'입력 처리 오류: {e}')
    finally:
        room.busy = False
        room.active_input_from = None
```
   주의: 기존 finally 블록이 이미 있으면 CancelledError except만 추가하고 finally는 유지.

3. tests/test_harness_server.py에 TestCancelTask 클래스 추가:
```python
class TestCancelTask:
    def test_cancel_by_active_input_from_cancels_tasks(self): ...
    def test_cancel_by_non_active_ignored(self): ...
    def test_cancel_broadcasts_agent_cancelled(self): ...
    def test_cancelled_error_clears_busy(self): ...
    def test_cancel_on_done_task_safe(self): ...
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestCancelTask -x -v && .venv/bin/python -m pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _dispatch_loop()에 `elif t == 'cancel':` 케이스가 있다
    - cancel 케이스에 `ws is not room.active_input_from` 가드가 있다
    - task.done() 체크 후 cancel() 호출 패턴이 있다
    - broadcast(room, type='agent_cancelled') 호출이 있다
    - _handle_input()에 `except asyncio.CancelledError: pass`가 있다
    - TestCancelTask 5건 green
    - 전체 pytest green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 연결 헤더 → _run_session() | 헤더 값 파싱 시 isdigit() + 상한선으로 주입 방지 |
| cancel 메시지 → task cancel | active_input_from 가드로 권한 확인 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | x-resume-from 헤더 정수 주입 | mitigate | `isdigit()` 체크 + `< 2**31` 상한선 — 부동소수/음수/대형 정수 모두 거부 |
| T-03-02-02 | Tampering | 관전자 cancel 권한 우회 | mitigate | `ws is not room.active_input_from` identity 가드 — confirm_write_response와 동일 패턴 (VERIFIED: harness_server.py line 694) |
| T-03-02-03 | Denial of Service | cancel 반복 전송으로 task.cancel() 남용 | mitigate | `task.done()` 체크 후 cancel 호출. 이미 완료된 task에 cancel은 False 반환이며 무해 |
| T-03-02-04 | Tampering | resume_from=0 으로 ring buffer 전체 재송신 강제 | accept | 인증된 클라이언트만 연결 가능. ring buffer는 60초 이내 이벤트만 보유 (최대 10000건). 3인 스케일에서 부하 무시 가능 |
</threat_model>

<verification>
1. `grep -n 'resume_from' /Users/johyeonchang/harness/harness_server.py` — _run_session() 내 파싱 + delta 재송신 2개 블록 확인
2. `grep -n "'cancel'" /Users/johyeonchang/harness/harness_server.py` — _dispatch_loop() 내 cancel 케이스 확인
3. `grep -n 'CancelledError' /Users/johyeonchang/harness/harness_server.py` — _handle_input() 내 except 블록 확인
4. `grep -n 'agent_cancelled' /Users/johyeonchang/harness/harness_server.py` — broadcast 호출 확인
5. `.venv/bin/python -m pytest -x --tb=short` — 전체 pytest green
</verification>

<success_criteria>
- harness_server.py에 PEXT-04, PEXT-05 구현 완료
- x-resume-from 헤더 파싱이 isdigit() + 2^31 상한선 검증을 포함한다
- cancel 케이스가 active_input_from 가드를 통과한 경우에만 task.cancel()을 실행한다
- CancelledError가 _handle_input() finally에서 정리된다
- pytest 전체 green (기존 대비 5건 이상 증가)
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-02-SUMMARY.md` 작성.
</output>
