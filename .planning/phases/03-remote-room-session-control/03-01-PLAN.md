---
phase: 03-remote-room-session-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness_server.py
autonomous: true
requirements:
  - PEXT-01
  - PEXT-02
  - PEXT-03

must_haves:
  truths:
    - "broadcast() 호출마다 event_id가 monotonic하게 부여되고 ring buffer에 기록된다"
    - "agent_start 이벤트가 per-subscriber로 전송되며 입력자는 from_self=true, 관전자는 from_self=false를 수신한다"
    - "confirm_write 이벤트에 old_content 필드가 포함된다"
  artifacts:
    - path: "harness_server.py"
      provides: "PEXT-01..03 WS 프로토콜 확장"
      contains: "_broadcast_agent_start"
    - path: "tests/test_harness_server.py"
      provides: "PEXT-01..03 단위 테스트"
      contains: "TestAgentStartFromSelf"
  key_links:
    - from: "harness_server.py:run_agent()"
      to: "_broadcast_agent_start()"
      via: "per-subscriber from_self 분기"
      pattern: "_broadcast_agent_start"
    - from: "harness_server.py:broadcast()"
      to: "Room.event_buffer"
      via: "event_counter 증가 + deque append"
      pattern: "event_counter"
---

<objective>
Phase 3 서버 WS 프로토콜 확장 1차 — Room 클래스에 monotonic event_id + ring buffer를 추가하고 (PEXT-03), broadcast()를 확장한다. agent_start를 per-subscriber 방식으로 전환하여 from_self 필드를 제공하고 (PEXT-01), confirm_write에 old_content 필드를 추가한다 (PEXT-02).

Purpose: Wave 2~4 클라이언트 구현의 전제. 서버가 event_id를 발행해야 delta replay가 가능하고, from_self가 있어야 관전 모드 판정이 가능하다.
Output: harness_server.py에 PEXT-01~03 구현 완료 + pytest 테스트 3건 추가 (기존 234건 유지)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BB-2-DESIGN.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md

<interfaces>
<!-- harness_server.py 현재 구조 — executor가 수정할 핵심 위치 -->

<!-- Room dataclass (lines 122-133): -->
```python
@dataclass
class Room:
    name: str
    state: Session
    subscribers: set = field(default_factory=set)
    busy: bool = False
    active_input_from: object = None
    input_tasks: set = field(default_factory=set)
```

<!-- broadcast() 현재 (lines 82-98): -->
```python
async def broadcast(room: 'Room', **kwargs):
    if not room.subscribers:
        return
    payload = json.dumps(kwargs, ensure_ascii=False)
    dead = []
    for s in list(room.subscribers):
        try:
            await s.send(payload)
        except Exception:
            dead.append(s)
    for s in dead:
        room.subscribers.discard(s)
```

<!-- confirm_write 현재 (lines 188-203 근처): -->
```python
def confirm_write(path: str, content: str | None = None) -> bool:
    ...
    asyncio.run_coroutine_threadsafe(
        send(ws, type='confirm_write', path=path), loop  # ← old_content 누락
    )
```

<!-- run_agent() 내 agent_start broadcast (line 236 근처): -->
```python
await broadcast(room, type='agent_start')  # ← per-subscriber 교체 필요
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Room 클래스에 PEXT-03 event_counter + ring buffer 추가 + broadcast() 확장</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (전체 파일 — Room 클래스 위치, broadcast 함수, import 블록 확인)
    - tests/test_harness_server.py (TestBroadcast 클래스, test_payload_is_valid_json 확인 — exact equality 체크 여부)
  </read_first>
  <behavior>
    - Test 1: broadcast() 호출 후 room.event_counter가 1씩 증가한다
    - Test 2: broadcast() 호출 후 room.event_buffer에 (event_id, timestamp, payload_dict) 튜플이 저장된다
    - Test 3: 60초 이상 된 항목이 다음 broadcast() 호출 시 event_buffer에서 제거된다
    - Test 4: event_buffer.maxlen이 10000이다
    - Test 5: broadcast() 후 전송된 JSON 페이로드에 event_id 필드가 포함된다
    - Test 6 (기존 파괴 방지): test_payload_is_valid_json 이 assertSubset 패턴으로 변경되어 green을 유지한다
  </behavior>
  <action>
1. harness_server.py 파일 상단 import 블록에 추가 (기존 import 바로 뒤):
```python
from collections import deque
import time
```
(이미 있으면 건너뜀)

2. Room dataclass에 필드 추가 (input_tasks 필드 바로 뒤):
```python
    # PEXT-03: monotonic event_id + 60초 ring buffer (maxlen=10000 으로 메모리 고갈 방지)
    event_counter: int = field(default=0)
    event_buffer: deque = field(default_factory=lambda: deque(maxlen=10000))
```

3. broadcast() 함수 시작부를 교체 — subscribers 체크 전에 event_id 부여 + ring buffer 기록:
```python
async def broadcast(room: 'Room', **kwargs):
    # PEXT-03: event_id 부여 + ring buffer 기록
    room.event_counter += 1
    kwargs['event_id'] = room.event_counter
    now = time.monotonic()
    room.event_buffer.append((room.event_counter, now, dict(kwargs)))
    # TTL 60초 초과 항목 eager cleanup
    while room.event_buffer and (now - room.event_buffer[0][1]) > 60:
        room.event_buffer.popleft()

    if not room.subscribers:
        return
    payload = json.dumps(kwargs, ensure_ascii=False)
    dead = []
    for s in list(room.subscribers):
        try:
            await s.send(payload)
        except Exception:
            dead.append(s)
    for s in dead:
        room.subscribers.discard(s)
```

4. tests/test_harness_server.py에서 TestBroadcast 클래스를 찾아:
   - `test_payload_is_valid_json`이 exact equality로 페이로드를 비교하면 → `assertsubset` 패턴으로 변경:
     ```python
     data = json.loads(captured_payload)
     assert data['type'] == 'token'
     assert 'event_id' in data  # PEXT-03 필드 포함 확인
     ```
   - 신규 `TestEventBuffer` 클래스 추가:
     ```python
     class TestEventBuffer:
         def test_event_counter_increments(self): ...
         def test_ring_buffer_stores_payload(self): ...
         def test_ttl_cleanup(self): ...
         def test_maxlen(self): ...
     ```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestEventBuffer -x -v && .venv/bin/python -m pytest tests/test_harness_server.py -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Room 클래스에 event_counter: int와 event_buffer: deque(maxlen=10000) 필드가 있다
    - broadcast() 호출마다 event_id가 kwargs에 추가되고 ring buffer에 기록된다
    - TestEventBuffer 4건 green
    - 기존 pytest 전체 green (234건 유지)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PEXT-01 _broadcast_agent_start + PEXT-02 confirm_write old_content</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (run_agent() 함수 전체, confirm_write 콜백 함수 — 정확한 line 위치와 send() 호출 구조 확인)
    - tests/test_harness_server.py (TestAgentStartFromSelf, TestConfirmWriteOldContent 클래스 미존재 확인)
  </read_first>
  <behavior>
    - Test A1: 두 구독자가 있을 때 _broadcast_agent_start() 호출 시 requester_ws에는 from_self=True, 나머지에는 from_self=False 전송된다
    - Test A2: _broadcast_agent_start() 내 끊긴 ws는 room.subscribers에서 제거된다
    - Test B1: confirm_write 콜백에서 old_content=_read_existing_file(path) 가 send()에 포함된다
    - Test B2: _read_existing_file()이 존재하는 파일에서 내용을 반환한다
    - Test B3: _read_existing_file()이 존재하지 않는 파일에서 None을 반환한다
  </behavior>
  <action>
1. harness_server.py에 _read_existing_file() 헬퍼 추가 (broadcast 함수 근처):
```python
def _read_existing_file(path: str) -> str | None:
    '''PEXT-02: confirm_write 의 old_content 제공용 파일 읽기 헬퍼.
    파일이 없거나 읽기 실패 시 None 반환 — 신규 파일 쓰기 시 diff 없음.
    '''
    try:
        with open(path, encoding='utf-8') as f:
            return f.read()
    except OSError:
        return None
```

2. _broadcast_agent_start() 함수 추가 (broadcast() 아래):
```python
async def _broadcast_agent_start(room: 'Room', requester_ws) -> None:
    '''PEXT-01: agent_start 는 per-subscriber — from_self 플래그가 구독자마다 다름.
    broadcast() 와 동일한 dead 처리 패턴 사용.
    참고: event_id 부여는 broadcast() 경유가 아니므로 별도 counter 증가 없음
         (agent_start 는 from_self 로 인해 공통 payload 가 불가능).
    '''
    dead = []
    for s in list(room.subscribers):
        try:
            await send(s, type='agent_start', from_self=(s is requester_ws))
        except Exception:
            dead.append(s)
    for s in dead:
        room.subscribers.discard(s)
```

3. run_agent() 함수 내에서 `broadcast(room, type='agent_start')` 호출을 찾아 교체:
```python
# 기존: await broadcast(room, type='agent_start')
await _broadcast_agent_start(room, ws)  # PEXT-01: per-subscriber from_self 분기
```
   주의: run_agent()의 첫 번째 인자가 ws인지 확인. 없으면 함수 시그니처에서 ws 파라미터를 찾아 사용.

4. confirm_write 콜백에서 send() 호출을 찾아 old_content 추가:
```python
# 기존:
asyncio.run_coroutine_threadsafe(
    send(ws, type='confirm_write', path=path), loop
)
# 변경:
asyncio.run_coroutine_threadsafe(
    send(ws, type='confirm_write', path=path,
         old_content=_read_existing_file(path)), loop  # PEXT-02
)
```

5. tests/test_harness_server.py에 추가:
```python
class TestAgentStartFromSelf:
    def test_requester_gets_from_self_true(self): ...
    def test_observer_gets_from_self_false(self): ...
    def test_dead_ws_removed(self): ...

class TestConfirmWriteOldContent:
    def test_read_existing_file_returns_content(self): ...
    def test_read_existing_file_returns_none_for_missing(self): ...
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestAgentStartFromSelf tests/test_harness_server.py::TestConfirmWriteOldContent -x -v && .venv/bin/python -m pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _broadcast_agent_start() 함수가 harness_server.py에 존재한다
    - run_agent() 내 broadcast(room, type='agent_start') 호출이 _broadcast_agent_start(room, ws)로 교체됐다
    - _read_existing_file() 헬퍼가 존재한다
    - confirm_write send() 호출에 old_content=_read_existing_file(path)가 포함됐다
    - TestAgentStartFromSelf 3건 + TestConfirmWriteOldContent 2건 green
    - 전체 pytest green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 연결 → server handler | `HARNESS_TOKENS` hmac.compare_digest로 인증된 클라이언트만 통과 (기존 구현) |
| broadcast payload → ring buffer | event_id 추가 데이터가 ring buffer에 누적됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | Room.event_buffer (ring buffer) | mitigate | `deque(maxlen=10000)` 로 최대 크기 제한 — 60초 내 10000건 초과 시 오래된 항목 자동 제거 |
| T-03-01-02 | Tampering | _broadcast_agent_start() from_self 플래그 | mitigate | `s is requester_ws` identity 비교 — 해시/토큰 위조 불가, 파이썬 객체 동일성 사용 |
| T-03-01-03 | Information Disclosure | confirm_write old_content 파일 내용 | accept | 서버는 이미 해당 파일에 접근 가능한 권한으로 실행 중. old_content는 이미 서버가 읽을 수 있는 파일만. 인증된 클라이언트에게만 전송 (active_input_from 가드) |
| T-03-01-04 | Denial of Service | broadcast() event_counter overflow | accept | Python int는 arbitrary precision — overflow 없음. 3인 스케일에서 2^31 초과 불가 |
</threat_model>

<verification>
1. `grep -n 'event_counter' /Users/johyeonchang/harness/harness_server.py` — Room 클래스와 broadcast() 양쪽에 존재 확인
2. `grep -n '_broadcast_agent_start' /Users/johyeonchang/harness/harness_server.py` — 함수 정의 + run_agent() 호출 2건 확인
3. `grep -n 'old_content' /Users/johyeonchang/harness/harness_server.py` — _read_existing_file + send() 호출 2건 확인
4. `.venv/bin/python -m pytest -x --tb=short` — 전체 pytest green (234건 이상)
</verification>

<success_criteria>
- harness_server.py에 PEXT-01, PEXT-02, PEXT-03 구현 완료
- broadcast()가 event_id를 자동 부여하고 ring buffer에 기록한다
- agent_start가 per-subscriber로 전송되어 from_self 필드가 구독자마다 다르다
- confirm_write에 old_content 필드가 포함된다
- pytest 전체 green (기존 234건 + 신규 ~8건 = 242건 이상)
- 기존 테스트 삭제 없음
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-01-SUMMARY.md` 작성.
</output>
