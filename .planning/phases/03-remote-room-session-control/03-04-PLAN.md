---
phase: 03-remote-room-session-control
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - ui-ink/src/utils/userColor.ts
  - ui-ink/src/components/PresenceSegment.tsx
  - ui-ink/src/components/ReconnectOverlay.tsx
  - ui-ink/src/components/ObserverOverlay.tsx
autonomous: true
requirements:
  - REM-02
  - REM-04
  - WSR-02
  - DIFF-01
  - DIFF-04

must_haves:
  truths:
    - "userColor(token)이 PALETTE[hash(token) % 8]를 반환하고, 자기 자신(HARNESS_TOKEN)은 항상 'cyan'을 반환한다"
    - "PresenceSegment가 roomName 없으면 null을 반환하고, 있으면 '🟢 N명 [alice·me]' 형식으로 렌더된다"
    - "ReconnectOverlay가 attempt prop에 따라 'disconnected — reconnecting... (attempt N/10)' 텍스트를 yellow로 렌더한다"
    - "ReconnectOverlay가 failed prop일 때 'disconnected — reconnect failed. Ctrl+C to exit.' 텍스트를 red로 렌더한다"
    - "ObserverOverlay가 username을 받아 '{username} 입력 중...' 형식을 dimColor italic으로 렌더한다"
  artifacts:
    - path: "ui-ink/src/utils/userColor.ts"
      provides: "결정론적 사용자 색 해시 순수함수"
      contains: "userColor"
    - path: "ui-ink/src/components/PresenceSegment.tsx"
      provides: "StatusBar 서브컴포넌트 — Presence 세그먼트"
      contains: "PresenceSegment"
    - path: "ui-ink/src/components/ReconnectOverlay.tsx"
      provides: "재연결 중 InputArea 치환 컴포넌트"
      contains: "ReconnectOverlay"
    - path: "ui-ink/src/components/ObserverOverlay.tsx"
      provides: "관전자 InputArea 치환 컴포넌트"
      contains: "ObserverOverlay"
  key_links:
    - from: "PresenceSegment.tsx"
      to: "store/room.ts"
      via: "useRoomStore(useShallow(s => ({roomName, members})))"
      pattern: "useRoomStore"
    - from: "PresenceSegment.tsx"
      to: "utils/userColor.ts"
      via: "userColor(m) 호출"
      pattern: "userColor"
    - from: "ReconnectOverlay.tsx"
      to: "store/room.ts"
      via: "wsState === 'reconnecting'"
      pattern: "wsState"
    - from: "ObserverOverlay.tsx"
      to: "utils/userColor.ts"
      via: "userColor(username) 호출"
      pattern: "userColor"
---

<objective>
Wave 2 신규 컴포넌트 4종 구현 — userColor.ts 순수함수 유틸 (DIFF-04), PresenceSegment.tsx (REM-02), ReconnectOverlay.tsx (WSR-02), ObserverOverlay.tsx (REM-04/DIFF-01). Plan 03-03과 병렬로 실행 가능 (파일 충돌 없음).

Purpose: Wave 4의 App.tsx 치환 우선순위와 StatusBar 연결에 필요한 컴포넌트를 미리 완성한다. 각 컴포넌트는 독립적으로 vitest로 검증 가능하다.
Output: 4개 신규 파일 생성. vitest 컴포넌트 테스트 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-remote-room-session-control/03-UI-SPEC.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md

<interfaces>
<!-- 기존 패턴 - CtxMeter 서브컴포넌트 (StatusBar.tsx lines 43-48): -->
```typescript
function CtxMeter() {
  const {ctxTokens} = useStatusStore(useShallow((s) => ({ctxTokens: s.ctxTokens})))
  if (typeof ctxTokens !== 'number') return null
  const pct = Math.min(100, Math.round((ctxTokens / MAX_CTX) * 100))
  return <Text color={theme.muted}>ctx {pct}%</Text>
}
```

<!-- 기존 패턴 - ConfirmReadOnlyView (ConfirmDialog.tsx lines 154-168): -->
```typescript
function ConfirmReadOnlyView({mode, payload}: ReadOnlyProps): React.ReactElement {
  return (
    <Box flexDirection='column' borderStyle='round' borderColor='gray' paddingX={1}>
      <Text color='gray'>{label} (관전 중 — 응답 불가)</Text>
      <Text dimColor>{detail}</Text>
    </Box>
  )
}
```

<!-- tty-guard.ts 순수함수 패턴 (lines 1-11): -->
```typescript
export function isInteractiveTTY(stdin: NodeJS.ReadStream): boolean {
  return stdin.isTTY === true && typeof stdin.setRawMode === 'function'
}
```

<!-- store/room.ts Phase 3 확장 후 (Plan 03-03에서 추가): -->
```typescript
interface RoomState {
  roomName: string
  members: string[]
  activeInputFrom: string | null
  activeIsSelf: boolean
  wsState: 'connected' | 'reconnecting' | 'failed'
  reconnectAttempt: number
  lastEventId: number | null
  // ...
}
```

<!-- 03-UI-SPEC.md 색상 계약: -->
<!--
- userColor PALETTE: ['cyan', 'green', 'yellow', 'magenta', 'blue', 'red', 'white', 'greenBright']
- 자기 자신: 항상 'cyan' (HARNESS_TOKEN 기준)
- Presence 세그먼트: '🟢 N명 [alice·me]', · = gray separator
- ReconnectOverlay: color='yellow' / failed시 color='red'
- ObserverOverlay: dimColor italic
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: utils/userColor.ts + PresenceSegment.tsx 구현</name>
  <files>
    ui-ink/src/utils/userColor.ts,
    ui-ink/src/components/PresenceSegment.tsx,
    ui-ink/src/__tests__/userColor.test.ts,
    ui-ink/src/__tests__/components.presence.test.tsx
  </files>
  <read_first>
    - ui-ink/src/tty-guard.ts (순수함수 파일 구조 패턴 확인)
    - ui-ink/src/components/StatusBar.tsx (CtxMeter 서브컴포넌트 + useShallow 패턴 확인)
    - ui-ink/src/store/room.ts (Plan 03-03 이후 — roomName/members/wsState 필드 확인)
    - ui-ink/src/__tests__/ 디렉토리 내 기존 테스트 파일 패턴 확인
  </read_first>
  <behavior>
    - Test U1: userColor('') 같은 빈 토큰 → PALETTE 중 하나 반환 (crash 없음)
    - Test U2: userColor(HARNESS_TOKEN) → 'cyan' (환경변수가 있으면)
    - Test U3: 동일 토큰 → 항상 동일 색 반환 (결정론)
    - Test U4: 두 다른 토큰 → PALETTE 범위 내 색 반환 (0..7 인덱스)
    - Test P1: roomName='' 시 PresenceSegment → null (미렌더)
    - Test P2: roomName='team', members=['alice','me'] → '🟢 2명' 텍스트 포함
    - Test P3: members 사이에 '·' 구분자 gray 있음
  </behavior>
  <action>
**ui-ink/src/utils/userColor.ts 생성:**
```typescript
// 사용자 색 해시 유틸 (DIFF-04, REM-02)
// 결정론적 — 동일 토큰은 항상 동일 색 반환 (재접속 후에도 동일)
// 03-UI-SPEC.md §사용자 색 해시 규격

const PALETTE = ['cyan', 'green', 'yellow', 'magenta', 'blue', 'red', 'white', 'greenBright'] as const

function _hash(token: string): number {
  // djb2 변형: 16비트 범위 유지
  return token.split('').reduce((acc, ch) => (acc * 31 + ch.charCodeAt(0)) & 0xffff, 0)
}

export function userColor(token: string): string {
  // 자기 자신은 항상 cyan — 기존 user role 색과 통일 (03-UI-SPEC.md)
  const myToken = process.env['HARNESS_TOKEN'] ?? ''
  if (token === myToken || token === 'me') return 'cyan'
  return PALETTE[_hash(token) % PALETTE.length]
}
```

**ui-ink/src/components/PresenceSegment.tsx 생성:**
```typescript
// PresenceSegment — StatusBar 서브컴포넌트 (REM-02, DIFF-04)
// CtxMeter 격리 패턴 동일 — roomName 없으면 null
// 03-UI-SPEC.md §Presence 세그먼트
import React from 'react'
import {Text} from 'ink'
import {useShallow} from 'zustand/react/shallow'
import {useRoomStore} from '../store/room.js'
import {userColor} from '../utils/userColor.js'

export function PresenceSegment(): React.ReactElement | null {
  const {roomName, members} = useRoomStore(useShallow((s) => ({
    roomName: s.roomName,
    members: s.members,
  })))

  // solo 모드(roomName 없음): CtxMeter의 null 반환과 동일 패턴
  if (!roomName) return null

  const count = members.length

  return (
    <Text>
      {'🟢 '}
      <Text color='green'>{count}명</Text>
      {' ['}
      {members.map((m, i) => (
        <React.Fragment key={m}>
          {i > 0 && <Text color='gray'>{' · '}</Text>}
          <Text color={userColor(m)} bold>{m === process.env['HARNESS_TOKEN'] ? 'me' : m}</Text>
        </React.Fragment>
      ))}
      {']'}
    </Text>
  )
}
```
  </action>
  <acceptance_criteria>
    - `test -f ui-ink/src/utils/userColor.ts` → 파일 존재
    - `test -f ui-ink/src/components/PresenceSegment.tsx` → 파일 존재
    - `grep 'PALETTE' ui-ink/src/utils/userColor.ts` → 8색 배열 있음
    - `grep 'useShallow' ui-ink/src/components/PresenceSegment.tsx` → 1건 (멀티 필드 구독)
    - `grep "\.js'" ui-ink/src/components/PresenceSegment.tsx` → import에 .js 확장자 있음
    - `cd ui-ink && bun run test -- --reporter=verbose 2>&1 | grep -E 'userColor|presence'` → green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run test -- --reporter=verbose 2>&1 | grep -E '(PASS|FAIL|userColor|presence)' | head -20</automated>
  </verify>
  <done>
    - userColor.ts: PALETTE 8색 + _hash() + userColor() 순수함수
    - PresenceSegment.tsx: solo 모드 null 반환, roomName 있으면 '🟢 N명 [...]' 렌더
    - userColor 테스트 4건 + presence 테스트 3건 green
    - tsc --noEmit green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReconnectOverlay.tsx + ObserverOverlay.tsx 구현</name>
  <files>
    ui-ink/src/components/ReconnectOverlay.tsx,
    ui-ink/src/components/ObserverOverlay.tsx,
    ui-ink/src/__tests__/components.observer.test.tsx
  </files>
  <read_first>
    - ui-ink/src/components/ConfirmDialog.tsx (lines 154-168 ConfirmReadOnlyView — Box+Text read-only 치환 패턴 확인)
    - ui-ink/src/store/room.ts (wsState/reconnectAttempt/activeInputFrom 필드 확인)
    - ui-ink/src/utils/userColor.ts (방금 생성한 파일 — userColor 시그니처 확인)
    - ui-ink/src/__tests__/components.observer.test.tsx (미존재 확인)
  </read_first>
  <behavior>
    - Test R1: attempt=3 prop → 텍스트에 '(attempt 3/10)' 포함
    - Test R2: attempt prop → color='yellow' 텍스트
    - Test R3: failed=true prop → 텍스트에 'reconnect failed' 포함, color='red'
    - Test O1: username='alice' → '입력 중...' 텍스트 포함
    - Test O2: username=null → '상대방 입력 중...' 렌더 (null 안전)
    - Test O3: '입력 중...' 부분에 dimColor italic 속성
  </behavior>
  <action>
**ui-ink/src/components/ReconnectOverlay.tsx 생성:**
```typescript
// ReconnectOverlay — WS 재연결 중 InputArea 치환 컴포넌트 (WSR-02)
// ConfirmReadOnlyView와 동일한 InputArea 치환 패턴
// 03-UI-SPEC.md §재연결 오버레이
import React from 'react'
import {Box, Text} from 'ink'

interface ReconnectOverlayProps {
  attempt?: number   // 재연결 시도 횟수 (1 기준)
  failed?: boolean   // 10회 실패 후
}

export function ReconnectOverlay({attempt, failed}: ReconnectOverlayProps): React.ReactElement {
  if (failed) {
    return (
      <Box>
        <Text color='red'>disconnected — reconnect failed. Ctrl+C to exit.</Text>
      </Box>
    )
  }

  return (
    <Box>
      <Text color='yellow'>
        {`disconnected — reconnecting... (attempt ${attempt ?? 1}/10)`}
      </Text>
    </Box>
  )
}
```

**ui-ink/src/components/ObserverOverlay.tsx 생성:**
```typescript
// ObserverOverlay — 관전자 시 InputArea 치환 컴포넌트 (REM-04, DIFF-01)
// "A 입력 중..." — 입력 주체 아닌 관전자에게만 표시
// 03-UI-SPEC.md §"A 입력 중" 오버레이
import React from 'react'
import {Box, Text} from 'ink'
import {userColor} from '../utils/userColor.js'

interface ObserverOverlayProps {
  username: string | null  // activeInputFrom (null 안전)
}

export function ObserverOverlay({username}: ObserverOverlayProps): React.ReactElement {
  const displayName = username ?? '상대방'
  return (
    <Box>
      <Text color={userColor(username ?? '')} bold>{displayName}</Text>
      <Text dimColor italic>{' 입력 중...'}</Text>
    </Box>
  )
}
```
  </action>
  <acceptance_criteria>
    - `test -f ui-ink/src/components/ReconnectOverlay.tsx` → 파일 존재
    - `test -f ui-ink/src/components/ObserverOverlay.tsx` → 파일 존재
    - `grep "color='yellow'" ui-ink/src/components/ReconnectOverlay.tsx` → 1건
    - `grep "color='red'" ui-ink/src/components/ReconnectOverlay.tsx` → 1건
    - `grep 'dimColor italic' ui-ink/src/components/ObserverOverlay.tsx` → 존재 (또는 별도 속성으로)
    - `grep "\.js'" ui-ink/src/components/ObserverOverlay.tsx` → .js 확장자 import
    - `cd ui-ink && bun run test -- --reporter=verbose 2>&1 | grep -E '(observer|reconnect)'` → green
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit && bun run test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ReconnectOverlay.tsx: attempt prop → yellow 텍스트, failed prop → red 텍스트
    - ObserverOverlay.tsx: username 색 + 'N 입력 중...' dimColor italic
    - observer 테스트 3건 + reconnect 테스트 3건 green
    - tsc --noEmit green
    - 전체 vitest green (기존 120건 + 신규 6건 이상)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| username prop → ObserverOverlay | username은 서버에서 수신한 문자열. 컴포넌트 출력은 터미널 텍스트 (XSS 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | userColor token으로 PALETTE 범위 외 접근 | mitigate | `% PALETTE.length` 모듈로 연산으로 인덱스 범위 보장. 빈 문자열 포함 모든 입력 안전 |
| T-03-04-02 | Information Disclosure | HARNESS_TOKEN 환경변수 노출 | accept | userColor는 토큰 원문이 아닌 'me'/'cyan' 여부만 판단. 토큰 값 자체를 렌더하지 않음 |
| T-03-04-03 | Tampering | username에 터미널 escape 시퀀스 포함 | accept | Ink Text 컴포넌트가 문자열을 안전하게 출력 (raw write 없음). ANSI injection 불가 |
</threat_model>

<verification>
1. `cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit` → 에러 0
2. `cd /Users/johyeonchang/harness/ui-ink && bun run test` → vitest green
3. `ls ui-ink/src/utils/userColor.ts ui-ink/src/components/PresenceSegment.tsx ui-ink/src/components/ReconnectOverlay.tsx ui-ink/src/components/ObserverOverlay.tsx` → 4개 파일 존재
4. `grep 'PALETTE' ui-ink/src/utils/userColor.ts | grep 'cyan'` → 8색 팔레트 확인
</verification>

<success_criteria>
- userColor.ts: 결정론적 hash + 자기 자신=cyan + PALETTE 8색
- PresenceSegment.tsx: solo 모드 null, room 모드 '🟢 N명 [alice·me]'
- ReconnectOverlay.tsx: 재연결 중 yellow, 실패 red
- ObserverOverlay.tsx: dimColor italic '입력 중...'
- tsc + vitest 전부 green
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-04-SUMMARY.md` 작성.
</output>
