---
phase: 03-remote-room-session-control
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - ui-ink/src/protocol.ts
  - ui-ink/src/store/room.ts
  - ui-ink/src/store/messages.ts
  - ui-ink/src/ws/dispatch.ts
autonomous: true
requirements:
  - REM-02
  - REM-03
  - REM-04
  - REM-05
  - DIFF-01
  - DIFF-03

must_haves:
  truths:
    - "protocol.ts에 AgentCancelledMsg 타입이 있고 ServerMsg union에 포함된다"
    - "AgentStartMsg에 from_self?: boolean 필드가 있다"
    - "ConfirmWriteMsg에 old_content?: string 필드가 있다"
    - "RoomJoinedMsg가 서버 실제 전송 구조(subscribers: number, members?: string[])와 일치한다"
    - "store/room.ts에 wsState, reconnectAttempt, lastEventId 필드와 setter가 있다"
    - "store/messages.ts에 loadSnapshot() action과 snapshotKey 필드가 있다"
    - "dispatch.ts에서 agent_start 이벤트가 from_self 필드를 처리한다"
    - "dispatch.ts에서 state_snapshot이 messages.loadSnapshot()을 호출한다"
    - "dispatch.ts에서 agent_cancelled 이벤트를 처리한다"
    - "dispatch.ts 모든 케이스에서 event_id 추적(setLastEventId)이 수행된다"
  artifacts:
    - path: "ui-ink/src/protocol.ts"
      provides: "PEXT-01/02/05 타입 업데이트"
      contains: "AgentCancelledMsg"
    - path: "ui-ink/src/store/room.ts"
      provides: "wsState/reconnectAttempt/lastEventId 확장"
      contains: "wsState"
    - path: "ui-ink/src/store/messages.ts"
      provides: "loadSnapshot action + snapshotKey"
      contains: "loadSnapshot"
    - path: "ui-ink/src/ws/dispatch.ts"
      provides: "dispatch 확장 (agent_start from_self, state_snapshot 히스토리, agent_cancelled)"
      contains: "agent_cancelled"
  key_links:
    - from: "dispatch.ts:agent_start case"
      to: "store/room.ts:setActiveIsSelf"
      via: "msg.from_self ?? true"
      pattern: "setActiveIsSelf"
    - from: "dispatch.ts:state_snapshot case"
      to: "store/messages.ts:loadSnapshot"
      via: "msg.messages 배열"
      pattern: "loadSnapshot"
    - from: "dispatch.ts(모든 case)"
      to: "store/room.ts:setLastEventId"
      via: "event_id 필드 추적"
      pattern: "setLastEventId"
---

<objective>
Wave 2 클라이언트 기반 — protocol.ts 타입 업데이트 (PEXT-01/02/05 반영 + Pitfall H 수정), store/room.ts 확장 (wsState/reconnectAttempt/lastEventId), store/messages.ts에 loadSnapshot 추가, dispatch.ts 확장 (from_self 처리, state_snapshot 히스토리 로드, agent_cancelled 처리, event_id 추적).

Purpose: Wave 3~4 컴포넌트들이 참조할 store 계약과 이벤트 처리 계층을 먼저 확정한다. 컴포넌트는 이 플랜 이후에 구현 가능하다.
Output: TypeScript 타입 + store 슬라이스 + dispatch 확장 완료. vitest green 유지.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md
@.planning/phases/03-remote-room-session-control/03-UI-SPEC.md

<interfaces>
<!-- protocol.ts 현재 (전체 내용은 이미 컨텍스트에 있음): -->
<!-- 수정 대상: -->
```typescript
// 현재:
export interface AgentStartMsg   { type: 'agent_start' }
export interface ConfirmWriteMsg { type: 'confirm_write'; path: string }
export interface RoomJoinedMsg   { type: 'room_joined'; room: string; members: string[] }
// ServerMsg union에 AgentCancelledMsg 없음
```

<!-- store/room.ts 현재 (전체 내용 컨텍스트에 있음): -->
<!-- 추가 필드 없음: wsState, reconnectAttempt, lastEventId -->

<!-- store/messages.ts 현재 (전체 내용 컨텍스트에 있음): -->
<!-- loadSnapshot action 없음, snapshotKey 없음 -->

<!-- dispatch.ts 현재 (전체 내용 컨텍스트에 있음): -->
```typescript
// 현재:
case 'agent_start':
  messages.agentStart()
  status.setBusy(true)
  break  // ← from_self 처리 없음

case 'state_snapshot':
  status.setState({...})  // ← messages.loadSnapshot() 호출 없음
  break

// agent_cancelled case 없음 — exhaustive switch에서 컴파일 에러 예정
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: protocol.ts 타입 업데이트 + store/room.ts wsState 확장</name>
  <files>ui-ink/src/protocol.ts, ui-ink/src/store/room.ts</files>
  <read_first>
    - ui-ink/src/protocol.ts (현재 전체 — ServerMsg union 구조, AgentStartMsg/ConfirmWriteMsg/RoomJoinedMsg 정확한 위치)
    - ui-ink/src/store/room.ts (현재 전체 — RoomState interface, useRoomStore create 블록)
  </read_first>
  <action>
**protocol.ts 수정:**

1. AgentStartMsg에 from_self 필드 추가 (PEXT-01):
```typescript
export interface AgentStartMsg   { type: 'agent_start'; from_self?: boolean }
```

2. ConfirmWriteMsg에 old_content 필드 추가 (PEXT-02):
```typescript
export interface ConfirmWriteMsg { type: 'confirm_write'; path: string; old_content?: string }
```

3. RoomJoinedMsg를 서버 실제 전송과 일치하게 수정 (Pitfall H 수정):
```typescript
export interface RoomJoinedMsg   {
  type: 'room_joined'
  room: string
  shared: boolean
  subscribers: number
  busy: boolean
  members?: string[]  // optional — 서버가 빈 배열 전송 (Plan 03-02에서 추가됨)
}
```

4. AgentCancelledMsg 인터페이스 신규 추가 (PEXT-05):
```typescript
export interface AgentCancelledMsg { type: 'agent_cancelled' }
```

5. ServerMsg union에 AgentCancelledMsg 추가:
```typescript
export type ServerMsg =
  | TokenMsg | ToolStartMsg | ToolEndMsg
  | AgentStartMsg | AgentEndMsg | AgentCancelledMsg  // ← 추가
  | ErrorMsg | InfoMsg
  | ConfirmWriteMsg | ConfirmBashMsg | CplanConfirmMsg
  | ReadyMsg
  | RoomJoinedMsg | RoomMemberJoinedMsg | RoomMemberLeftMsg | RoomBusyMsg
  | StateSnapshotMsg | StateMsg
  | SlashResultMsg
  | QuitMsg | QueueMsg | QueueReadyMsg | PongMsg
  | ClaudeStartMsg | ClaudeEndMsg | ClaudeTokenMsg
```

**store/room.ts 수정:**

1. RoomState interface에 필드 추가:
```typescript
interface RoomState {
  roomName: string
  members: string[]
  activeInputFrom: string | null
  activeIsSelf: boolean
  busy: boolean
  // Phase 3 추가 (WSR-01~03)
  wsState: 'connected' | 'reconnecting' | 'failed'
  reconnectAttempt: number
  lastEventId: number | null
  // actions
  setRoom: (name: string, members: string[]) => void
  addMember: (user: string) => void
  removeMember: (user: string) => void
  setActiveInputFrom: (user: string | null) => void
  setActiveIsSelf: (v: boolean) => void
  setRoomBusy: (v: boolean) => void
  // Phase 3 신규 actions
  setWsState: (s: 'connected' | 'reconnecting' | 'failed') => void
  setReconnectAttempt: (n: number) => void
  setLastEventId: (id: number) => void
}
```

2. useRoomStore create 블록에 초기값 + actions 추가:
```typescript
export const useRoomStore = create<RoomState>((set) => ({
  roomName: '',
  members: [],
  activeInputFrom: null,
  activeIsSelf: true,
  busy: false,
  // Phase 3 초기값
  wsState: 'connected',
  reconnectAttempt: 0,
  lastEventId: null,
  // 기존 actions (변경 없음)
  setRoom: (name, members) => set({roomName: name, members}),
  addMember: (user) => set((s) => ({members: [...s.members.filter(m => m !== user), user]})),
  removeMember: (user) => set((s) => ({members: s.members.filter(m => m !== user)})),
  setActiveInputFrom: (user) => set({activeInputFrom: user}),
  setActiveIsSelf: (v) => set({activeIsSelf: v}),
  setRoomBusy: (v) => set({busy: v}),
  // Phase 3 신규 actions
  setWsState: (s) => set({wsState: s}),
  setReconnectAttempt: (n) => set({reconnectAttempt: n}),
  setLastEventId: (id) => set({lastEventId: id}),
}))
```
  </action>
  <acceptance_criteria>
    - `grep 'from_self' ui-ink/src/protocol.ts` → AgentStartMsg에 from_self?: boolean 있음
    - `grep 'old_content' ui-ink/src/protocol.ts` → ConfirmWriteMsg에 old_content?: string 있음
    - `grep 'AgentCancelledMsg' ui-ink/src/protocol.ts` → 인터페이스 정의 + ServerMsg union 2건
    - `grep 'subscribers: number' ui-ink/src/protocol.ts` → RoomJoinedMsg에 있음
    - `grep 'wsState' ui-ink/src/store/room.ts` → interface + create 블록 2건 이상
    - `grep 'lastEventId' ui-ink/src/store/room.ts` → interface + create 블록 2건 이상
    - `cd ui-ink && bun run tsc --noEmit` → 에러 없음 (dispatch.ts의 agent_cancelled case 추가 전까지 일시적 에러 허용)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit 2>&1 | grep -E 'error TS' | grep -v 'agent_cancelled' | head -10</automated>
  </verify>
  <done>
    - protocol.ts에 AgentStartMsg.from_self?, ConfirmWriteMsg.old_content?, AgentCancelledMsg, RoomJoinedMsg(subscribers: number) 반영
    - store/room.ts에 wsState/reconnectAttempt/lastEventId 필드 + setter 추가
    - agent_cancelled 관련 tsc 에러만 있고 다른 에러 없음 (Task 2에서 dispatch 케이스 추가 후 완전 green)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store/messages.ts loadSnapshot + dispatch.ts 확장</name>
  <files>ui-ink/src/store/messages.ts, ui-ink/src/ws/dispatch.ts, ui-ink/src/__tests__/store.messages.test.ts</files>
  <read_first>
    - ui-ink/src/store/messages.ts (MessagesState interface 전체, useMessagesStore create 블록 — clearMessages 패턴 확인)
    - ui-ink/src/ws/dispatch.ts (전체 — agent_start/state_snapshot/room_joined 케이스 정확한 위치, assertNever default 위치)
    - ui-ink/src/__tests__/store.messages.test.ts (존재 여부 및 기존 테스트 패턴 확인)
  </read_first>
  <behavior>
    - Test 1: loadSnapshot([]) 호출 시 completedMessages가 빈 배열이 되고 snapshotKey가 1 증가한다
    - Test 2: loadSnapshot([{role:'user', content:'hi'}]) 호출 시 completedMessages에 id가 부여된 메시지가 로드된다
    - Test 3: loadSnapshot() 호출 후 activeMessage가 null이 된다
    - Test 4: dispatch('agent_start', {from_self: false}) 후 room.activeIsSelf가 false가 된다
    - Test 5: dispatch('agent_start', {}) (from_self 없음) 후 room.activeIsSelf가 true가 된다 (구버전 호환)
    - Test 6: dispatch('agent_cancelled') 후 busy가 false가 된다
    - Test 7: dispatch에서 event_id: 42가 있는 메시지 수신 시 room.lastEventId가 42가 된다
  </behavior>
  <action>
**store/messages.ts 수정:**

1. MessagesState interface에 snapshotKey 필드와 loadSnapshot action 추가:
```typescript
interface MessagesState {
  completedMessages: Message[]
  activeMessage: Message | null
  snapshotKey: number           // Phase 3: Static key remount 트리거 (REM-03)
  appendUserMessage: (content: string, meta?: Record<string, unknown>) => void
  agentStart: () => void
  appendToken: (text: string) => void
  agentEnd: () => void
  appendToolStart: (name: string, args: Record<string, unknown>) => void
  appendToolEnd: (name: string, result: string) => void
  appendSystemMessage: (content: string) => void
  clearMessages: () => void
  loadSnapshot: (rawMessages: unknown[]) => void  // Phase 3: state_snapshot 히스토리 로드
}
```

2. useMessagesStore create 블록에 snapshotKey 초기값 + loadSnapshot 추가:
```typescript
// 초기값에 추가:
snapshotKey: 0,

// actions에 추가:
loadSnapshot: (rawMessages) => set((s) => {
  // rawMessages는 서버의 messages 배열. 최대한 안전하게 파싱.
  const parsed: Message[] = rawMessages
    .filter((m): m is Record<string, unknown> => typeof m === 'object' && m !== null)
    .map((m) => ({
      id: crypto.randomUUID(),
      role: (['user', 'assistant', 'tool', 'system'].includes(String(m['role']))
        ? m['role'] as Message['role']
        : 'system'),
      content: typeof m['content'] === 'string' ? m['content'] : JSON.stringify(m),
      meta: typeof m['meta'] === 'object' && m['meta'] !== null
        ? m['meta'] as Record<string, unknown>
        : undefined,
    }))
  return {
    snapshotKey: s.snapshotKey + 1,  // Static key remount 트리거
    completedMessages: parsed,
    activeMessage: null,
  }
}),
```

   appendUserMessage를 meta 파라미터 지원으로 확장 (DIFF-02 author prefix용):
```typescript
appendUserMessage: (content, meta?) => set((s) => ({
  completedMessages: [...s.completedMessages, {
    id: crypto.randomUUID(), role: 'user', content, meta,
  }],
})),
```

**dispatch.ts 수정:**

1. dispatch() 함수 시작부에 event_id 추적 추가 (switch 문 전):
```typescript
export function dispatch(msg: ServerMsg): void {
  const messages = useMessagesStore.getState()
  const status = useStatusStore.getState()
  const room = useRoomStore.getState()
  const confirm = useConfirmStore.getState()

  // PEXT-03: event_id 추적 — 모든 이벤트에 적용
  if ('event_id' in msg && typeof (msg as {event_id?: unknown}).event_id === 'number') {
    room.setLastEventId((msg as {event_id: number}).event_id)
  }

  switch (msg.type) {
    // ...
```

2. agent_start 케이스 확장 (from_self 처리):
```typescript
case 'agent_start':
  messages.agentStart()
  status.setBusy(true)
  // PEXT-01: from_self 필드로 관전 모드 판정 (undefined → true = 구버전 호환)
  room.setActiveIsSelf(msg.from_self ?? true)
  break
```

3. state_snapshot 케이스 확장 (loadSnapshot 추가):
```typescript
case 'state_snapshot':
  status.setState({
    working_dir: msg.working_dir,
    model: msg.model,
    mode: msg.mode,
    turns: msg.turns,
    ctx_tokens: msg.ctx_tokens,
  })
  // REM-03: 과거 turn 히스토리 일괄 로드 (Static key remount 트리거)
  if (msg.messages && Array.isArray(msg.messages)) {
    messages.loadSnapshot(msg.messages)
  }
  break
```

4. room_joined 케이스 수정 (Pitfall H 수정 — members 처리):
```typescript
case 'room_joined':
  // msg.members는 optional — 없으면 빈 배열 (Plan 03-02에서 서버가 [] 전송)
  room.setRoom(msg.room, msg.members ?? [])
  break
```

5. room_member_joined/left 시스템 메시지 UI-SPEC 포맷으로 교정 (REM-05):
```typescript
case 'room_member_joined':
  room.addMember(msg.user)
  messages.appendSystemMessage(`↗ ${msg.user} 님이 참여했습니다`)
  break

case 'room_member_left':
  room.removeMember(msg.user)
  messages.appendSystemMessage(`↘ ${msg.user} 님이 나갔습니다`)
  break
```

6. agent_cancelled 케이스 추가 (PEXT-05):
```typescript
case 'agent_cancelled':
  messages.agentEnd()
  status.setBusy(false)
  room.setActiveIsSelf(true)
  messages.appendSystemMessage('에이전트 실행이 취소되었습니다')
  break
```

7. confirm_write 케이스에 old_content 전달 (PEXT-02 연계):
```typescript
case 'confirm_write':
  confirm.setConfirm('confirm_write', {path: msg.path, oldContent: msg.old_content})
  break
```
   주의: store/confirm.ts의 payload 타입이 oldContent를 지원하는지 확인. 없으면 Record<string, unknown>에 그대로 넣으면 됨.
  </action>
  <acceptance_criteria>
    - `grep 'snapshotKey' ui-ink/src/store/messages.ts` → interface + 초기값 + loadSnapshot 3건
    - `grep 'loadSnapshot' ui-ink/src/store/messages.ts` → interface + action 2건
    - `grep 'loadSnapshot' ui-ink/src/ws/dispatch.ts` → state_snapshot case 내 1건
    - `grep 'from_self' ui-ink/src/ws/dispatch.ts` → agent_start case 내 1건
    - `grep 'agent_cancelled' ui-ink/src/ws/dispatch.ts` → case 1건
    - `grep 'setLastEventId' ui-ink/src/ws/dispatch.ts` → dispatch() 시작부 1건
    - `grep "addMember(msg.user)" ui-ink/src/ws/dispatch.ts` → room_member_joined case 내 1건 (B4: 서버가 Plan 03-02에서 user 필드 전송 — msg.user가 실제 token_hash[:8] 값임)
    - `cd ui-ink && bun run tsc --noEmit` → 에러 없음 (완전 green)
    - `cd ui-ink && bun run test` → 120건 이상 green (기존 + 신규 7건)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit && bun run test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tsc --noEmit green (에러 0)
    - vitest green (기존 120건 + 신규 7건 이상)
    - dispatch.ts에 agent_cancelled case, from_self 처리, event_id 추적 전부 있음
    - store/messages.ts에 loadSnapshot action + snapshotKey 있음
    - store/room.ts에 wsState/reconnectAttempt/lastEventId 있음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 수신 msg → dispatch() | parseServerMsg()가 이미 JSON 파싱. dispatch는 타입 분기만 수행 |
| state_snapshot.messages → loadSnapshot() | 서버가 전송한 배열. 각 항목을 안전하게 파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | loadSnapshot() rawMessages 악성 데이터 | mitigate | `filter`로 object만 허용, role 화이트리스트(['user','assistant','tool','system']), content를 string으로 강제 변환 |
| T-03-03-02 | Information Disclosure | event_id 추적 — 서버 내부 카운터 노출 | accept | event_id는 클라이언트가 resume_from 헤더로 재사용하는 공개 값. 보안 민감 정보 아님 |
| T-03-03-03 | Tampering | from_self=false 주입으로 입력자를 관전자로 오인 | accept | from_self는 서버가 per-subscriber로 주입 (PEXT-01). 클라이언트가 위조 불가 — 서버 identity 비교 결과 |
</threat_model>

<verification>
1. `cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit` → 에러 0
2. `cd /Users/johyeonchang/harness/ui-ink && bun run test` → vitest green
3. `grep -n 'AgentCancelledMsg' ui-ink/src/protocol.ts` → 2건 (정의 + union)
4. `grep -n 'snapshotKey' ui-ink/src/store/messages.ts` → 3건 이상
5. `grep -n 'setLastEventId' ui-ink/src/ws/dispatch.ts` → 1건
</verification>

<success_criteria>
- protocol.ts: AgentCancelledMsg 신규, from_self/old_content/subscribers:number 필드 업데이트
- store/room.ts: wsState/reconnectAttempt/lastEventId 필드 + 3개 setter
- store/messages.ts: snapshotKey + loadSnapshot action
- dispatch.ts: from_self 처리, loadSnapshot 연결, agent_cancelled case, event_id 추적, room 메시지 UI-SPEC 포맷
- tsc --noEmit green, vitest green
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-03-SUMMARY.md` 작성.
</output>
