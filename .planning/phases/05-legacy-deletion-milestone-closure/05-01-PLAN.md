---
phase: 05-legacy-deletion-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - cli/intent.py
  - cli/render.py
  - cli/claude.py
  - cli/setup.py
  - cli/callbacks.py
  - cli/slash.py
  - cli/tui.py
  - cli/app.py
  - cli/__init__.py
  - ui/index.js
  - ui/package.json
  - ui/package-lock.json
autonomous: true
requirements:
  - LEG-01
  - LEG-02
  - LEG-03

must_haves:
  truths:
    - "cli/ 디렉터리 내 REPL 관련 Python 모듈 전부 삭제"
    - "ui/index.js (구형 JS 클라이언트) 삭제"
    - "main.py 가 harness_server.py 래핑 thin shim 또는 완전 삭제"
    - "prompt_toolkit import 가 harness 소스 어디에도 남지 않음"
  artifacts:
    - path: "cli/"
      provides: "삭제 후 빈 상태 (또는 디렉터리 자체 제거)"
    - path: "ui/index.js"
      provides: "존재하지 않아야 함"
    - path: "main.py"
      provides: "thin shim (harness_server.py 위임) 또는 삭제"
  key_links:
    - from: "harness_server.py"
      to: "agent.py / harness_core/"
      via: "직접 import (main.py 경유 없음)"
      pattern: "from harness_core"
---

<objective>
Legacy Python UI 파일 전수 삭제 — cli/ 모듈 7종 + tui.py/app.py + ui/index.js + main.py REPL 경로를 제거한다.

Purpose: 사용자 명시 결정 "기존 코드 유지해보니까 gsd나 다른 에이전트들이 헷갈리는 경우가 상당히 많음" 이행. ui-ink 가 유일한 UI 임을 코드베이스 수준에서 확정한다.
Output: cli/ legacy 모듈 삭제 완료, ui/index.js 삭제 완료, main.py thin shim 또는 완전 삭제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- 삭제 대상 목록 (CLAUDE.md 명시) -->
<!-- cli/intent.py · cli/render.py · cli/claude.py · cli/setup.py · cli/callbacks.py · cli/slash.py · cli/tui.py · cli/app.py -->
<!-- ui/index.js -->
<!-- main.py REPL 경로 → harness_server.py 래핑 shim 으로 축소 또는 완전 삭제 -->

<!-- 유지 대상 (절대 손대지 않음) -->
<!-- agent.py · tools/ · session/ · evolution/ · harness_core/ · harness_server.py -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: cli/ legacy 모듈 삭제</name>
  <files>
    cli/intent.py
    cli/render.py
    cli/claude.py
    cli/setup.py
    cli/callbacks.py
    cli/slash.py
    cli/tui.py
    cli/app.py
    cli/__init__.py
  </files>
  <action>
    다음 파일들을 git rm 으로 삭제한다 (LEG-01, LEG-02):

    ```
    git rm cli/intent.py cli/render.py cli/claude.py cli/setup.py \
           cli/callbacks.py cli/slash.py cli/tui.py cli/app.py cli/__init__.py
    ```

    삭제 후 cli/ 디렉터리가 비어있으면 디렉터리 자체도 제거한다:
    ```
    rmdir cli/ 2>/dev/null || true
    ```

    주의: `__pycache__` 는 git 추적 대상 아니므로 자연히 무시됨.
    agent.py · harness_core/ · session/ · evolution/ · tools/ 는 절대 건드리지 않는다.
  </action>
  <verify>
    <automated>python -c "import cli" 2>&amp;1 | grep -q "ModuleNotFoundError\|No module named" &amp;&amp; echo "PASS: cli 임포트 실패 확인" || echo "FAIL: cli 모듈이 아직 존재함"</automated>
  </verify>
  <done>python -c "import cli" 가 ModuleNotFoundError 를 발생시킴. cli/ 내 .py 파일 0개.</done>
</task>

<task type="auto">
  <name>Task 2: ui/index.js 및 구형 JS 클라이언트 삭제</name>
  <files>
    ui/index.js
    ui/package.json
    ui/package-lock.json
  </files>
  <action>
    ui/index.js 와 ui/ 하위 구형 JS 파일을 삭제한다 (LEG-03):

    ```
    git rm ui/index.js
    git rm -f ui/package.json ui/package-lock.json 2>/dev/null || true
    ```

    ui/node_modules/ 는 git 추적 대상이 아니므로 rm -rf 로 별도 정리:
    ```
    rm -rf ui/node_modules/
    ```

    ui/ 디렉터리 자체가 빈 경우 제거:
    ```
    rmdir ui/ 2>/dev/null || true
    ```

    주의: ui-ink/ 디렉터리는 절대 손대지 않는다. ui/ 와 ui-ink/ 는 별개다.
  </action>
  <verify>
    <automated>test ! -f ui/index.js &amp;&amp; echo "PASS: ui/index.js 삭제 확인" || echo "FAIL: ui/index.js 여전히 존재"</automated>
  </verify>
  <done>ui/index.js 존재하지 않음. ui-ink/ 는 정상 존재.</done>
</task>

<task type="auto">
  <name>Task 3: main.py REPL 경로 정리 — thin shim 또는 완전 삭제</name>
  <files>
    main.py
  </files>
  <action>
    현재 main.py (553줄) 는 REPL 오케스트레이터 + cli.* 임포트 집합체다.
    cli/ 삭제 후 main.py 를 그대로 두면 ImportError 로 실행 불가 상태가 된다.

    다음 중 하나를 선택해 실행한다 (LEG-01 SC-3 준수):

    **옵션 A — main.py 완전 삭제 (권장, 가장 깔끔)**
    harness_server.py 가 독립 진입점이므로 main.py 가 불필요한 경우:
    ```
    git rm main.py
    ```

    **옵션 B — thin shim 으로 대체 (main.py 가 다른 곳에서 참조되는 경우)**
    아래 내용으로 main.py 를 덮어씌운다:
    ```python
    #!/usr/bin/env python3
    '''harness 서버 진입점 shim.

    이 파일은 하위호환 래퍼다. 실제 서버는 harness_server.py 에 있다.
    ui-ink (ui-ink/ 디렉터리) 가 클라이언트 역할을 담당한다.

    사용법:
      .venv/bin/python harness_server.py
    또는:
      cd ui-ink && bun start
    '''
    import subprocess
    import sys
    import os

    if __name__ == '__main__':
        script = os.path.join(os.path.dirname(__file__), 'harness_server.py')
        sys.exit(subprocess.call([sys.executable, script] + sys.argv[1:]))
    ```

    **판단 기준:** `grep -rn "from main import\|import main" .` 결과가 빈 경우 → 옵션 A.
    결과가 있으면 해당 참조를 먼저 제거한 뒤 옵션 A 적용.

    tests/ 에 main.py 의존 테스트가 있으면 해당 테스트도 함께 삭제하거나
    harness_core 기반으로 재배선한다 (pytest 260건 기준 유지 — 만약 main.py
    의존 테스트가 존재하면 삭제가 아닌 재배선을 선택한다).
  </action>
  <verify>
    <automated>grep -rn "prompt_toolkit" . --include="*.py" --exclude-dir=".venv" --exclude-dir="__pycache__" | grep -v "^Binary" | head -5; echo "EXIT:$?"</automated>
  </verify>
  <done>
    - grep -rn "prompt_toolkit" . (venv 제외) 결과 빈 줄 (0건).
    - main.py 가 삭제됐거나 harness_server.py 위임 shim 으로만 존재.
    - python -c "import cli" 가 ModuleNotFoundError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git rm → 파일시스템 | 삭제는 되돌릴 수 없음. git history 에 보존 |
| tests/ 의존성 | main.py 의존 테스트 삭제 시 pytest 회귀 카운트 변동 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | git rm | mitigate | 삭제 전 `git status` 로 staged 변경 없음 확인 후 실행. 실수 시 `git restore --staged` 가능 |
| T-05-02 | Denial | pytest 회귀 | mitigate | Task 3 에서 main.py 의존 테스트 재배선 우선, 삭제는 최후 수단 |
| T-05-03 | Information | ui/node_modules 잔재 | accept | git 추적 대상 아님, rm -rf 로 정리. 민감정보 없음 |
</threat_model>

<verification>
Wave 1 완료 기준:

```bash
# 1. cli 모듈 임포트 실패 확인
python -c "import cli" 2>&1 | grep -q "ModuleNotFoundError" && echo "PASS" || echo "FAIL"

# 2. ui/index.js 삭제 확인
test ! -f ui/index.js && echo "PASS" || echo "FAIL"

# 3. prompt_toolkit 잔재 0건 확인
count=$(grep -rn "prompt_toolkit" . --include="*.py" --exclude-dir=".venv" --exclude-dir="__pycache__" 2>/dev/null | wc -l)
[ "$count" -eq 0 ] && echo "PASS: prompt_toolkit 0건" || echo "FAIL: ${count}건 잔재"

# 4. 유지 대상 파일 존재 확인
for f in harness_server.py agent.py; do
  test -f "$f" && echo "PASS: $f 존재" || echo "FAIL: $f 사라짐"
done
```
</verification>

<success_criteria>
- `python -c "import cli"` → ModuleNotFoundError
- `test ! -f ui/index.js` → exit 0
- `grep -rn "prompt_toolkit" . --include="*.py" --exclude-dir=".venv"` → 빈 결과
- `harness_server.py`, `agent.py`, `harness_core/`, `tools/`, `session/`, `evolution/` 전부 존재
- `main.py` 가 없거나 thin shim (30줄 이하) 만 존재
</success_criteria>

<output>
완료 후 `.planning/phases/05-legacy-deletion-milestone-closure/05-01-SUMMARY.md` 작성.
포함 항목: 삭제된 파일 목록, main.py 처리 방식 (삭제 vs shim), prompt_toolkit grep 결과 0건 확인.
</output>
