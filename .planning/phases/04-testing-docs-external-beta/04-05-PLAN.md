---
phase: 04-testing-docs-external-beta
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
autonomous: false
requirements:
  - TST-05
  - TST-08

files_modified:
  - harness_server.py
  - .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md

must_haves:
  truths:
    - "CR-01 수정: harness_server.py:782,789에서 msg.get('result', msg.get('accept', False))로 confirm_write/bash 응답 필드 양쪽 허용"
    - "Python pytest 260건 green (CR-01 수정 후 회귀 없음)"
    - "PITFALLS 17항목 체크리스트 파일 작성 완료 (04-PITFALLS-CHECKLIST.md)"
    - "수동 체크리스트 beta 실행 전 준비 완료 확인 checkpoint"
  artifacts:
    - path: "harness_server.py"
      provides: "CR-01 수정 — confirm_write/bash 응답 accept 필드 처리"
      contains: "msg.get('accept'"
    - path: ".planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md"
      provides: "PITFALLS 17항목 수동 체크리스트"
      contains: "Alternate screen"
  key_links:
    - from: "harness_server.py:782"
      to: "ui-ink/src/store/confirm.ts:61"
      via: "confirm_write_response accept 필드"
      pattern: "msg.get\\('accept'"
    - from: "04-PITFALLS-CHECKLIST.md"
      to: ".planning/research/PITFALLS.md"
      via: "17항목 구조화"
      pattern: "P1.*Alternate"
---

<objective>
CR-01 버그를 서버에서 수정하고 PITFALLS 17항목 수동 체크리스트를 작성한다.

Purpose: confirm 기능 전체를 무력화하는 CR-01 버그(accept vs result 필드명 불일치)를 harness_server.py에서 수정. PITFALLS 17항목을 구조화된 체크리스트로 작성하여 beta 진행 전 수동 검증 가이드 완성.

Output: harness_server.py CR-01 수정 + 04-PITFALLS-CHECKLIST.md
</objective>

<execution_context>
@/Users/johyeonchang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johyeonchang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-testing-docs-external-beta/04-CONTEXT.md
@.planning/phases/03-remote-room-session-control/03-VERIFICATION.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- CR-01 수정 대상 정확한 위치 -->

harness_server.py:782 (03-VERIFICATION.md 확인):
```python
# 현재 (버그):
state._confirm_result = msg.get('result', False)

# 수정 후:
state._confirm_result = msg.get('result', msg.get('accept', False))
```

harness_server.py:789 (confirm_bash 동일 패턴):
```python
# 현재 (버그):
state._confirm_bash_result = msg.get('result', False)

# 수정 후:
state._confirm_bash_result = msg.get('result', msg.get('accept', False))
```

PITFALLS 17항목 (research/PITFALLS.md "Looks Done But Isn't" 섹션):
- P1: Alternate screen 미발생 (H)
- P2: resize 후 stale line 없음 (H)
- P3: Raw mode 복구 (H)
- P4: 큰 paste 500줄 (H)
- P5: macOS IME (H)
- P6: 스트리밍 500 토큰 연속 (H)
- P7: WS 재연결 (M)
- P8: 3 사용자 동시 재연결 (M)
- P9: non-TTY one-shot (H)
- P10: /undo 후 새 메시지 순서 (M)
- P11: child_process 흔적 없음 (M)
- P12: process.stdout.write / console.log 흔적 없음 (H)
- P13: 세션 200 메시지 타이핑 lag (M)
- P14: 토큰 인증 실패 시 친절한 에러 (M)
- P15: 원격 사용자 setup 10분 이내 (M)
- P16: 한국어 wrap 글자 깨짐 없음 (M)
- P17: Ctrl+C 2단계 (취소→종료) (M)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: harness_server.py CR-01 수정</name>
  <read_first>
    - harness_server.py (line 775-800 범위 — confirm_write_response/confirm_bash_response 처리 로직)
  </read_first>
  <files>harness_server.py</files>
  <action>
`harness_server.py` 의 두 줄을 수정한다.

**수정 위치 1 — line 782 (confirm_write_response):**
```python
# 수정 전:
state._confirm_result = msg.get('result', False)

# 수정 후 (CR-01 수정: accept 필드도 fallback으로 허용):
state._confirm_result = msg.get('result', msg.get('accept', False))
```

**수정 위치 2 — line 789 (confirm_bash_response):**
```python
# 수정 전:
state._confirm_bash_result = msg.get('result', False)

# 수정 후 (CR-01 수정: accept 필드도 fallback으로 허용):
state._confirm_bash_result = msg.get('result', msg.get('accept', False))
```

수정 전 실제 라인을 읽어 정확한 패턴을 파악한 후 Edit 툴로 최소 변경. `msg.get('result', False)` → `msg.get('result', msg.get('accept', False))` 두 곳만 변경.

파일을 읽어 실제 줄 번호가 782/789가 아닐 수 있으므로 `msg.get('result', False)` 패턴을 grep으로 위치 확인 후 수정:
```bash
grep -n "msg.get('result', False)" harness_server.py
```

두 곳 모두 수정 완료 후 Python pytest 실행으로 회귀 확인.

**주석 추가 (수정 줄 끝에):**
```python
state._confirm_result = msg.get('result', msg.get('accept', False))  # CR-01 수정: 클라는 accept 필드 전송
state._confirm_bash_result = msg.get('result', msg.get('accept', False))  # CR-01 수정
```
  </action>
  <verify>
    <automated>grep -n "msg.get('accept'" /Users/johyeonchang/harness/harness_server.py && .venv/bin/python -m pytest tests/ -x --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 수정 패턴 2줄 존재: `grep -c "msg.get('accept'" harness_server.py` → 2
    - 구버전 패턴 없음: `grep -n "msg.get('result', False)" harness_server.py` → 빈 결과
    - pytest 260건 green: `.venv/bin/python -m pytest -x --tb=short -q` → "260 passed" 또는 기존 통과 건수 유지
    - Python 문법 오류 없음: `.venv/bin/python -c "import harness_server"` → 에러 없음 (또는 import 에러 없는 수준 확인)
  </acceptance_criteria>
  <done>harness_server.py:782,789 CR-01 수정 완료 (accept 필드 fallback). pytest 260건 green. confirm_write/bash y 입력이 서버에서 올바르게 처리됨.</done>
</task>

<task type="auto">
  <name>Task 2: PITFALLS 17항목 수동 체크리스트 작성</name>
  <read_first>
    - .planning/research/PITFALLS.md (579행~600행 "Looks Done But Isn't" 섹션 전체 17항목)
  </read_first>
  <files>.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md</files>
  <action>
신규 파일 `.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md` 작성.

PITFALLS.md의 "Looks Done But Isn't" 섹션 17항목을 아래 구조화된 테이블로 변환:

```markdown
# Phase 4 PITFALLS 수동 체크리스트

**대상 Phase:** 4 (Testing + Docs + External Beta)
**작성 기준:** `.planning/research/PITFALLS.md` "Looks Done But Isn't" 섹션
**사용 방법:** beta 진행 전 모든 항목 [ ]을 [x]로 완료해야 함. 자동 검증 가능한 항목은 명령어 기재.

---

| # | 항목 | 심각도 | 확인 방법 | 자동/수동 | 상태 |
|---|------|--------|-----------|-----------|------|
| P01 | Alternate screen 미발생 — 세션 종료 후 Cmd+↑/Shift+PgUp으로 scrollback 접근 가능 | H | `bun run ci:no-escape` 통과 확인 | 자동 | [ ] |
| P02 | resize 후 stale line 없음 — 터미널 폭 200→40→200 반복, 한국어+emoji 포함 | H | 터미널 수동 resize + 화면 확인 | 수동 | [ ] |
| P03 | Raw mode 복구 — `kill -9 <pid>` 후 터미널 에코/라인편집 정상 | H | `kill -9 $(pgrep -f "bun start")` 후 `stty -a` 확인 | 수동 | [ ] |
| P04 | 큰 paste — 500줄 텍스트 붙여넣기 시 첫 줄에서 submit 안 됨 | H | 500줄 텍스트 실제 paste 테스트 | 수동 | [ ] |
| P05 | macOS IME — 한국어 조합 중 submit 안 됨, 완성 후만 submit | H | macOS 한국어 입력기로 "안녕하세요" 입력 테스트 | 수동 | [ ] |
| P06 | 스트리밍 500 토큰 — flicker 없음, CPU 50% 미만, scrollback spinner 찌꺼기 없음 | H | 장문 응답 요청 후 `top` 모니터링 | 수동 | [ ] |
| P07 | WS 재연결 — 서버 kill→restart 시 자동 재연결 + 이벤트 복구 | M | `kill $(pgrep -f harness_server)` 후 서버 재시작 | 수동 | [ ] |
| P08 | 3 사용자 동시 재연결 — 로컬+원격 2인 전원 자연 복구 | M | 3터미널 동시 접속 후 서버 재시작 | 수동 | [ ] |
| P09 | non-TTY one-shot — `echo 'test' \| bun start` crash 없이 one-shot 동작 | H | `echo 'test' \| cd ui-ink && bun start` 실행 | 자동 가능 | [ ] |
| P10 | /undo 후 새 메시지 순서 정확 — 덮어쓰기 없음 | M | `/undo` 후 새 메시지 입력 + 메시지 목록 순서 확인 | 수동 | [ ] |
| P11 | child_process 흔적 없음 | M | `grep -rn 'spawn\|exec' ui-ink/src/` → 빈 결과 | 자동 | [ ] |
| P12 | process.stdout.write / console.log 흔적 없음 | H | `bun run guard` 통과 | 자동 | [ ] |
| P13 | 세션 200 메시지 타이핑 lag 없음 — messages 리스트 매 타이핑 리렌더 안 함 | M | 200메시지 후 타이핑 응답성 체감 확인 | 수동 | [ ] |
| P14 | 토큰 인증 실패 시 친절한 에러 + 정상 종료 | M | 잘못된 HARNESS_TOKEN으로 `bun start` 실행 | 수동 | [ ] |
| P15 | fresh 환경 setup 10분 이내 — `git clone + bun install + bun start` | M | fresh VM 또는 신규 사용자 환경에서 시계 측정 | 수동 | [ ] |
| P16 | 한국어 wrap 글자 깨짐 없음 — 폭 40에서 wrap 시 | M | 터미널 폭 40으로 축소 후 한국어 메시지 확인 | 수동 | [ ] |
| P17 | Ctrl+C 2단계 — 첫 번째 = turn 취소, 두 번째 = exit | M | 에이전트 실행 중 Ctrl+C 1회 → 취소 확인 → 다시 Ctrl+C → exit 확인 | 수동 | [ ] |

---

## 자동 검증 가능 항목 일괄 실행

```bash
# P01: alternate screen 가드
cd ui-ink && bun run ci:no-escape

# P09: non-TTY one-shot
echo 'test' | cd /Users/johyeonchang/harness/ui-ink && HARNESS_URL=ws://127.0.0.1:7891 HARNESS_TOKEN=test bun start 2>&1 | head -5

# P11: child_process 흔적
grep -rn 'spawn\|exec' ui-ink/src/ && echo "발견됨!" || echo "OK — 없음"

# P12: guard
cd ui-ink && bun run guard
```

---

## 수동 검증 기록

beta 진행 후 각 항목 결과를 여기에 기록:

| # | 검증일 | 결과 | 비고 |
|---|--------|------|------|
| P01 | | | |
| P02 | | | |
| P03 | | | |
| P04 | | | |
| P05 | | | |
| P06 | | | |
| P07 | | | |
| P08 | | | |
| P09 | | | |
| P10 | | | |
| P11 | | | |
| P12 | | | |
| P13 | | | |
| P14 | | | |
| P15 | | | |
| P16 | | | |
| P17 | | | |

---

*Source: `.planning/research/PITFALLS.md` "Looks Done But Isn't" (17항목)*
*Phase 4 산출물: TST-05*
```
  </action>
  <verify>
    <automated>ls /Users/johyeonchang/harness/.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md && grep -c "^| P" /Users/johyeonchang/harness/.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `ls .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md`
    - 17항목 모두 포함: `grep -c "^| P" .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md` → 17 이상
    - Alternate screen(P01) 포함: `grep "Alternate screen\|P01" .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md`
    - 자동 검증 명령어 섹션 포함: `grep "bun run ci:no-escape\|bun run guard" .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md`
    - 수동 검증 기록 테이블 포함: `grep "검증일\|결과" .planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md`
  </acceptance_criteria>
  <done>04-PITFALLS-CHECKLIST.md 작성 완료. P01~P17 전 17항목, 심각도, 확인 방법, 자동/수동 구분, 결과 기록 테이블 포함.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. CR-01 수정: harness_server.py에서 confirm_write/bash 응답 시 accept 필드를 fallback으로 허용
    2. PITFALLS 17항목 수동 체크리스트 (04-PITFALLS-CHECKLIST.md)
    3. Python pytest 260건 green 확인
  </what-built>
  <how-to-verify>
    **CR-01 수정 확인:**
    ```bash
    # 수정 패턴 2곳 확인
    grep -n "msg.get('accept'" /Users/johyeonchang/harness/harness_server.py

    # pytest 260건 회귀 없음
    cd /Users/johyeonchang/harness && .venv/bin/python -m pytest -x --tb=short -q 2>&1 | tail -3
    ```

    **체크리스트 확인:**
    ```bash
    # 17항목 존재
    grep -c "^| P" /Users/johyeonchang/harness/.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md
    ```

    **자동 항목 일괄 검증:**
    ```bash
    cd /Users/johyeonchang/harness/ui-ink
    bun run ci:no-escape    # P01
    bun run guard           # P12
    grep -rn 'spawn\|exec' src/ || echo "OK"  # P11
    ```
  </how-to-verify>
  <resume-signal>위 검증 완료 후 "approved" 또는 발견된 문제를 기술해 주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness_server.py confirm 처리 | 클라이언트 confirm 응답 → 서버 파일 쓰기/bash 실행 권한 부여 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation of Privilege | harness_server.py confirm 수정 | mitigate | CR-01 수정은 `result` 우선 + `accept` fallback. 기존 서버가 result 필드를 보내는 클라이언트와도 호환 유지. `msg.get('result', msg.get('accept', False))` — accept=True로 조작된 메시지가 result가 없을 때만 수락되므로 기존 동작과 동등. |
| T-04-13 | Information Disclosure | 04-PITFALLS-CHECKLIST.md | accept | planning 디렉토리 내 문서 — 실제 토큰/IP 없음. beta 환경 정보도 포함하지 않음. |
| T-04-14 | Spoofing | confirm_write_response accept 필드 | accept | WS 연결 자체가 x-harness-token으로 인증됨. confirm 응답은 인증된 연결에서만 가능. |
</threat_model>

<verification>
## Phase 4 Plan 05 검증

```bash
# CR-01 수정 확인
grep -n "msg.get('accept'" /Users/johyeonchang/harness/harness_server.py

# 구버전 패턴 없음
grep -n "msg.get('result', False)" /Users/johyeonchang/harness/harness_server.py || echo "OK"

# pytest 260건 green
cd /Users/johyeonchang/harness && .venv/bin/python -m pytest -x --tb=short -q | tail -5

# 체크리스트 17항목
grep -c "^| P" /Users/johyeonchang/harness/.planning/phases/04-testing-docs-external-beta/04-PITFALLS-CHECKLIST.md
```
</verification>

<success_criteria>
- harness_server.py: confirm_write_response와 confirm_bash_response 두 곳 모두 `msg.get('result', msg.get('accept', False))` 패턴으로 수정
- Python pytest 260건 green (회귀 없음)
- 04-PITFALLS-CHECKLIST.md: P01~P17 전 17항목, 심각도, 확인 방법, 결과 기록 테이블
- checkpoint:human-verify 통과
</success_criteria>

<output>
완료 후 `.planning/phases/04-testing-docs-external-beta/04-05-SUMMARY.md` 작성.
</output>
