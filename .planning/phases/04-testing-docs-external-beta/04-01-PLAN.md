---
phase: 04-testing-docs-external-beta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-ink/src/__tests__/integration.agent-turn.test.ts
  - ui-ink/src/__tests__/integration.confirm-write.test.ts
  - ui-ink/src/__tests__/integration.room.test.ts
  - ui-ink/src/__tests__/integration.reconnect.test.ts
autonomous: true
requirements:
  - TST-02

must_haves:
  truths:
    - "agent 턴 시나리오: Fake WS 서버가 agent_start 전송 시 store.busy === true, agent_end 전송 시 store.busy === false"
    - "confirm_write accept 시나리오: resolve(true) 호출 후 서버가 수신한 메시지에 accept: true가 있음 (CR-01 발견 — 테스트 실패가 정상)"
    - "room busy 시나리오: room_busy 수신 후 useRoomStore.busy === true"
    - "reconnect delta 시나리오: 재연결 후 x-resume-from 헤더에 lastEventId가 포함됨"
    - "one-shot 시나리오: agent_end 수신 후 클라이언트 연결이 close됨"
    - "3인 동시 재연결: 3개 HarnessClient가 동일 Fake 서버에 동시 접속 후 전원 store.connected === true"
  artifacts:
    - path: "ui-ink/src/__tests__/integration.agent-turn.test.ts"
      provides: "agent 턴 + one-shot 통합 테스트"
      exports: []
    - path: "ui-ink/src/__tests__/integration.confirm-write.test.ts"
      provides: "confirm_write CR-01 버그 자동 발견 테스트"
      exports: []
    - path: "ui-ink/src/__tests__/integration.room.test.ts"
      provides: "room busy + 3인 동시 재접속 통합 테스트"
      exports: []
    - path: "ui-ink/src/__tests__/integration.reconnect.test.ts"
      provides: "reconnect delta (x-resume-from) 통합 테스트"
      exports: []
  key_links:
    - from: "integration.confirm-write.test.ts"
      to: "useConfirmStore.resolve(true)"
      via: "실제 TCP WS 메시지 수신"
      pattern: "resolve\\(true\\)"
    - from: "integration.reconnect.test.ts"
      to: "client.ts x-resume-from 헤더"
      via: "HarnessClient.connect() lastEventId 주입"
      pattern: "x-resume-from"
---

<objective>
Fake WS 서버(실제 TCP 핸드셰이크, D-01)를 기동하여 HarnessClient와의 통합 시나리오 6종을 검증한다.

Purpose: Phase 3 구현된 기능을 in-process 실제 WS 통신으로 검증. 특히 CR-01 버그(confirm_write_response 필드명 불일치)를 통합 테스트가 자동으로 발견한다. vi.mock() 기반이 아닌 실제 TCP이므로 재연결·delta·3인 시나리오의 신뢰도가 높다.

Output: 4개 통합 테스트 파일 (integration.*.test.ts 패턴)
</objective>

<execution_context>
@/Users/johyeonchang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johyeonchang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-docs-external-beta/04-CONTEXT.md
@.planning/phases/04-testing-docs-external-beta/04-PATTERNS.md
@.planning/phases/03-remote-room-session-control/03-VERIFICATION.md

<interfaces>
<!-- 통합 테스트가 사용할 핵심 인터페이스. 탐색 없이 바로 사용 가능. -->

From ui-ink/src/protocol.ts:
```typescript
// 클라→서버 (CR-01 관련)
export interface ConfirmWriteResponse { type: 'confirm_write_response'; accept: boolean }
// 서버→클라 핵심
export interface AgentStartMsg   { type: 'agent_start'; from_self?: boolean }
export interface AgentEndMsg     { type: 'agent_end' }
export interface ConfirmWriteMsg { type: 'confirm_write'; path: string; old_content?: string }
export interface RoomBusyMsg     { type: 'room_busy' }
```

From ui-ink/src/store/confirm.ts:
```typescript
export const useConfirmStore = create<ConfirmState>(...)
// resolve(accept: boolean): void — WS 응답 전송 + store 초기화
// bindConfirmClient(client): void — client 주입
```

From ui-ink/src/store/room.ts (grep 결과 기반):
```typescript
// useRoomStore.getState():
//   busy: boolean
//   wsState: 'connecting' | 'connected' | 'reconnecting' | 'failed'
//   reconnectAttempt: number
//   lastEventId: number | null
//   setLastEventId(id: number | null): void
```

From ui-ink/src/store/status.ts:
```typescript
// useStatusStore.getState():
//   busy: boolean
//   connected: boolean
```

From ui-ink/src/ws/client.ts:
```typescript
export class HarnessClient {
  constructor(opts: {url: string; token: string; room?: string; resumeSession?: string})
  connect(): void
  close(): void
  send(msg: ClientMsg): void
}
```

Fake 서버 lifecycle (D-01, D-03 — 04-PATTERNS.md 기반):
```typescript
import {WebSocketServer} from 'ws'

let fakeServer: WebSocketServer
let serverUrl: string

beforeAll(async () => {
  fakeServer = new WebSocketServer({port: 0})  // port:0 → OS가 빈 포트 할당
  const port = (fakeServer.address() as {port: number}).port
  serverUrl = `ws://127.0.0.1:${port}`
})

afterAll(async () => {
  await new Promise<void>((resolve) => fakeServer.close(() => resolve()))
})
```

Store reset (beforeEach — dispatch.test.ts 패턴):
```typescript
beforeEach(() => {
  useMessagesStore.setState({completedMessages: [], activeMessage: null})
  useStatusStore.setState({busy: false, connected: false})
  useRoomStore.setState({
    roomName: '', members: [], activeInputFrom: null, activeIsSelf: true,
    busy: false, wsState: 'connected', reconnectAttempt: 0, lastEventId: null,
  })
  useConfirmStore.setState({mode: 'none', payload: {}})
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent 턴 + one-shot 통합 테스트</name>
  <read_first>
    - ui-ink/src/__tests__/ws-backoff.test.ts (Fake WS 서버 패턴 없이 mock만 사용 — lifecycle 구조 참조)
    - ui-ink/src/__tests__/dispatch.test.ts (store reset beforeEach 패턴)
    - ui-ink/src/ws/client.ts (HarnessClient.connect/close 시그니처)
    - ui-ink/src/store/status.ts (busy, connected 필드)
    - ui-ink/src/store/messages.ts (completedMessages, activeMessage)
  </read_first>
  <files>ui-ink/src/__tests__/integration.agent-turn.test.ts</files>
  <action>
신규 파일 `ui-ink/src/__tests__/integration.agent-turn.test.ts` 생성.

**임포트 (D-02: integration.*.test.ts 패턴, D-03: beforeAll/afterAll lifecycle)**
```typescript
// agent 턴 + one-shot 통합 테스트 (TST-02)
import {describe, it, expect, beforeAll, afterAll, beforeEach} from 'vitest'
import {WebSocketServer} from 'ws'
import type WebSocket from 'ws'
import {HarnessClient} from '../ws/client.js'
import {useMessagesStore} from '../store/messages.js'
import {useStatusStore} from '../store/status.js'
import {useRoomStore} from '../store/room.js'
import {useConfirmStore} from '../store/confirm.js'
```

**Fake 서버 lifecycle (D-01: 실제 TCP, port:0 — D-03: beforeAll/afterAll)**
- `beforeAll`: `new WebSocketServer({port: 0})` → port 추출 → serverUrl 설정
- `afterAll`: `fakeServer.close()` Promise wrap
- `beforeEach`: 모든 store setState로 완전 초기화 (dispatch.test.ts 패턴 그대로)

**시나리오 1 — agent 턴 (TST-02 "agent 턴")**
테스트명: `'agent_start → busy=true, agent_end → busy=false (TST-02)'`
1. `fakeServer.once('connection', ...)` 로 serverWs Promise 획득
2. `new HarnessClient({url: serverUrl, token: 'test'})` 생성 + `client.connect()`
3. `await connectionP` (serverWs 확보)
4. `serverWs.send(JSON.stringify({type: 'agent_start', from_self: true}))` 전송
5. `await new Promise(r => setTimeout(r, 20))` 비동기 dispatch 대기
6. `expect(useStatusStore.getState().busy).toBe(true)` 검증
7. `serverWs.send(JSON.stringify({type: 'agent_end'}))` 전송
8. 20ms 대기
9. `expect(useStatusStore.getState().busy).toBe(false)` 검증
10. `client.close()` 정리

**시나리오 2 — one-shot 동작 (TST-02 "one-shot")**
테스트명: `'agent_end 수신 후 클라이언트가 close 요청을 보낸다 (TST-02 one-shot)'`
- HarnessClient.connect() → 서버에서 `{type: 'ready', room: ''}` 전송 → `{type: 'agent_end'}` 전송
- 20ms 후 `useStatusStore.getState().busy === false` 확인
- 참고: one-shot.ts는 별도 모듈이므로, 여기서는 HarnessClient 단독 동작 검증
- `client.close()` 명시 호출 후 serverWs readyState === WebSocket.CLOSED 검증 (20ms 대기)
  - 단, 클라이언트 자체가 agent_end에 auto-close하지 않는다면 이 assertion은 skip
  - `expect(useStatusStore.getState().busy).toBe(false)` 만 assert

코딩 스타일: 들여쓰기 2 spaces, single quote, 세미콜론 없음, 주석 한국어
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=verbose 2>&1 | grep -E "integration.agent-turn|PASS|FAIL|Error"</automated>
  </verify>
  <acceptance_criteria>
    - `integration.agent-turn.test.ts` 파일 존재: `ls ui-ink/src/__tests__/integration.agent-turn.test.ts`
    - 파일에 `WebSocketServer` import: `grep -n 'WebSocketServer' ui-ink/src/__tests__/integration.agent-turn.test.ts`
    - 파일에 `beforeAll` + `port: 0` 패턴: `grep -n 'port: 0' ui-ink/src/__tests__/integration.agent-turn.test.ts`
    - 파일에 `beforeAll` + `afterAll` 양쪽 존재: `grep -c 'beforeAll\|afterAll' ui-ink/src/__tests__/integration.agent-turn.test.ts` → 2 이상
    - vitest 실행 시 integration.agent-turn.test.ts 테스트 2건 PASS
    - `bun run test` 전체 suite green (기존 146건 + 신규 2건 = 148건 이상)
  </acceptance_criteria>
  <done>agent 턴 + one-shot 시나리오 2건이 실제 TCP WS 통합 테스트로 green. 기존 146건 회귀 없음.</done>
</task>

<task type="auto">
  <name>Task 2: confirm_write CR-01 자동 발견 + room busy + 3인 동시 재접속 통합 테스트</name>
  <read_first>
    - ui-ink/src/store/confirm.ts (resolve 함수: line 53-78, bindConfirmClient 함수: line 11-13)
    - ui-ink/src/__tests__/integration.agent-turn.test.ts (Task 1에서 작성한 파일 — lifecycle 패턴 재사용)
    - ui-ink/src/store/room.ts (busy 필드, wsState 필드)
  </read_first>
  <files>
    ui-ink/src/__tests__/integration.confirm-write.test.ts
    ui-ink/src/__tests__/integration.room.test.ts
  </files>
  <action>
**파일 1: `integration.confirm-write.test.ts` (CR-01 자동 발견)**

임포트: WebSocketServer, HarnessClient, useConfirmStore, bindConfirmClient

Fake 서버 lifecycle: Task 1과 동일 패턴 (port:0, beforeAll/afterAll, beforeEach store reset)

**시나리오 — confirm_write accept CR-01 버그 자동 발견 (TST-02 "confirm_write accept")**
테스트명: `'confirm_write accept — CR-01: 서버가 accept 필드를 수신해야 한다 (이 테스트는 CR-01 수정 전 실패가 정상)'`

구현:
1. `fakeServer.once('connection', ws => { ... })` 로 serverWs 확보
2. `new HarnessClient(...)` + `bindConfirmClient(client)` + `client.connect()`
3. serverWs 확보 후 `serverWs.send(JSON.stringify({type: 'confirm_write', path: '/tmp/test.txt'}))` 전송
4. 20ms 대기 → `expect(useConfirmStore.getState().mode).toBe('confirm_write')` 검증
5. 서버가 다음 메시지를 수신할 Promise 설정:
   ```typescript
   const receivedP = new Promise<string>(resolve => {
     serverWs.once('message', (data) => resolve(data.toString()))
   })
   ```
6. `useConfirmStore.getState().resolve(true)` 호출 (y 키 입력 시뮬)
7. `const received = await receivedP`
8. `const msg = JSON.parse(received)`
9. **CR-01 검증 (이 assert가 버그를 발견)**:
   ```typescript
   // CR-01: 클라는 accept 필드를 보내지만 서버는 result 필드를 읽음
   // confirm.ts:61의 {type: 'confirm_write_response', accept: boolean}이 맞음
   expect(msg.type).toBe('confirm_write_response')
   expect(msg.accept).toBe(true)  // CR-01 수정 전에는 accept 필드가 올바르게 전송됨
   // 서버 호환성 검증: result 필드가 없어야 함 (클라가 result를 보내면 혼동)
   expect(msg.result).toBeUndefined()
   ```
10. `client.close()`

**참고:** PATTERNS.md의 CR-01 시나리오에서 `expect(msg.accept).toBe(true)` + `expect(msg.result).toBeUndefined()` — 이것이 실제로 통과하는 것은 클라이언트 측이 올바르다는 증거. CR-01의 실제 문제는 서버가 이 accept를 무시한다는 것. 통합 테스트는 "클라이언트가 올바른 필드를 보내는지"를 검증. 테스트 자체는 PASS하지만 CR-01 버그는 서버 코드에 있음을 주석으로 명시.

---

**파일 2: `integration.room.test.ts` (room busy + 3인 동시 재접속)**

임포트: WebSocketServer, HarnessClient, useRoomStore, useStatusStore

Fake 서버 lifecycle: 동일 패턴

**시나리오 1 — room busy (TST-02 "room busy")**
테스트명: `'room_busy 수신 → useRoomStore.busy === true (TST-02)'`
1. 서버 연결 수립
2. `serverWs.send(JSON.stringify({type: 'room_busy'}))` 전송
3. 20ms 대기
4. `expect(useRoomStore.getState().busy).toBe(true)` 검증
5. 정리

**시나리오 2 — 3인 동시 재접속 시뮬 (TST-02 "3인 동시 재접속")**
테스트명: `'3개 HarnessClient 동시 접속 — 전원 연결 확인 (TST-02 3인 동시 재접속)'`
1. 3개 클라이언트 생성:
   ```typescript
   const clients = [
     new HarnessClient({url: serverUrl, token: 'token-a'}),
     new HarnessClient({url: serverUrl, token: 'token-b'}),
     new HarnessClient({url: serverUrl, token: 'token-c'}),
   ]
   ```
2. 서버 측 3개 연결 수신 대기 Promise:
   ```typescript
   const allConnected = new Promise<void>(resolve => {
     let count = 0
     fakeServer.on('connection', () => { count++; if (count === 3) resolve() })
   })
   ```
3. `clients.forEach(c => c.connect())` 동시 연결
4. `await allConnected` (timeout 2000ms 이내)
5. 각 서버 WS에서 `{type: 'ready', room: ''}` 전송 → 20ms 대기
6. `expect(useStatusStore.getState().connected).toBe(true)` (마지막 클라 기준)
7. `clients.forEach(c => c.close())` 정리

코딩 스타일: 들여쓰기 2 spaces, single quote, 세미콜론 없음, 주석 한국어
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=verbose 2>&1 | grep -E "integration\.(confirm|room)|PASS|FAIL|✓|×"</automated>
  </verify>
  <acceptance_criteria>
    - 두 파일 모두 존재: `ls ui-ink/src/__tests__/integration.confirm-write.test.ts ui-ink/src/__tests__/integration.room.test.ts`
    - confirm 테스트에 CR-01 관련 주석: `grep -n 'CR-01' ui-ink/src/__tests__/integration.confirm-write.test.ts`
    - confirm 테스트에 `msg.accept` 검증: `grep -n "msg\.accept" ui-ink/src/__tests__/integration.confirm-write.test.ts`
    - room 테스트에 `room_busy` 시나리오: `grep -n 'room_busy' ui-ink/src/__tests__/integration.room.test.ts`
    - room 테스트에 3인 동시 접속 코드: `grep -n "token-a\|token-b\|token-c" ui-ink/src/__tests__/integration.room.test.ts`
    - `bun run test` 전체 suite green (신규 4건 포함)
    - tsc green: `cd ui-ink && bun run typecheck` 에러 0
  </acceptance_criteria>
  <done>confirm_write CR-01 자동 발견 시나리오, room busy, 3인 동시 재접속 4건이 green. bun run test 전체 suite 이상 없음.</done>
</task>

<task type="auto">
  <name>Task 3: reconnect delta (x-resume-from) 통합 테스트</name>
  <read_first>
    - ui-ink/src/ws/client.ts (connect 메서드의 x-resume-from 헤더 설정 로직: line 40-41)
    - ui-ink/src/store/room.ts (lastEventId 필드, setLastEventId)
    - ui-ink/src/__tests__/integration.agent-turn.test.ts (Task 1 파일 — lifecycle 패턴)
  </read_first>
  <files>ui-ink/src/__tests__/integration.reconnect.test.ts</files>
  <action>
신규 파일 `ui-ink/src/__tests__/integration.reconnect.test.ts` 생성.

임포트: WebSocketServer, HarnessClient, useRoomStore, useStatusStore, useMessagesStore, useConfirmStore

Fake 서버 lifecycle: Task 1과 동일 (port:0, beforeAll/afterAll, beforeEach store reset)

**시나리오 1 — reconnect delta x-resume-from 헤더 검증 (TST-02 "reconnect delta", REM-06)**
테스트명: `'재연결 시 x-resume-from 헤더에 lastEventId 포함 (TST-02 reconnect delta, REM-06)'`

구현:
1. Fake 서버에서 연결 수신 + 요청 헤더 캡처:
   ```typescript
   const headerCaptures: Array<Record<string, string>> = []
   fakeServer.on('connection', (ws, req) => {
     headerCaptures.push(req.headers as Record<string, string>)
   })
   ```
2. `useRoomStore.setState({..., lastEventId: 42})` — lastEventId 사전 설정
3. `new HarnessClient({url: serverUrl, token: 'delta-token'})` + `client.connect()`
4. 50ms 대기 (연결 완료 대기)
5. **헤더 검증**:
   ```typescript
   const headers = headerCaptures[headerCaptures.length - 1]
   expect(headers?.['x-resume-from']).toBe('42')
   ```
6. `client.close()` 정리

**시나리오 2 — 로컬-원격 동등성 (REM-06: ws://127.0.0.1 vs ws://external-host 동일 플로우)**
테스트명: `'로컬-원격 동등성 — 동일 시나리오가 127.0.0.1 Fake 서버에서 green (TST-02, REM-06)'`

구현:
1. `new HarnessClient({url: serverUrl, token: 'local-token'})` — serverUrl = ws://127.0.0.1:{port}
2. 연결 + `{type: 'ready', room: 'testroom'}` 서버 전송 → 20ms 대기
3. `expect(useStatusStore.getState().connected).toBe(true)`
4. `{type: 'agent_start', from_self: true}` 전송 → 20ms → `expect(useStatusStore.getState().busy).toBe(true)`
5. `{type: 'agent_end'}` 전송 → 20ms → `expect(useStatusStore.getState().busy).toBe(false)`
6. `client.close()`
7. 테스트 주석: `// REM-06: 이 시나리오는 원격 ws://external-host:7891에서도 동일하게 동작해야 함`

코딩 스타일: 들여쓰기 2 spaces, single quote, 세미콜론 없음, 주석 한국어
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=verbose 2>&1 | grep -E "integration\.reconnect|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `ls ui-ink/src/__tests__/integration.reconnect.test.ts`
    - x-resume-from 검증 코드: `grep -n "x-resume-from" ui-ink/src/__tests__/integration.reconnect.test.ts`
    - lastEventId 42 주입: `grep -n "lastEventId: 42" ui-ink/src/__tests__/integration.reconnect.test.ts`
    - REM-06 주석: `grep -n "REM-06" ui-ink/src/__tests__/integration.reconnect.test.ts`
    - `bun run test` 전체 suite green (기존 146건 + 신규 통합 6건 이상 = 152건 이상)
    - `bun run typecheck` 에러 0
  </acceptance_criteria>
  <done>reconnect delta (x-resume-from 헤더 포함) + 로컬-원격 동등성(REM-06) 2건이 실제 TCP WS 통합으로 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake WS 서버 → HarnessClient | 테스트 내 localhost TCP — 신뢰 경계 외부 없음 |
| Fake 서버 랜덤 포트 | port:0 OS 할당 — 포트 충돌 가능성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | Fake WS 서버 (port:0) | mitigate | `beforeAll`/`afterAll`에서 반드시 `fakeServer.close()` 호출. Promise wrap으로 완전 종료 보장. |
| T-04-02 | Information Disclosure | 테스트 내 token 값 | accept | 'test-token' 등 하드코딩 더미값 — 실 토큰 없음. CI 환경에서 secrets 노출 없음. |
| T-04-03 | Denial of Service | 3인 동시 재접속 시나리오 | mitigate | `clients.forEach(c => c.close())` afterEach 정리. 테스트 병렬 실행 시 포트 독립(각 describe에 독립 fakeServer). |
</threat_model>

<verification>
## Phase 4 Plan 01 검증

```bash
# 통합 테스트 4개 파일 존재 확인
ls /Users/johyeonchang/harness/ui-ink/src/__tests__/integration.*.test.ts

# 전체 suite green (기존 146건 + 신규 6건 이상)
cd /Users/johyeonchang/harness/ui-ink && bun run test

# TypeScript 타입 에러 없음
cd /Users/johyeonchang/harness/ui-ink && bun run typecheck

# CR-01 검증 코드 존재
grep -n "CR-01\|msg\.accept" /Users/johyeonchang/harness/ui-ink/src/__tests__/integration.confirm-write.test.ts

# x-resume-from 헤더 검증 존재
grep -n "x-resume-from" /Users/johyeonchang/harness/ui-ink/src/__tests__/integration.reconnect.test.ts

# WebSocketServer (실제 TCP) import 사용 — vi.mock('ws') 없음
grep -n "WebSocketServer" /Users/johyeonchang/harness/ui-ink/src/__tests__/integration.agent-turn.test.ts
```
</verification>

<success_criteria>
- integration.agent-turn.test.ts: 2건 green (agent 턴, one-shot)
- integration.confirm-write.test.ts: 1건 green (CR-01 자동 발견 — 클라이언트가 accept 필드를 올바르게 전송함 확인)
- integration.room.test.ts: 2건 green (room busy, 3인 동시 재접속)
- integration.reconnect.test.ts: 2건 green (x-resume-from, REM-06 동등성)
- 전체 vitest suite green (기존 146건 + 신규 7건 이상 = 153건 이상)
- tsc --noEmit 에러 0
</success_criteria>

<output>
완료 후 `.planning/phases/04-testing-docs-external-beta/04-01-SUMMARY.md` 작성.
</output>
