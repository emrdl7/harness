---
phase: 05-legacy-deletion-milestone-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: true
requirements:
  - LEG-04
  - LEG-05

must_haves:
  truths:
    - "Python pytest 전 케이스 green"
    - "ui-ink vitest 전 케이스 green"
    - "prompt_toolkit grep 0건"
    - "import cli 의도적 실패"
    - "harness_server.py + ui-ink 가 유일 실행 경로"
  artifacts:
    - path: "ui-ink/"
      provides: "vitest green 상태"
    - path: "harness_server.py"
      provides: "단독 서버 진입점"
  key_links:
    - from: "pytest"
      to: "tests/"
      via: ".venv/bin/pytest"
      pattern: "pytest tests/"
    - from: "vitest"
      to: "ui-ink/src/"
      via: "bun run test"
      pattern: "bun.*test"
---

<objective>
Legacy 삭제 후 최종 회귀 검증 — Python pytest + ui-ink vitest + 환경 위생 grep 을 전부 통과시킨다.

Purpose: Phase 5 의 핵심 품질 게이트. 삭제로 인해 깨진 테스트가 없는지, prompt_toolkit 잔재가 없는지 확인한다.
Output: pytest green · vitest green · 환경 위생 grep 결과 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-legacy-deletion-milestone-closure/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python pytest 회귀 라운드</name>
  <files></files>
  <action>
    05-01 에서 cli/ 와 main.py REPL 경로를 삭제했으므로 pytest 가 깨졌을 수 있다.
    아래 순서로 실행한다 (LEG-04):

    1. pytest 실행:
       ```
       .venv/bin/pytest tests/ -x --tb=short 2>&1 | tail -30
       ```

    2. 실패한 테스트가 있으면 원인을 파악한다:
       - `test_main_intent.py` 또는 `test_handle_slash.py` 가 `from main import ...` 로
         깨졌다면: 해당 테스트 파일에서 `main` 심볼을 `harness_core` 로 재배선한다.
         (main.py 의 intent/slash 로직은 harness_core 로 이관됐으므로 re-export 경로 수정)
       - `from cli import ...` 로 깨진 테스트: 해당 import 를 harness_core 또는
         직접 모듈 경로로 교체한다.
       - 다른 이유로 깨진 테스트: 삭제와 무관한 선행 버그라면 skip 마크 없이 수정한다.

    3. 수정 후 재실행해 green 확인:
       ```
       .venv/bin/pytest tests/ --tb=short 2>&1 | tail -5
       ```

    목표 케이스 수: ROADMAP Phase 5 SC-4 기준 "Python pytest 199건" — 단, STATE.md 에
    Phase 4 완료 시 260건으로 기록됨. 최신 기준(260건 또는 현재 통과 건수)을 기준으로 삼는다.
    main.py 의존 테스트 삭제로 건수가 소폭 줄어도 무방하나 0 건 실패가 목표다.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ --tb=short -q 2>&1 | tail -3</automated>
  </verify>
  <done>pytest 실패 0건. "X passed" 메시지 확인.</done>
</task>

<task type="auto">
  <name>Task 2: ui-ink vitest 회귀 라운드</name>
  <files></files>
  <action>
    ui-ink 테스트를 실행해 Phase 4 완료 시점 green 상태를 유지하는지 확인한다 (LEG-04):

    ```
    cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=verbose 2>&1 | tail -20
    ```

    실패 시:
    - 테스트 파일이 삭제된 legacy 파일을 import 하는지 확인한다.
    - ui-ink 는 cli/ 나 main.py 를 직접 참조하지 않으므로 실패 가능성은 낮다.
    - 만약 실패한다면 테스트 자체의 버그를 수정한다 (Phase 4 에서 확인된 163건 기준).

    빌드 타입 확인도 함께 수행:
    ```
    cd /Users/johyeonchang/harness/ui-ink && bun run tsc --noEmit 2>&1 | tail -5
    ```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>vitest 실패 0건. tsc --noEmit error 0건.</done>
</task>

<task type="auto">
  <name>Task 3: 환경 위생 grep — prompt_toolkit · import cli · 단독 실행 경로</name>
  <files></files>
  <action>
    삭제 완료 상태를 다음 grep 으로 공식 확인하고 결과를 SUMMARY 에 기록한다 (LEG-05):

    ```bash
    # 1. prompt_toolkit 잔재 확인 (0건 이어야 함)
    echo "=== prompt_toolkit grep ==="
    grep -rn "prompt_toolkit" . \
      --include="*.py" \
      --exclude-dir=".venv" \
      --exclude-dir="__pycache__" \
      --exclude-dir=".planning" \
      2>/dev/null | head -10
    echo "EXIT:$?"

    # 2. import cli 의도적 실패 확인
    echo "=== import cli ==="
    python -c "import cli" 2>&1

    # 3. ui/index.js 존재 확인
    echo "=== ui/index.js ==="
    test ! -f ui/index.js && echo "DELETED" || echo "STILL EXISTS"

    # 4. 유일한 실행 경로 확인
    echo "=== server entry ==="
    test -f harness_server.py && echo "harness_server.py: OK" || echo "MISSING"
    test -d ui-ink && echo "ui-ink/: OK" || echo "MISSING"

    # 5. alternate screen / mouse tracking escape 잔재 확인 (FND-11 유지)
    echo "=== escape guard ==="
    grep -rn $'\x1b\[?1049\|\x1b\[?1000' ui-ink/src/ 2>/dev/null | head -5
    echo "EXIT:$?"
    ```

    모든 결과를 SUMMARY 에 그대로 붙여넣는다.
  </action>
  <verify>
    <automated>grep -rn "prompt_toolkit" . --include="*.py" --exclude-dir=".venv" --exclude-dir="__pycache__" 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - prompt_toolkit grep 결과 0건.
    - python -c "import cli" → ModuleNotFoundError.
    - ui/index.js 존재하지 않음.
    - harness_server.py 존재, ui-ink/ 존재.
    - escape guard grep 0건.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → tests/ | 삭제로 인해 테스트 import 경로가 깨질 수 있음 |
| vitest → ui-ink/src | Phase 4 이후 변경 없으므로 실패 가능성 낮음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial | pytest import 오류 | mitigate | Task 1 에서 from main / from cli import 를 harness_core 로 재배선 |
| T-05-05 | Information | 환경 위생 grep 누락 | mitigate | Task 3 에서 5개 grep 전부 실행 후 결과를 SUMMARY 에 기록 |
</threat_model>

<verification>
```bash
# 종합 검증
.venv/bin/pytest tests/ -q 2>&1 | tail -2
cd /Users/johyeonchang/harness/ui-ink && bun run test --reporter=dot 2>&1 | tail -2
grep -rn "prompt_toolkit" /Users/johyeonchang/harness --include="*.py" --exclude-dir=".venv" | wc -l
```
세 명령 모두: passed / 0 failures / 0 이어야 한다.
</verification>

<success_criteria>
- pytest: X passed, 0 failed (X ≥ 199)
- vitest: 0 failed
- prompt_toolkit grep 0건
- python -c "import cli" → ModuleNotFoundError
- harness_server.py + ui-ink/ 존재 확인
</success_criteria>

<output>
완료 후 `.planning/phases/05-legacy-deletion-milestone-closure/05-02-SUMMARY.md` 작성.
포함 항목: pytest 통과 건수, vitest 통과 건수, 환경 위생 grep 5종 결과 전문.
</output>
