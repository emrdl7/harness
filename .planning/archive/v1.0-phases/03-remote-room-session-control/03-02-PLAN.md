---
phase: 03-remote-room-session-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - harness_server.py
  - tests/test_harness_server.py
autonomous: true
requirements:
  - PEXT-04
  - PEXT-05
  - SES-02

must_haves:
  truths:
    - "WS 연결 시 x-resume-from 헤더가 있으면 ring buffer에서 해당 event_id 이후 이벤트를 순서대로 재송신한다"
    - "클라이언트가 cancel 메시지를 보내면 active_input_from인 경우에만 현재 input task가 취소된다"
    - "Room._cancel_requested 플래그가 on_token 콜백에서 체크되어 executor 스레드가 조기 종료된다"
    - "CancelledError는 _handle_input() finally에서 busy=False + active_input_from=None으로 정리된다"
    - "agent_cancelled 이벤트가 broadcast된다"
    - "WS 연결 시 x-resume-session 헤더가 있으면 _run_session()이 session/store.py load()로 세션을 로드한다"
    - "room_member_joined broadcast에 user=token_hash[:8] 필드가 포함된다"
  artifacts:
    - path: "harness_server.py"
      provides: "PEXT-04 resume_from 파싱 + PEXT-05 cancel 처리 + SES-02 resume-session + B4 token_hash 헬퍼"
      contains: "resume_from"
    - path: "tests/test_harness_server.py"
      provides: "PEXT-04/05/SES-02/B4 테스트"
      contains: "TestDeltaReplay"
  key_links:
    - from: "harness_server.py:_run_session()"
      to: "Room.event_buffer"
      via: "x-resume-from 헤더 → ring buffer delta 재송신"
      pattern: "resume_from"
    - from: "harness_server.py:_dispatch_loop()"
      to: "room.input_tasks"
      via: "cancel 케이스 → task.cancel() + _cancel_requested 플래그"
      pattern: "_cancel_requested"
    - from: "harness_server.py:_run_session()"
      to: "session/store.py:load()"
      via: "x-resume-session 헤더 → sess.load(resume_session_id)"
      pattern: "x-resume-session"
    - from: "harness_server.py:_on_member_joined()"
      to: "broadcast room_member_joined"
      via: "_token_hash() 헬퍼 → user 필드"
      pattern: "token_hash"
---

<objective>
Phase 3 서버 WS 프로토콜 확장 2차 — _run_session()에 x-resume-from 헤더 파싱과 ring buffer delta 재송신 구현 (PEXT-04), _dispatch_loop()에 cancel 메시지 처리 + Room._cancel_requested 플래그로 executor 스레드 안전 중단 (PEXT-05, B2 수정), x-resume-session 헤더 파싱 + session/store.py load() 호출 (SES-02, B3 수정), room_member_joined broadcast에 token_hash 기반 user 필드 추가 (B4 수정).

Purpose: WSR-03 delta replay (Plan 03-05에서 클라이언트 측 구현), WSR-04 Ctrl+C 취소 경로(Plan 03-06에서 App.tsx 연결), SES-02 세션 resume, REM-02 Presence 식별자의 서버 기반을 완성한다.
Output: harness_server.py에 PEXT-04~05 + SES-02 + B3/B4 구현 완료 + pytest 테스트 추가 (기존 green 유지)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-remote-room-session-control/03-RESEARCH.md
@.planning/phases/03-remote-room-session-control/03-PATTERNS.md
@.planning/BB-2-DESIGN.md

<interfaces>
<!-- _run_session() 현재 진입부 (lines 711-731 근처): -->
```python
async def _run_session(ws):
    room_header = (ws.request.headers.get('x-harness-room', '') or '').strip()
    room_name = room_header if room_header else f'_solo_{uuid.uuid4().hex}'
    is_shared = bool(room_header)
    room = _get_or_create_room(room_name)
    room.subscribers.add(ws)
    state = room.state
    try:
        await send_state(ws, state)
        await send(ws, type='ready', room=room_name)
        await send(ws, type='room_joined',
                   room=room_name,
                   shared=is_shared,
                   subscribers=len(room.subscribers),
                   busy=room.busy)
        # state_snapshot 전송 ...
```

<!-- _dispatch_loop() 현재 구조 (lines 654-709 근처): -->
```python
async def _dispatch_loop(ws, room: 'Room', queue: asyncio.Queue):
    state = room.state
    while True:
        msg = await queue.get()
        if msg is None:
            return
        t = msg.get('type')

        if t == 'input':
            # ... busy 체크 + _spawn_input_task()
        elif t == 'confirm_write_response':
            if ws is not room.active_input_from:  # DQ2 가드
                continue
            # ...
        elif t == 'ping':
            await send(ws, type='pong')
```

<!-- _handle_input() 현재 구조 (lines 617-635 근처): -->
```python
async def _handle_input(ws, room: 'Room', text: str):
    try:
        if text.startswith('/'):
            await handle_slash(ws, room, text)
        ...
    except Exception as e:
        await broadcast(room, type='error', text=f'입력 처리 오류: {e}')
    finally:
        room.busy = False
        room.active_input_from = None
```

<!-- room_joined 현재 서버 전송 (lines 727-731): -->
```python
await send(ws, type='room_joined',
           room=room_name,
           shared=is_shared,
           subscribers=len(room.subscribers),
           busy=room.busy)
# ← members 필드 없음 — protocol.ts 불일치 (RESEARCH.md Pitfall H)
```

<!-- session/store.py load() API: -->
```python
# session/store.py (VERIFIED)
def load(filename: str) -> dict:
    path = os.path.join(SESSION_DIR, filename)
    with open(path, encoding='utf-8') as f:
        return json.load(f)
# 반환: {'working_dir': str, 'messages': list}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PEXT-04 + SES-02 + B3 + B4 — _run_session() 헤더 파싱 확장 + token_hash 헬퍼</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (_run_session 함수 전체 — room_header 파싱 위치, room_joined send() 호출 위치, state_snapshot 전송 위치, room_member_joined broadcast 호출 위치 확인)
    - harness_server.py (import 블록 — hashlib 이미 import 여부 확인)
    - harness_server.py (Room dataclass — _cancel_requested 필드 없음 확인)
    - tests/test_harness_server.py (TestDeltaReplay 클래스 미존재 확인, 기존 _run_session 관련 테스트 확인)
  </read_first>
  <behavior>
    - Test 1: x-resume-from 헤더가 없으면 resume_from=None으로 처리된다 (delta 재송신 없음)
    - Test 2: x-resume-from: "5" 헤더 → event_id > 5인 이벤트만 재송신된다
    - Test 3: x-resume-from: "0" 헤더 → ring buffer의 모든 이벤트가 재송신된다
    - Test 4: x-resume-from: "abc" 같은 비정수값 → resume_from=None (무시, 재송신 없음)
    - Test 5: x-resume-from: "999999999999" (2^31 초과) → resume_from=None (방어 처리)
    - Test 6: room_joined 이벤트에 members 필드가 추가된다 (Pitfall H 수정)
    - Test 7: x-resume-session 헤더가 있고 첫 번째 접속자이면 sess.load()가 호출된다 (SES-02)
    - Test 8: x-resume-session 헤더가 있고 session 파일이 없으면 error 이벤트가 전송된다
    - Test 9: _token_hash("abc123")이 SHA-256 앞 8자 hex string을 반환한다
    - Test 10: room_member_joined broadcast에 user=_token_hash(token) 필드가 포함된다 (B4)
  </behavior>
  <action>
1. harness_server.py 파일 상단 import 블록에 hashlib 추가 (없는 경우만):
```python
import hashlib
```
   및 session 모듈 import (기존 import 확인 후):
```python
import session.store as sess
```

2. _token_hash() 헬퍼 함수 추가 — broadcast 함수 근처 (B4):
```python
def _token_hash(token: str) -> str:
    '''토큰의 SHA-256 앞 8자 hex — Presence 식별자 (개인정보 노출 없음).'''
    return hashlib.sha256(token.encode()).hexdigest()[:8]
```

3. harness_server.py의 Room dataclass에 _cancel_requested 필드 추가 (B2 준비):
```python
@dataclass
class Room:
    name: str
    state: Session
    subscribers: set = field(default_factory=set)
    busy: bool = False
    active_input_from: object = None
    input_tasks: set = field(default_factory=set)
    # PEXT-03 추가 (Wave 1 Plan 01에서 추가됨)
    event_counter: int = field(default=0)
    event_buffer: deque = field(default_factory=lambda: deque(maxlen=10000))
    # PEXT-05 (B2): executor 스레드 조기 종료 플래그
    _cancel_requested: bool = field(default=False)
```

4. harness_server.py의 _run_session() 함수에서 room_header 파싱 블록 직후에 추가:
```python
# PEXT-04: resume_from 헤더 파싱 (재연결 시 delta replay 요청)
resume_from_raw = (ws.request.headers.get('x-resume-from', '') or '').strip()
if resume_from_raw.isdigit() and int(resume_from_raw) < 2**31:
    resume_from: int | None = int(resume_from_raw)
else:
    resume_from = None

# SES-02 (B3): resume_session 헤더 파싱
resume_session_id = (ws.request.headers.get('x-resume-session', '') or '').strip()
```

5. room_joined 전송 직후, state_snapshot 전송 이전에 세 블록 추가:
```python
# SES-02 (B3): 세션 resume — 첫 번째 접속자만 세션 로드
if resume_session_id and not (room.subscribers - {ws}):
    try:
        data = sess.load(resume_session_id)
        room.state.messages = data.get('messages', [])
        if 'working_dir' in data:
            room.state.working_dir = data['working_dir']
    except FileNotFoundError:
        await send(ws, type='error', text=f'세션 없음: {resume_session_id}')
    except Exception as e:
        await send(ws, type='error', text=f'세션 로드 오류: {e}')

# PEXT-04: delta 재송신 — ring buffer에서 resume_from 이후 이벤트 순서대로 전송
if resume_from is not None:
    for (eid, _ts, payload_dict) in list(room.event_buffer):
        if eid > resume_from:
            await send(ws, **payload_dict)
```

6. room_joined send() 호출에 members 필드 추가 (Pitfall H 수정):
```python
await send(ws, type='room_joined',
           room=room_name,
           shared=is_shared,
           subscribers=len(room.subscribers),
           busy=room.busy,
           members=[])  # 현재는 빈 리스트 — room_member_joined 이벤트로 점진 추가
```
   주의: HARNESS_TOKENS 환경변수에서 토큰을 직접 멤버 식별자로 사용하지 않음 (개인정보 노출 금지).

7. room_member_joined broadcast 호출 위치를 찾아 user 필드 추가 (B4):
```python
# room_member_joined broadcast — user=token_hash[:8] 추가
# (기존에 subscribers만 전송하는 broadcast 호출을 찾아 수정)
await broadcast(room, type='room_member_joined',
                subscribers=len(room.subscribers),
                user=_token_hash(token))  # token은 ws.harness_token 또는 헤더에서 추출
```
   주의: 기존 broadcast 호출에 이미 token이 없는 경우, ws에서 token을 조회하는 방법 확인:
   - harness_server.py에서 ws별 token을 저장하는 방식 확인 후 접근
   - HARNESS_TOKENS dict에서 ws와 매칭되는 token 조회 또는 ws 연결 시 저장된 속성 사용

8. tests/test_harness_server.py에 TestDeltaReplay 클래스 추가:
```python
class TestDeltaReplay:
    def test_no_header_no_replay(self): ...
    def test_resume_from_filters_events(self): ...
    def test_resume_from_zero_sends_all(self): ...
    def test_non_integer_header_ignored(self): ...
    def test_overflow_header_ignored(self): ...
    def test_resume_session_loads_state(self): ...
    def test_resume_session_not_found_sends_error(self): ...
    def test_token_hash_returns_8_char_hex(self): ...
    def test_room_member_joined_has_user_field(self): ...
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestDeltaReplay -x -v && .venv/bin/python -m pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _run_session()에 x-resume-from 헤더 파싱 코드가 있다 (isdigit() + 2^31 상한선 체크)
    - delta 재송신 블록이 room_joined 이후에 있다
    - room_joined send()에 members=[] 필드가 추가됐다
    - harness_server.py에 `x-resume-session` 문자열이 있다 (B3)
    - harness_server.py에 `sess.load(` 또는 `session.store.load(` 호출이 있다 (B3)
    - harness_server.py에 `_token_hash` 함수 정의가 있다 (B4)
    - harness_server.py의 room_member_joined broadcast에 user 필드가 있다 (B4)
    - Room dataclass에 `_cancel_requested` 필드가 있다 (B2 준비)
    - TestDeltaReplay 9건 green
    - 전체 pytest green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PEXT-05 + B2 — cancel 케이스 + _cancel_requested 플래그 + on_token 콜백 체크</name>
  <files>harness_server.py, tests/test_harness_server.py</files>
  <read_first>
    - harness_server.py (_dispatch_loop() 전체 — elif 체인 구조 확인, confirm_write_response 가드 패턴 확인)
    - harness_server.py (_handle_input() 전체 — except/finally 블록 구조 확인)
    - harness_server.py (run_agent() 함수 — on_token 콜백 정의 위치 확인. run_in_executor 호출 구조 파악)
    - tests/test_harness_server.py (TestCancelTask 클래스 미존재 확인)
  </read_first>
  <behavior>
    - Test 1: active_input_from과 동일한 ws가 cancel 전송 시 input_tasks의 모든 task가 cancel()된다
    - Test 2: active_input_from이 아닌 ws가 cancel 전송 시 아무 일도 일어나지 않는다 (관전자 취소 방지)
    - Test 3: cancel 메시지 수신 후 broadcast(room, type='agent_cancelled')가 호출된다
    - Test 4: cancel 메시지 수신 후 room._cancel_requested가 True가 된다 (B2)
    - Test 5: _handle_input()에서 CancelledError 발생 시 finally 블록에서 room.busy=False, room.active_input_from=None으로 정리된다
    - Test 6: task.cancel()이 이미 done() 상태인 task에 호출되어도 오류 없이 처리된다
    - Test 7: _handle_input() 완료 시 finally 블록에서 room._cancel_requested가 False로 리셋된다 (B2)
  </behavior>
  <action>
1. _dispatch_loop()의 elif 체인에서 `elif t == 'ping':` 앞에 cancel 케이스 추가:
```python
elif t == 'cancel':
    # PEXT-05: 입력 주체(active_input_from)만 취소 가능 — confirm_write_response 와 동일한 DQ2 가드
    if ws is not room.active_input_from:
        continue
    # B2: executor 스레드 조기 종료를 위한 플래그 설정
    room._cancel_requested = True
    # input_tasks에서 살아있는 task를 cancel (done() 체크로 안전 처리)
    for task in list(room.input_tasks):
        if not task.done():
            task.cancel()
    await broadcast(room, type='agent_cancelled')
```

2. _handle_input() 함수의 except 블록에 CancelledError 처리 추가, finally에서 _cancel_requested 리셋:
```python
async def _handle_input(ws, room: 'Room', text: str):
    try:
        if text.startswith('/'):
            await handle_slash(ws, room, text)
        elif text.startswith('@claude '):
            await run_claude(ws, room, text[8:].strip(), add_to_session=True)
        else:
            await run_agent(ws, room, text)
            await broadcast_state(room)
    except asyncio.CancelledError:
        # PEXT-05: 정상 취소 경로 — busy/active_input_from은 finally에서 정리
        pass
    except Exception as e:
        await broadcast(room, type='error', text=f'입력 처리 오류: {e}')
    finally:
        room.busy = False
        room.active_input_from = None
        room._cancel_requested = False  # B2: 다음 실행을 위해 리셋
```
   주의: 기존 finally 블록이 이미 있으면 CancelledError except + _cancel_requested 리셋만 추가하고 나머지는 유지.

3. run_agent() 함수 내 on_token 콜백에서 _cancel_requested 플래그 체크 추가 (B2):
   - harness_server.py에서 run_agent()가 on_token 콜백을 정의하는 위치를 찾는다
   - on_token 콜백의 가장 첫 줄에 플래그 체크 추가:
```python
def on_token(token_text: str):
    # B2: _cancel_requested 플래그 체크 — executor 스레드 조기 종료
    if room._cancel_requested:
        return  # 더 이상 토큰을 처리하지 않음 (스트리밍 중단 효과)
    # 기존 on_token 로직 (broadcast 등) ...
```
   주의:
   - on_token은 executor 스레드에서 호출되므로 asyncio를 사용하지 않는다
   - room._cancel_requested는 bool 필드 — GIL로 읽기 안전
   - return만 하면 스트리밍이 중단되어 클라이언트에게 더 이상 token 이벤트가 전달되지 않음
   - 이미 broadcast된 토큰은 취소 불가 — 이것은 의도된 동작 (asyncio cancel과의 병행 처리)

4. tests/test_harness_server.py에 TestCancelTask 클래스 추가:
```python
class TestCancelTask:
    def test_cancel_by_active_input_from_cancels_tasks(self): ...
    def test_cancel_by_non_active_ignored(self): ...
    def test_cancel_broadcasts_agent_cancelled(self): ...
    def test_cancel_sets_cancel_requested_flag(self): ...  # B2
    def test_cancelled_error_clears_busy(self): ...
    def test_cancel_on_done_task_safe(self): ...
    def test_handle_input_finally_resets_cancel_flag(self): ...  # B2
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_harness_server.py::TestCancelTask -x -v && .venv/bin/python -m pytest -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - _dispatch_loop()에 `elif t == 'cancel':` 케이스가 있다
    - cancel 케이스에 `ws is not room.active_input_from` 가드가 있다
    - cancel 케이스에 `room._cancel_requested = True` 가 있다 (B2)
    - task.done() 체크 후 cancel() 호출 패턴이 있다
    - broadcast(room, type='agent_cancelled') 호출이 있다
    - _handle_input()에 `except asyncio.CancelledError: pass`가 있다
    - _handle_input() finally에 `room._cancel_requested = False` 리셋이 있다 (B2)
    - run_agent()의 on_token 콜백에 `if room._cancel_requested: return` 이 있다 (B2)
    - harness_server.py에 `_cancel_requested` 문자열이 있다 (acceptance: `grep -c '_cancel_requested' harness_server.py` >= 4)
    - harness_server.py에 `x-resume-session` 문자열이 있다 (B3)
    - TestCancelTask 7건 green
    - 전체 pytest green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 연결 헤더 → _run_session() | 헤더 값 파싱 시 isdigit() + 상한선으로 주입 방지 |
| cancel 메시지 → task cancel | active_input_from 가드로 권한 확인 |
| x-resume-session 헤더 → session 파일 로드 | session ID를 파일 경로로 사용 시 path traversal 방지 필요 |
| room_member_joined → token_hash | SHA-256 앞 8자 — 토큰 원문 미노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | x-resume-from 헤더 정수 주입 | mitigate | `isdigit()` 체크 + `< 2**31` 상한선 — 부동소수/음수/대형 정수 모두 거부 |
| T-03-02-02 | Tampering | 관전자 cancel 권한 우회 | mitigate | `ws is not room.active_input_from` identity 가드 — confirm_write_response와 동일 패턴 (VERIFIED: harness_server.py line 694) |
| T-03-02-03 | Denial of Service | cancel 반복 전송으로 task.cancel() 남용 | mitigate | `task.done()` 체크 후 cancel 호출. 이미 완료된 task에 cancel은 False 반환이며 무해 |
| T-03-02-04 | Tampering | resume_from=0 으로 ring buffer 전체 재송신 강제 | accept | 인증된 클라이언트만 연결 가능. ring buffer는 60초 이내 이벤트만 보유 (최대 10000건). 3인 스케일에서 부하 무시 가능 |
| T-03-02-05 | Tampering | x-resume-session으로 타인 세션 파일 접근 | mitigate | sess.load()가 SESSION_DIR 내 파일만 허용. FileNotFoundError 시 error 이벤트 전송으로 graceful 처리 |
| T-03-02-06 | Information Disclosure | token_hash를 통한 토큰 역산 가능성 | accept | SHA-256 앞 8자 = 4바이트 = 32비트 공간. 3인 스케일에서 충돌 가능성 무시. 토큰 역산은 SHA-256 preimage 문제 |
</threat_model>

<verification>
1. `grep -n 'resume_from' /Users/johyeonchang/harness/harness_server.py` — _run_session() 내 파싱 + delta 재송신 2개 블록 확인
2. `grep -n 'x-resume-session' /Users/johyeonchang/harness/harness_server.py` — _run_session() 내 1건 확인 (B3)
3. `grep -n "'cancel'" /Users/johyeonchang/harness/harness_server.py` — _dispatch_loop() 내 cancel 케이스 확인
4. `grep -n 'CancelledError' /Users/johyeonchang/harness/harness_server.py` — _handle_input() 내 except 블록 확인
5. `grep -n '_cancel_requested' /Users/johyeonchang/harness/harness_server.py` — 4건 이상 (dataclass 필드 + cancel에서 True + finally에서 False + on_token에서 체크)
6. `grep -n 'agent_cancelled' /Users/johyeonchang/harness/harness_server.py` — broadcast 호출 확인
7. `grep -n '_token_hash' /Users/johyeonchang/harness/harness_server.py` — 함수 정의 + room_member_joined broadcast 2건 (B4)
8. `.venv/bin/python -m pytest -x --tb=short` — 전체 pytest green
</verification>

<success_criteria>
- harness_server.py에 PEXT-04, PEXT-05 구현 완료
- x-resume-from 헤더 파싱이 isdigit() + 2^31 상한선 검증을 포함한다
- cancel 케이스가 active_input_from 가드를 통과한 경우에만 task.cancel()을 실행한다
- Room._cancel_requested 플래그가 cancel 시 True, handle_input finally에서 False로 리셋된다 (B2)
- on_token 콜백에서 _cancel_requested 체크로 executor 스레드가 조기 종료된다 (B2)
- x-resume-session 헤더 파싱 + sess.load() 호출이 구현된다 (B3)
- room_member_joined broadcast에 user=_token_hash(token) 필드가 포함된다 (B4)
- CancelledError가 _handle_input() finally에서 정리된다
- pytest 전체 green (기존 대비 16건 이상 증가)
</success_criteria>

<output>
완료 후 `.planning/phases/03-remote-room-session-control/03-02-SUMMARY.md` 작성.
</output>
